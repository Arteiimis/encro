# Quick Task 260506-44c: 优化buildMediaPackPlan函数 - Context

**Gathered:** 2026-05-05
**Status:** Ready for planning

<domain>
## Task Boundary

优化 `buildMediaPackPlan` 函数（`src/pack/pack.cpp:108-286`，~178行），提取各逻辑段为独立辅助函数，降低认知负载。目标：缩减至 ~25 行编排器，零头文件变更。

</domain>

<decisions>
## Implementation Decisions

### Extraction Granularity
- **激进: 全部提取** — 6 个辅助函数，`buildMediaPackPlan` 缩减至 ~25 行编排器
- 提取：entry 转换、summary 追加、阈值解析、partition+转换、entryName 覆盖、zipName 策略

### Entry 转换 (段①)
- **单个函数** `collectPackInputs(PackRequest const& request) -> std::vector<pack::detail::PackEntryInput>`
- 封装：namingStrategy 解析、Keep 的 common root 计算、entries 循环 + switch

### Zip Name 策略 (段⑥)
- **提取为独立函数** `resolveZipNameStrategy(PackRequest const& request, ...) -> std::function<std::string(std::size_t)>`
- 封装消费者策略分支 + 默认策略调用

### Partition + Convert + Stable Partition (段④)
- **提取为单个流程** `partitionPackInputs(packInputs, keepTogetherThreshold) -> tuple<groupedEntries, groupNameParts, subPartCountsByPart>`
- 封装：`groupPackEntriesWithSubparts` → 转换为 groupedEntries → stable_partition

### 命名惯例
- **动词驱动 (简短的)** — 如 `collectPackInputs`、`appendSummaryEntries`、`resolveKeepTogetherThreshold`、`partitionPackInputs`、`applyEntryNameOverrides`、`resolveZipNameStrategy`

### Function Placement
- 全部置于 `src/pack/pack.cpp` 匿名命名空间，零头文件变更
- 遵循上次 `260505-vyf` 的模式

</decisions>

<specifics>
## Specific Ideas

### 提取的函数清单

| 函数 | 行数 | 职责 |
|------|------|------|
| `collectPackInputs` | ~65 | entry → PackEntryInput 向量转换（命名策略 + common root） |
| `appendSummaryEntries` | ~20 | 将 summary entries 追加到 packInputs |
| `resolveKeepTogetherThreshold` | ~8 | groupingStrategy → std::optional<size_t> |
| `partitionPackInputs` | ~30 | packInputs → groupedEntries + nameParts + subPartCounts（含 stable_partition） |
| `applyEntryNameOverrides` | ~8 | entryNameForFile 回调应用到每个 entry |
| `resolveZipNameStrategy` | ~30 | 消费者策略或默认 → zipNameForIndex lambda |
| `buildMediaPackPlan` | ~25 | 缩减后的编排器 |

### buildMediaPackPlan 缩减后 (目标形态)

```cpp
auto buildMediaPackPlan(PackRequest const& request) -> eh::Result<PackPlan> {
  // 1. 构建 packInputs + 追加 summary
  auto packInputs = collectPackInputs(request);
  appendSummaryEntries(request, packInputs);

  // 2. 分区 + 分组
  auto const keepTogether = resolveKeepTogetherThreshold(request);
  auto [groups, nameParts, subPartCounts] = partitionPackInputs(packInputs, keepTogether);

  // 3. entryName 覆盖
  applyEntryNameOverrides(request, groups);

  // 4. 命名配置
  auto const baseName = request.naming.has_value() && request.naming->baseName.has_value()
    ? request.naming->baseName.value() : std::string{};
  auto const ordinalRanges = pack::internal::buildGroupOrdinalRanges(groups);
  auto zipNameFn = resolveZipNameStrategy(request, baseName, ordinalRanges, nameParts, subPartCounts);

  return PackPlan{
    .groups = std::move(groups),
    .outputDir = request.outputDir,
    .zipNameForIndex = std::move(zipNameFn),
    .maxParallelJobs = request.maxParallelJobs,
    .removeOnFailure = request.removeOnFailure,
    .compact = request.compact,
  };
}
```

</specifics>

<canonical_refs>
## Canonical References

参考上次 task `260505-vyf` (runPicturePackWorkflow 优化) 的模式：匿名命名空间提取，零头文件变更，编排器缩减至调度器。

</canonical_refs>
