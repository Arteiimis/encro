# Quick Task 260429-1yf: 打包进度条未计入zip关闭耗时 - Plan

**Created:** 2026-04-28
**Status:** Ready for execution

## Task

### Task 1: 为 compact 模式添加 zip 关闭期间的 Finalizing 旋转指示器

**Files:** `src/pack/packer.h`, `src/pack/packer.cpp`, `src/pack/pack_service.cpp`

**Action:**
1. `packer.h`: 给 compact 版 `packFilesToZip` 签名增加 `std::atomic<std::size_t>* finalizingCount = nullptr` 参数
2. `packer.cpp`: 在 `zip.close()` 前后对 `finalizingCount` 做 `fetch_add(1)` / `fetch_sub(1)`
3. `pack_service.cpp:packGroups()`: 新增 `std::atomic<std::size_t> finalizingCount{0}`，启动 spinner jthread 当 `finalizingCount > 0` 时旋转更新进度条文本追加 `| Finalizing /`，传递 `&finalizingCount` 到 compact 任务，任务完成后停止 spinner

**Verify:**
- 运行 `tests/pack_service_tests.cpp` 确保现有测试通过
- 手动验证：打包大文件时进度条在100%后显示 Finalizing 旋转指示器，zip 保存完成后恢复正常

**Done:** Progress bar shows "Finalizing /" spinner during `zip.close()` in compact mode.
