---
phase: 05-picture-refactor-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/picture/picture_process.cpp
autonomous: true
requirements:
  - REF-04

must_haves:
  truths:
    - "toJpgEntryName is a static free function in the anonymous namespace, not a lambda variable"
    - "All 3 call sites use the function without change to behavior"
    - "addCompressTask lambda body continues to compile and function correctly"
  artifacts:
    - path: "src/picture/picture_process.cpp"
      provides: "toJpgEntryName static free function in anonymous namespace"
      contains: "auto toJpgEntryName(std::string const& entryName) -> std::string"
  key_links:
    - from: "addCompressTask lambda body (line ~332)"
      to: "toJpgEntryName free function"
      via: "direct call (unchanged syntax, no longer a capture)"
      pattern: "toJpgEntryName\\(entryName\\)"
    - from: "packInputs building loop (line ~392)"
      to: "toJpgEntryName free function"
      via: "direct call"
      pattern: "toJpgEntryName\\(buildSummaryPictureEntryName"
    - from: "packInputs building loop (lines ~414-415)"
      to: "toJpgEntryName free function"
      via: "direct call in ternary expressions"
      pattern: "toJpgEntryName\\((plannedIt->second|picPath\\.filename)"
---

<objective>
Extract the `toJpgEntryName` lambda variable (lines 322-326) to a static free function in the anonymous namespace of `picture_process.cpp`.

Purpose: Per D-03 and D-06, `toJpgEntryName` is a pure function with zero captures — trivially extractable. This extraction simplifies `addCompressTask`'s capture list (removes one implicit `[&]` capture) before Plan 02 extracts it.

Output: A static free function `toJpgEntryName` in the anonymous namespace, with all 3 call sites updated (syntax is unchanged — `toJpgEntryName(...)` already reads as a function call).
</objective>

<execution_context>
@C:/Users/LEGION/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LEGION/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-picture-refactor-validation/05-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/04-pack-subsystem-refactor/04-02-SUMMARY.md

<interfaces>
<!-- Key types and conventions the executor needs -->

From CONVENTIONS.md:
- **Naming:** camelCase free functions, trailing return type (`auto fn() -> ReturnType`)
- **Anonymous namespace:** `namespace { ... }  // namespace` for internal linkage in .cpp files
- **Formatting:** clang-format applied via pre-commit hook (external config at `D:/clangformat/.clang-format`)

From src/picture/picture_process.cpp (current state):
- Lambda at lines 322-326:
```cpp
auto toJpgEntryName = [](std::string const& entryName) -> std::string {
    auto const stemEnd = entryName.rfind('.');
    if (stemEnd != std::string::npos) { return entryName.substr(0, stemEnd) + ".jpg"; }
    return entryName + ".jpg";
};
```

From Phase 3/4 established patterns (D-01, D-03):
- Extracted functions go in the anonymous namespace as free functions
- Pure functions with no captures: extract to static free functions
- Do NOT add to header — internal linkage only

Call sites for toJpgEntryName (all within `runPicturePackWorkflow`):
1. Line ~334 (inside addCompressTask lambda): `auto const jpgEntryName = toJpgEntryName(entryName);`
2. Line ~392: `auto const entryName = toJpgEntryName(buildSummaryPictureEntryName(dirPath, summaryPic));`
3. Lines ~414-415 (ternary expressions):
   - `? toJpgEntryName(plannedIt->second)`
   - `: toJpgEntryName(picPath.filename().generic_string());`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract toJpgEntryName to static free function</name>
  <files>src/picture/picture_process.cpp</files>

  <read_first>
    - src/picture/picture_process.cpp — Read lines 27-60 (anonymous namespace top), lines 320-326 (lambda definition), lines 330-360 (addCompressTask body + call sites), lines 385-415 (packInputs call sites)
  </read_first>

  <action>
Extract the `toJpgEntryName` lambda into a static free function in the anonymous namespace, following Phase 3 D-01 (anonymous namespace free functions) and D-03 (static extraction — no RED gate needed per D-06).

**Step 1: Add function in anonymous namespace**
Insert after `buildConflictHandledPictureEntryName` (line ~59, before `planPictureZipEntryNames`) in the anonymous namespace:

```cpp
auto toJpgEntryName(std::string const& entryName) -> std::string {
  auto const stemEnd = entryName.rfind('.');
  if (stemEnd != std::string::npos) { return entryName.substr(0, stemEnd) + ".jpg"; }
  return entryName + ".jpg";
}
```

Function signature is identical to the lambda body — trailing return type per convention, same logic.

**Step 2: Remove the lambda variable**
Delete lines 322-326 (the `auto toJpgEntryName = [](...) { ... };` declaration). Keep a blank line to preserve spacing. Remove the blank line that separates the lambda from `addCompressTask` if it creates a double-blank.

**Step 3: Verify call sites (NO changes needed)**
All 3 call sites use `toJpgEntryName(...)` — identical syntax to the lambda call. The compiler resolves to the free function (not a captured variable) automatically. Verify:

- Line ~334 (inside addCompressTask): `auto const jpgEntryName = toJpgEntryName(entryName);` — unchanged
- Line ~392: `auto const entryName = toJpgEntryName(buildSummaryPictureEntryName(...));` — unchanged
- Lines ~414-415: `toJpgEntryName(plannedIt->second)` and `toJpgEntryName(picPath.filename().generic_string())` — unchanged

**Step 4: Build and verify**
Run `xmake build` to confirm compilation succeeds. The `[&]` capture in `addCompressTask` will automatically stop capturing `toJpgEntryName` (it's no longer a local variable).
  </action>

  <verify>
    <automated>xmake build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "auto toJpgEntryName = \[\]" src/picture/picture_process.cpp` returns 0 (lambda variable removed)
    - `grep -c "auto toJpgEntryName(std::string const& entryName) -> std::string" src/picture/picture_process.cpp` returns 1 (function exists)
    - `grep -c "toJpgEntryName(" src/picture/picture_process.cpp` returns 6 (3 call sites = 3 pairs: definition + 3 calls + 2 packInputs calls = wait, let me recount)
    - `grep -n "toJpgEntryName" src/picture/picture_process.cpp` shows:
      - One function definition (Line with `auto toJpgEntryName(std::string const&`)
      - 3 call sites within `runPicturePackWorkflow` body (one in addCompressTask, two in packInputs building)
    - `xmake build` exits 0
  </acceptance_criteria>

  <done>toJpgEntryName is a free function in the anonymous namespace. All call sites compile unchanged. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None affected — this is a pure code reorganization with zero behavioral change. No new inputs, outputs, or trust boundaries.

## STRIDE Threat Register
No threats applicable — function logic is byte-identical to the original lambda (copy-paste extraction).
</threat_model>

<verification>
- [ ] `xmake build` exits 0
- [ ] `grep "auto toJpgEntryName = \[\]" src/picture/picture_process.cpp` returns nothing (lambda removed)
- [ ] `grep "auto toJpgEntryName(std::string const& entryName)" src/picture/picture_process.cpp` finds the function definition in the anonymous namespace
- [ ] `xmake run --workdir="." tests "[picture-process][compress]"` passes all compress-related picture tests
</verification>

<success_criteria>
1. The `toJpgEntryName` lambda variable no longer exists in `picture_process.cpp`
2. A static free function `toJpgEntryName` exists in the anonymous namespace (lines 27-268)
3. All 3 call sites use the free function (syntax unchanged — `toJpgEntryName(...)`)
4. Build succeeds without errors
5. Existing compress-picture tests pass (they exercise the full compress path including toJpgEntryName via `runPicturePackWorkflow`)
</success_criteria>

<output>
After completion, create `.planning/phases/05-picture-refactor-validation/05-01-SUMMARY.md`
</output>
