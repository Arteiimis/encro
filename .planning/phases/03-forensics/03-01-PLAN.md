---
phase: 03-forensics
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/logging/logging.h
  - tests/logging_error_context_test.cpp
autonomous: true
requirements:
  - FOR-03

must_haves:
  truths:
    - "ScopedErrorContext pushes a frame onto TLS stack on construction"
    - "ScopedErrorContext pops the frame on destruction"
    - "Moved-from ScopedErrorContext does not double-pop"
    - "Nested ScopedErrorContext produces correctly ordered chain (outermost first)"
    - "Context depth is bounded to 16 frames with FIFO eviction and [truncated] marker"
    - "ScopedErrorContext destructor is noexcept and move-only"
  artifacts:
    - path: "src/logging/logging.h"
      provides: "ScopedErrorContext class, ContextFrame struct, TLS stack helpers, formatContextChain()"
      contains: "ScopedErrorContext"
      contains: "ContextFrame"
      contains: "formatContextChain"
    - path: "tests/logging_error_context_test.cpp"
      provides: "TDD test coverage for ScopedErrorContext lifecycle and context chain formatting"
      min_lines: 200
  key_links:
    - from: "ScopedErrorContext constructor"
      to: "detail::pushContextFrame"
      via: "function call in constructor body"
    - from: "ScopedErrorContext destructor"
      to: "detail::popContextFrame"
      via: "function call in destructor body (guarded by movedFrom_)"
    - from: "detail::formatContextChain"
      to: "TLS context stack"
      via: "thread_local std::vector<ContextFrame> read"
---

<objective>
Implement ScopedErrorContext -- the RAII class that pushes/pops context frames onto a thread-local stack,
plus the formatContextChain() helper that serializes the stack into a formatted string per D-03/D-04.

Purpose: Establish the core forensic infrastructure that the LOG_ERROR/LOG_CRITICAL macros (Plan 03-02)
and pipeline placement (Plan 03-03) build upon. Follows the established ScopedTimer RAII pattern from Phase 2.

Output: ScopedErrorContext class in src/logging/logging.h, context chain formatting helper,
and comprehensive TDD test suite in tests/logging_error_context_test.cpp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-forensics/03-CONTEXT.md
@.planning/phases/03-forensics/03-RESEARCH.md
@.planning/phases/02-file-management-observability/02-CONTEXT.md

<interfaces>
<!-- ScopedTimer reference pattern from Phase 2 that ScopedErrorContext mirrors -->
From src/logging/logging.h (lines 153-193):
  - ScopedTimer class: constructor takes std::string_view stageName, logs begin; destructor noexcept,
    checks movedFrom_, logs elapsed; copy deleted; move constructor marks other.movedFrom_=true;
    move assignment self-guards with `if (this != &other)`

From tests/logging_scoped_timer_test.cpp (lines 19-41):
  - registerCapturingLoggerForTimer() helper pattern: creates ostream_sink, registers logger,
    returns pair<shared_ptr<logger>, ostringstream*>

From src/core/error_handle.h:
  - eh::Result<T> = std::expected<T, std::string> (NOT modified per D-09)

From src/logging/log_tags.h:
  - logtags::TEST_INFRA = "test.infra" (existing test logger tag)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for ScopedErrorContext lifecycle and context chain formatting</name>
  <files>tests/logging_error_context_test.cpp</files>
  <read_first>
    - src/logging/logging.h (lines 153-193: ScopedTimer class pattern to mirror; lines 68-126: existing LOG_* macros)
    - src/logging/log_tags.h (logtags::TEST_INFRA for DEFINE_LOGGER)
    - tests/logging_scoped_timer_test.cpp (lines 19-41: registerCapturingLoggerForTimer helper; full file for test structure patterns)
    - .planning/phases/03-forensics/03-CONTEXT.md (D-01 through D-04: context stack design, depth limit, serialization format)
    - .planning/phases/03-forensics/03-RESEARCH.md (Pattern 1: Thread-Local RAII Context Stack; Code Examples section for expected class + macro design)
  </read_first>
  <action>
    Create tests/logging_error_context_test.cpp with these Catch2 TEST_CASE entries (tagged [logging][error_context][scoped_error_context]):

    Test 1 — ScopedErrorContext pushes frame on construction: Create a ScopedErrorContext, call formatContextChain(), verify the output contains the stage name and (if detail non-empty) the detail value.

    Test 2 — ScopedErrorContext pops frame on destruction: Create a ScopedErrorContext inside a block scope, verify formatContextChain() is non-empty inside the block, then verify formatContextChain() is empty after the block exits.

    Test 3 — ScopedErrorContext is not copyable: STATIC_CHECK_FALSE on std::is_copy_constructible_v and std::is_copy_assignable_v.

    Test 4 — ScopedErrorContext destructor is noexcept: STATIC_CHECK on std::is_nothrow_destructible_v.

    Test 5 — Moved-from ScopedErrorContext does not double-pop: Create a ScopedErrorContext, move-construct another from it. Destroy the original (moved-from) — verify the frame is still on the stack (formatContextChain non-empty). Destroy the moved-to — verify the frame is popped (formatContextChain empty). Verify exactly one pop occurred.

    Test 6 — Nested ScopedErrorContext produces ordered chain: Push "outer" then "inner". Verify formatContextChain() renders "outer" before "inner" (left-to-right = push order). Note: C++ destruction order is reverse of construction, so formatContextChain() called inside the inner scope should see [outer > inner] in that order.

    Test 7 — Self-move-assignment is safe: Create a ScopedErrorContext "self", perform `t = std::move(t)`, destroy it. Verify exactly one frame was pushed and popped (no double-pop, no crash).

    Test 8 — Context depth limit 16 frames with truncation: Push 20 frames. Verify formatContextChain() contains "[truncated: 4]" marker and only the 16 most recent frames (frames 5-20, not frames 1-4).

    Test 9 — Empty stage name edge case: Create ScopedErrorContext with "" stage and "" detail. Verify formatContextChain() does not crash and returns a non-empty string (containing at least the context bracket).

    Test 10 — Context chain format matches D-03/D-04: Push frames with stage="encode", detail="retry 2/3" then stage="ffmpeg", detail="exit 1". Verify formatContextChain() produces " [context: encode(retry 2/3) > ffmpeg(exit 1)]". Also test: detail="" renders as "stage" without empty parentheses.

    Test 11 — Empty TLS stack produces empty string: Call formatContextChain() with no active ScopedErrorContext. Verify it returns "" (empty string).

    Each test uses a helper function `registerCapturingLoggerForContext(char const* name)` (adapting the existing registerCapturingLoggerForTimer pattern from logging_scoped_timer_test.cpp) and DEFINE_LOGGER(logtags::TEST_INFRA) at file scope.

    Include a `ScopedContextReset` RAII helper (or call a `resetContextStack()` function) at the start of each TEST_CASE to clear the TLS stack and prevent cross-test contamination.
  </action>
  <acceptance_criteria>
    - All 11 tests compile and FAIL (RED phase) because ScopedErrorContext class does not exist yet.
    - Expected failures: compilation errors for undefined `logging::ScopedErrorContext`, `logging::detail::formatContextChain()`, `logging::detail::pushContextFrame/popContextFrame`.
    - Verification command: `xmake build tests 2>&1` must show compilation FAILURE for logging_error_context_test.cpp.
    - The test file includes: `#include "logging/logging.h"`, `#include "logging/log_tags.h"`, `#include <spdlog/spdlog.h>`, `#include <spdlog/sinks/ostream_sink.h>`, `#include <catch2/catch_all.hpp>`
  </acceptance_criteria>
  <verify>
    <automated>xmake build tests 2>&1 | grep -c "error:" ; test $? -eq 0 (compilation fails = RED phase success)</automated>
  </verify>
  <done>All 11 tests written and compilation fails because implementation classes/functions don't exist yet.</done>
</task>

<task type="tdd">
  <name>Task 2: Implement ScopedErrorContext + TLS stack + formatContextChain to pass tests (GREEN)</name>
  <files>src/logging/logging.h</files>
  <read_first>
    - src/logging/logging.h (lines 153-193: ScopedTimer class — exact pattern to mirror for constructor/destructor/move/movedFrom_ flag)
    - src/logging/logging.h (lines 1-11: existing includes; lines 18-28: shortFile helper — shows the detail namespace pattern)
    - src/logging/logging.h (lines 128-136: loggerPtr forward declaration pattern for inline method calls)
    - tests/logging_error_context_test.cpp (the tests written in Task 1 — implement to make these pass)
    - .planning/phases/03-forensics/03-CONTEXT.md (D-01, D-02, D-03, D-04, D-07: all design constraints)
    - .planning/phases/03-forensics/03-RESEARCH.md (Pattern 1: Thread-Local RAII Context Stack; Code Examples section for ScopedErrorContext class definition and context frame rendering)
    - .planning/phases/02-file-management-observability/02-CONTEXT.md (D-11: noexcept destructor precedent)
  </read_first>
  <action>
    Add to src/logging/logging.h, in the `namespace logging` block (after ScopedTimer, before the closing `} // namespace logging`):

    A) ContextFrame struct:
    ```cpp
    struct ContextFrame {
        std::string_view stage;
        std::string_view detail;
    };
    ```

    B) In namespace `logging::detail`, add TLS stack management (declared as inline functions or regular functions with implementation in the header):

    - Declare `auto pushContextFrame(std::string_view stage, std::string_view detail) -> void` — pushes a ContextFrame onto a `thread_local std::vector<ContextFrame>`. Enforces D-02: max 16 frames. When the stack is at capacity, drop the oldest frame (vector::erase at index 0) before pushing. Track dropped count in a thread_local counter for the truncation marker.

    - Declare `auto popContextFrame() -> void` — pops the most recent frame from the TLS stack. No-op if stack is empty.

    - Declare `auto formatContextChain() -> std::string` — reads the TLS stack and returns a formatted string per D-03/D-04. Returns "" (empty string) when stack is empty. Format per D-03: `" [context: frame1 > frame2 > ...]"`. Per D-04: each frame renders as `"stage(detail)"` when detail is non-empty, `"stage"` when detail is empty. When frames were dropped due to overflow, prepend `"(truncated N) > "` before the chain (where N is the number of dropped frames). Include a header in logging.h: `#include <vector>`.

    Implementation notes for formatContextChain():
    - The truncation marker format: `"(truncated " + std::to_string(droppedCount) + ") > "` prepended before the first frame.
    - Frame rendering: if `frame.detail.empty()` → `frame.stage`; else → `std::format("{}"("{}")", frame.stage, frame.detail)` but using fmt or string concatenation. Actually, use simple string building since stage/detail are string_views. Build with: `result += frame.stage; if (!frame.detail.empty()) { result += "("; result += frame.detail; result += ")"; }`.
    - Chain joining: iterate frames, add `" > "` between them.
    - Return `" [context: " + chain + "]"` when non-empty, `""` when empty.

    C) ScopedErrorContext class in namespace logging (add to logging.h BEFORE the closing `} // namespace logging`, next to ScopedTimer):

    Pattern: mirror ScopedTimer exactly (lines 155-193) with these differences:
    - Constructor takes `std::string_view stage, std::string_view detail` and calls `detail::pushContextFrame(stage, detail)`. Does NOT log (unlike ScopedTimer which logs "begin").
    - Destructor is noexcept, checks `movedFrom_`, calls `detail::popContextFrame()`. Does NOT log (unlike ScopedTimer which logs "completed").
    - Move constructor: copy stageName_ pattern → mark `other.movedFrom_ = true`.
    - Move assignment: self-guard with `if (this != &other)`, then same logic as ScopedTimer's operator=.
    - Copy constructor and copy assignment: = delete.
    - Members: replace `stageName_` and `start_` with a bool `movedFrom_{false}`.
    - The movedFrom_ flag prevents double-pop: if destructor sees movedFrom_ == true, return immediately.

    D) Forward declaration of loggerPtr — ensure the existing forward declaration block (lines 128-136) covers the new code. Since ScopedErrorContext does not call LOG_* macros, no forward declaration adjustment is needed.

    E) Add `#include <vector>` near the top of logging.h (after existing includes).

    Ensure all code follows conventions: east const, trailing return type, PascalCase types, camelCase functions, `noexcept` where appropriate. Place ContextFrame and ScopedErrorContext in namespace logging. Place pushContextFrame/popContextFrame/formatContextChain in namespace logging::detail.
  </action>
  <acceptance_criteria>
    - `xmake build tests` compiles successfully (no errors).
    - `xmake run tests "[logging][error_context]"` — all tests from Task 1 pass (GREEN).
    - ScopedErrorContext destructor is noexcept (static_assert in test verifies).
    - ScopedErrorContext is move-only (copy deleted, static_assert in test verifies).
    - formatContextChain() returns "" when no ScopedErrorContext is active.
    - formatContextChain() returns " [context: encode(retry 2/3) > ffmpeg(exit 1)]" for two-frame stack with details.
    - formatContextChain() returns " [context: scan > probe]" for two-frame stack without details.
    - Pushing 20 frames: formatContextChain() contains "(truncated 4)" marker and only 16 most recent frame contents.
  </acceptance_criteria>
  <verify>
    <automated>xmake build tests && xmake run tests "[logging][error_context]"</automated>
  </verify>
  <done>All 11 ScopedErrorContext tests pass. RED→GREEN cycle complete.</done>
</task>

<task type="tdd">
  <name>Task 3: Refactor — verify noexcept, code style, and clang-format (REFACTOR)</name>
  <files>src/logging/logging.h, tests/logging_error_context_test.cpp</files>
  <read_first>
    - src/logging/logging.h (full file: verify ScopedErrorContext implementation follows ScopedTimer pattern)
    - tests/logging_error_context_test.cpp (full file: verify test quality and coverage)
    - .claude/skills/ or .agents/skills/ directory if either exists (project skill rules)
  </read_first>
  <action>
    Review and refactor under test protection:

    1. Verify ScopedErrorContext matches ScopedTimer's move-assignment self-guard pattern: `if (this != &other)`.
    2. Verify all functions use trailing return type syntax.
    3. Verify east const on all declarations.
    4. Verify ContextFrame uses PascalCase, ScopedErrorContext uses PascalCase.
    5. Run `xmake format` to apply clang-format.
    6. Verify all tests still pass after formatting.
    7. Check for any unused includes in logging.h (the new `#include <vector>` must be present; verify no other new unnecessary includes).
    8. Verify thread_local variable is declared inside `logging::detail` namespace, not at global scope.
  </action>
  <acceptance_criteria>
    - `xmake run tests "[logging][error_context]"` — all 11 tests still pass.
    - `xmake format -k check` exits 0 (no formatting issues).
    - No compilation warnings from `xmake build tests -v 2>&1`.
  </acceptance_criteria>
  <verify>
    <automated>xmake run tests "[logging][error_context]"</automated>
    <automated>xmake format -k check</automated>
  </verify>
  <done>Code is formatted, tests pass, implementation follows ScopedTimer pattern and project conventions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TLS stack access | Context frames pushed/popped only via ScopedErrorContext RAII; formatContextChain() is read-only |
| string_view in ContextFrame | stage and detail point to caller-owned strings; validity depends on caller lifetime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | TLS context stack | mitigate | Max 16 frames per D-02; FIFO eviction with truncation marker prevents unbounded growth |
| T-03-02 | Tampering | ScopedErrorContext destructor | mitigate | movedFrom_ flag prevents double-pop; copy deleted prevents duplicate guards |
| T-03-03 | Information Disclosure | formatContextChain() | accept | Context chain contains file paths already visible in progress bars and log output; no new exposure |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package dependencies in this phase; all dependencies already installed and audited |
</threat_model>

<verification>
After all tasks complete:
- `xmake build tests && xmake run tests "[logging][error_context]"` — all 11+ tests green
- `xmake format -k check` exits 0
</verification>

<success_criteria>
- [ ] ScopedErrorContext class exists in src/logging/logging.h alongside ScopedTimer
- [ ] TLS context stack with pushContextFrame/popContextFrame/formatContextChain in logging::detail
- [ ] Context depth limited to 16 frames with truncation marker
- [ ] 11 TDD tests pass covering lifecycle, move semantics, nesting, truncation, formatting
- [ ] Code passes clang-format check
</success_criteria>

<output>
Create .planning/phases/03-forensics/03-01-SUMMARY.md when done
</output>
