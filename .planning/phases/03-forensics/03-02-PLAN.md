---
phase: 03-forensics
plan: 02
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/logging/logging.h
  - src/logging/setup.h
  - src/logging/setup.cpp
  - src/core/app_context.h
  - src/utils/utils.h
  - src/utils/utils.cpp
  - tests/logging_error_context_test.cpp
  - tests/logging_snapshot_test.cpp
autonomous: true
requirements:
  - FOR-01
  - FOR-02

must_haves:
  truths:
    - "LOG_ERROR appends context chain when ScopedErrorContext is active"
    - "LOG_ERROR produces no context suffix when TLS stack is empty (no regression)"
    - "LOG_CRITICAL appends context chain when ScopedErrorContext is active"
    - "LOG_ERROR/LOG_CRITICAL emit environment snapshot as a separate log line after the error"
    - "Environment snapshot includes active slots, pending count, FFmpeg PID/cmdline when encoding active"
    - "Environment snapshot is safe when no encoding is active (minimal snapshot, no crash)"
    - "ExecResult carries subprocess PID for forensic tracking"
  artifacts:
    - path: "src/logging/logging.h"
      provides: "Modified LOG_ERROR/LOG_CRITICAL macros with context chain and snapshot trigger"
      contains: "do {"
      contains: "formatContextChain()"
    - path: "src/logging/setup.cpp"
      provides: "captureEnvironmentSnapshot() implementation and forensic context pointer storage"
      contains: "captureEnvironmentSnapshot"
      min_lines: 60
    - path: "src/core/app_context.h"
      provides: "EncodingState subprocess fields"
      contains: "subprocessPid"
      contains: "subprocessCmdline"
    - path: "src/utils/utils.h"
      provides: "ExecResult pid field"
      contains: "std::optional<int> pid"
  key_links:
    - from: "LOG_ERROR macro expansion"
      to: "formatContextChain()"
      via: "do-while block in macro body calls formatContextChain, appends result to message"
    - from: "LOG_ERROR macro expansion"
      to: "captureEnvironmentSnapshot()"
      via: "do-while block in macro body calls snapshot, logs result via SPDLOG_LOGGER_CALL"
    - from: "captureEnvironmentSnapshot()"
      to: "EncodingState::subprocessPid/cmdline"
      via: "reads stored AppContext pointer, iterates active encoding states"
    - from: "exec2Impl"
      to: "ExecResult::pid"
      via: "bp::child::id() captured before process exit"
---

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-forensics/03-CONTEXT.md
@.planning/phases/03-forensics/03-RESEARCH.md

<interfaces>
<!-- Plan 03-01 delivers (use these in macro modifications) -->
From src/logging/logging.h (after Plan 03-01):
  - logging::ScopedErrorContext class (RAII push/pop on TLS stack)
  - logging::detail::formatContextChain() -> std::string
  - logging::detail::pushContextFrame(std::string_view, std::string_view) -> void
  - logging::detail::popContextFrame() -> void
  - logging::ContextFrame struct { std::string_view stage, detail }

<!-- Existing macros from Phase 1 to modify -->
From src/logging/logging.h (lines 108-116 — current LOG_ERROR):
  #define LOG_ERROR(...) \
    SPDLOG_LOGGER_CALL(loggerPtr(), spdlog::level::err, "[{}:{}] {}", \
      logging::detail::shortFile(__FILE__), __LINE__, fmt::format(__VA_ARGS__))

From src/logging/logging.h (lines 118-126 — current LOG_CRITICAL): same pattern at level::critical

<!-- EncodingState to extend -->
From src/core/app_context.h (lines 68-87):
  struct EncodingState — has inputPath, actionId, outputPath, plannedOutputFile, outputFile,
  progressFilePath, barIndex, startTime, endTime, lastProgress, lastProgressAtomic,
  lastFrameCount, lastStatus, lastError, totalFrames, finished, success, mtx

<!-- ExecResult to extend -->
From src/utils/utils.h (lines 9-12):
  struct ExecResult { int exitCode; std::string output; };

<!-- Setup infrastructure -->
From src/logging/setup.h: existing LogConfig, setup(), shutdown(), currentLogFilePath()
From src/logging/setup.cpp: existing implementation (D-13 current log file path, D-03 file sink creation)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for LOG_ERROR context chain + environment snapshot</name>
  <files>tests/logging_error_context_test.cpp, tests/logging_snapshot_test.cpp</files>
  <read_first>
    - src/logging/logging.h (plan 03-01 output: ScopedErrorContext class, formatContextChain; lines 108-126: current LOG_ERROR/LOG_CRITICAL macros to understand existing format string pattern)
    - src/logging/log_tags.h (logtags::TEST_INFRA)
    - tests/logging_scoped_timer_test.cpp (registerCapturingLogger helper pattern)
    - tests/logging_error_context_test.cpp (plan 03-01 output: existing tests, ScopedContextReset helper, registerCapturingLoggerForContext helper)
    - src/core/app_context.h (lines 68-87: EncodingState struct — understand what subprocess fields will extend)
    - .planning/phases/03-forensics/03-CONTEXT.md (D-03: format; D-05: snapshot contents; D-06: captureEnvironmentSnapshot API)
    - .planning/phases/03-forensics/03-RESEARCH.md (Pattern 2: Macro-Level Context Chain Injection; Pattern 3: Lock-Free Environment Snapshot; Pitfall 3)
  </read_first>
  <action>
    **A) Add to tests/logging_error_context_test.cpp** (append after existing tests, same [logging][error_context] tags):

    Test 12 — LOG_ERROR appends context chain: Create a ScopedErrorContext with stage="encode" detail="input.mkv", then call LOG_ERROR("ffmpeg failed"). Verify the captured output contains "ffmpeg failed" followed by " [context: encode(input.mkv)]".

    Test 13 — LOG_ERROR without context has no suffix: Call LOG_ERROR("plain error") with no ScopedErrorContext active. Verify the captured output contains "plain error" but does NOT contain "[context:".

    Test 14 — LOG_ERROR with nested context produces ordered chain: Push ctx1("outer") then ctx2("inner" detail="file.mkv"). Call LOG_ERROR("fail"). Verify output contains " [context: outer > inner(file.mkv)]".

    Test 15 — LOG_CRITICAL appends context chain: Same pattern as Test 12 but using LOG_CRITICAL macro.

    Test 16 — LOG_INFO does NOT append context chain: Create a ScopedErrorContext, call LOG_INFO("info message"). Verify output does NOT contain "[context:" (only LOG_ERROR and LOG_CRITICAL append per D-03).

    IMPORTANT: Each test in this file that calls LOG_ERROR/LOG_CRITICAL must use the capturing logger pattern (registerCapturingLoggerForContext) to capture output, because these macros now involve do-while blocks with multiple SPDLOG_LOGGER_CALL invocations. The logger's pattern should be set to "%v" (message only) for clean assertions.

    **B) Create tests/logging_snapshot_test.cpp** (new file, tagged [logging][snapshot]):

    Test 1 — captureEnvironmentSnapshot() returns empty string when no context is set: Call captureEnvironmentSnapshot() before setting any forensic context. Verify it returns "" (empty string).

    Test 2 — captureEnvironmentSnapshot() returns non-empty snapshot when context is set: Set a mock AppContext pointer, call captureEnvironmentSnapshot(). Verify the returned string contains "Environment:" prefix.

    Test 3 — Snapshot format contains required fields when encoding active: Set mock context with active slots, pending count, and subprocess info. Verify the snapshot string contains "active-slots=", "pending=", and "subprocess=" or equivalent structured keys.

    Test 4 — Snapshot is safe with null encoding context: Set AppContext but leave encoding context null. Verify captureEnvironmentSnapshot() returns a string containing "Environment:" but indicating no encoding is active (e.g., "pipeline=..." or "no encoding slots").

    Test 5 — Snapshot after LOG_ERROR: Create ScopedErrorContext, mock forensic context, call LOG_ERROR("test error"). Verify the captured output contains both the error message with context chain AND a separate log line with the environment snapshot (check that the snapshot appears after the error line).

    Each snapshot test must include ScopedContextReset at the start. Use a test-specific namespace for mock context setup (create minimal mock structures or use a helper function `logging::setForensicContextForTest(...)`).
  </action>
  <acceptance_criteria>
    - All 5 new tests in logging_error_context_test.cpp compile and FAIL because LOG_ERROR/LOG_CRITICAL macros don't yet use formatContextChain.
    - All 5 tests in logging_snapshot_test.cpp FAIL because captureEnvironmentSnapshot() doesn't exist or returns incorrect values.
    - Compilation errors or runtime test failures confirm RED phase.
  </acceptance_criteria>
  <verify>
    <automated>xmake build tests && xmake run tests "[logging][error_context]" ; test $? -ne 0 (RED phase)</automated>
  </verify>
  <done>All new integration tests written and failing because macro/snapshot implementation doesn't exist yet.</done>
</task>

<task type="tdd">
  <name>Task 2: Extend infrastructure types (EncodingState + ExecResult) then implement macro modifications + snapshot (GREEN)</name>
  <files>src/logging/logging.h, src/logging/setup.h, src/logging/setup.cpp, src/core/app_context.h, src/utils/utils.h, src/utils/utils.cpp</files>
  <read_first>
    - src/logging/logging.h (full file: plan 03-01 ScopedErrorContext output; lines 68-126: current macros to modify; lines 128-136: loggerPtr forward declaration)
    - src/logging/setup.h (full file: LogConfig, setup(), shutdown(), currentLogFilePath)
    - src/logging/setup.cpp (full file: understand existing patterns for module-level statics and sink creation)
    - src/core/app_context.h (lines 68-87: EncodingState)
    - src/utils/utils.h (lines 9-12: ExecResult)
    - src/utils/utils.cpp (lines 30-80: exec2Impl function — process construction at lines 48-50)
    - .planning/phases/03-forensics/03-CONTEXT.md (D-03, D-05, D-06, D-09)
    - .planning/phases/03-forensics/03-RESEARCH.md (Pattern 2: macro injection; Pattern 3: lock-free snapshot; Pitfall 3: TLS with async)
    - tests/logging_error_context_test.cpp (Task 1 tests)
    - tests/logging_snapshot_test.cpp (Task 1 tests)
  </read_first>
  <action>
    Implement in this order to satisfy test requirements:

    **Step A — Extend EncodingState (src/core/app_context.h):**
    Add two fields to the EncodingState struct (after line 83 `std::optional<std::string> lastError;`, before `bool finished = false;`):
    - `std::optional<int> subprocessPid;` — FFmpeg process PID, set during encoding
    - `std::optional<std::string> subprocessCmdline;` — full FFmpeg command line, set before exec2 call
    These are runtime-only fields (not serialized to job state JSON), used exclusively by captureEnvironmentSnapshot().

    **Step B — Extend ExecResult (src/utils/utils.h) and capture PID (src/utils/utils.cpp):**
    In utils.h, add to ExecResult struct: `std::optional<int> pid;`
    In utils.cpp, modify exec2Impl():
    - After bp::child construction (lines 48-50), capture `process.id()` into a local variable.
    - Before returning ExecResult, set `.pid = capturedPid`.
    - The `process.id()` call is available on all platforms via Boost.Process.

    **Step C — Add forensic context setters to logging/setup.h and implement in logging/setup.cpp:**
    Declare in setup.h (namespace logging):
    - `auto setForensicAppContext(void* appCtx) -> void;` — stores pointer for snapshot access
    - `auto setForensicExecContext(void* execCtx) -> void;` — stores pointer for slot/pending reads (not yet used in this plan; called in plan 03-03 from video_batch_execution)
    - `auto captureEnvironmentSnapshot() -> std::string;` — returns formatted snapshot or ""

    Implement in setup.cpp (namespace logging, or anonymous namespace for pointer storage + logging namespace for functions):
    - Module-level pointers: `std::atomic<void*> gForensicAppCtx{nullptr}` and `std::atomic<void*> gForensicExecCtx{nullptr}`
    - `setForensicAppContext(void* appCtx)`: stores `gForensicAppCtx` with memory_order_release
    - `setForensicExecContext(void* execCtx)`: stores `gForensicExecCtx` with memory_order_release
    - `captureEnvironmentSnapshot()`: loads gForensicAppCtx with memory_order_acquire. If null, returns "".
      Cast appCtx to `appctx::AppContext*` (include "core/app_context.h" in setup.cpp if not already included).
      If execCtx is null: return minimal snapshot string `"Environment: pipeline=... (no encoding slots)"` with actual process type if available from context chain.
      If execCtx is non-null: read active slots, pending count, subprocess info. Format as single INFO line per D-05:
      `"Environment: active-slots=N/M pending=P finished=F subprocess=[pid=X cmd='...']"`.
      Build using fmt::format. Use displaytext::pathToUtf8String() for file paths (if available) or .string().
      Include necessary headers: `<atomic>`, `<format>` (already available via spdlog/fmt).

    **Step D — Modify LOG_ERROR and LOG_CRITICAL macros (src/logging/logging.h):**
    Replace the existing single SPDLOG_LOGGER_CALL with a `do { ... } while(0)` block:

    For LOG_ERROR (replace lines 108-116):
    ```
    #define LOG_ERROR(...)                                                  \
      do {                                                                   \
        auto const __encro_ctx_chain = logging::detail::formatContextChain();  \
        SPDLOG_LOGGER_CALL(                                                   \
          loggerPtr(),                                                        \
          spdlog::level::err,                                                 \
          "[{}:{}] {}{}",                                                     \
          logging::detail::shortFile(__FILE__),                               \
          __LINE__,                                                           \
          fmt::format(__VA_ARGS__),                                           \
          __encro_ctx_chain                                                   \
        );                                                                    \
        auto const __encro_snapshot = logging::captureEnvironmentSnapshot();   \
        if (!__encro_snapshot.empty()) {                                       \
          SPDLOG_LOGGER_CALL(                                                  \
            loggerPtr(),                                                       \
            spdlog::level::info,                                               \
            "[{}:{}] {}",                                                      \
            logging::detail::shortFile(__FILE__),                              \
            __LINE__,                                                          \
            __encro_snapshot                                                   \
          );                                                                   \
        }                                                                      \
      } while(0)
    ```

    Apply the same `do { ... } while(0)` pattern to LOG_CRITICAL (replace lines 118-126), using `spdlog::level::critical` for the error message and `spdlog::level::info` for the snapshot.

    Do NOT modify LOG_TRACE, LOG_DEBUG, LOG_INFO, LOG_WARN — these do not append context or trigger snapshot per D-03/D-05.

    **Step E — Add forward declarations (src/logging/logging.h):**
    Before ScopedTimer's forward declaration block (around line 128), add:
    ```
    namespace logging {
    auto captureEnvironmentSnapshot() -> std::string;
    }
    ```
    This allows the macros to compile before the captureEnvironmentSnapshot definition (which is in setup.cpp).

    **Step F — Wire setForensicAppContext in logging::setup():**
    In src/logging/setup.cpp, the existing `logging::setup(LogConfig const& config)` function does not currently have access to AppContext. Instead of modifying setup()'s signature, add a note that the caller should invoke `logging::setForensicAppContext(appCtx)` separately. This will be done in Plan 03-03 from pipeline orchestration code.
  </action>
  <acceptance_criteria>
    - `xmake build tests` compiles successfully (logging_error_context_test.cpp and logging_snapshot_test.cpp compile).
    - `xmake run tests "[logging][error_context]"` — all 16 tests pass (11 from Plan 03-01 + 5 new from Task 1).
    - `xmake run tests "[logging][snapshot]"` — all 5 snapshot tests pass.
    - LOG_ERROR with active ScopedErrorContext: output contains both the error message and context chain suffix.
    - LOG_ERROR without ScopedErrorContext: output is identical to before (no regression) — plain "[file:line] message" format.
    - LOG_INFO does NOT append context chain (verification via Test 16).
    - LOG_CRITICAL with active ScopedErrorContext: output contains context chain suffix.
    - captureEnvironmentSnapshot() returns "" when no AppContext pointer set.
    - ExecResult.pid is populated after exec2() calls (verifiable in tests).
  </acceptance_criteria>
  <verify>
    <automated>xmake build tests && xmake run tests "[logging][error_context]" && xmake run tests "[logging][snapshot]"</automated>
  </verify>
  <done>All 21 tests pass (16 error_context + 5 snapshot). RED→GREEN cycle complete for Plan 03-02.</done>
</task>

<task type="tdd">
  <name>Task 3: Refactor — verify noexcept, formatting, and no regressions (REFACTOR)</name>
  <files>src/logging/logging.h, src/logging/setup.cpp, src/logging/setup.h, src/core/app_context.h, src/utils/utils.h, src/utils/utils.cpp, tests/logging_error_context_test.cpp, tests/logging_snapshot_test.cpp</files>
  <read_first>
    - src/logging/logging.h (verify do-while macro bodies are correct)
    - src/logging/setup.cpp (verify captureEnvironmentSnapshot implementation patterns)
    - tests/logging_error_context_test.cpp (verify test coverage)
    - tests/logging_snapshot_test.cpp (verify test coverage)
  </read_first>
  <action>
    Under test protection, perform these refactoring checks:

    1. Verify LOG_ERROR/LOG_CRITICAL do-while blocks use `while(0)` (not `while(false)` — C++ convention).
    2. Verify macro temporary variable names use `__encro_` prefix to avoid collision with user code.
    3. Run `xmake format` to apply clang-format to all modified files.
    4. Verify all tests pass after formatting: `xmake run tests "[logging][error_context]" && xmake run tests "[logging][snapshot]"`.
    5. Verify no other test suites regress: `xmake run tests` (full suite).
    6. Check that `logging::detail::shortFile` and `__LINE__` still produce correct source locations in error messages (spot-check captured output in tests).
    7. Verify that `#include <atomic>` is present in setup.cpp (needed for std::atomic<void*>).
    8. Verify captureEnvironmentSnapshot() does not acquire any locks (it only reads atomics with memory_order_acquire). No mutex included in the snapshot code path.
    9. Verify ExecResult.pid field uses `std::optional<int>` (not raw int with sentinel value).
  </action>
  <acceptance_criteria>
    - `xmake run tests "[logging][error_context]"` — all 16 tests pass.
    - `xmake run tests "[logging][snapshot]"` — all 5 tests pass.
    - `xmake run tests` — full test suite green (no regressions in other modules).
    - `xmake format -k check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>xmake run tests "[logging][error_context]" && xmake run tests "[logging][snapshot]"</automated>
    <automated>xmake format -k check</automated>
  </verify>
  <done>All tests pass, code is formatted, macros follow established pattern, no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macro call site → async logger | Context chain formatted at call site (on calling thread) before SPDLOG_LOGGER_CALL queues to async worker — Pitfall #3 prevention |
| captureEnvironmentSnapshot() → stored pointers | Pointers set via setForensicAppContext/setForensicExecContext; snapshot reads via memory_order_acquire — no data race |
| LOG_ERROR macro → captureEnvironmentSnapshot() | snapshot called inside do-while on calling thread, before async queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | Environment snapshot file paths | accept | File paths already visible in progress bars and log output; snapshot logged at INFO level to file sink only |
| T-03-05 | Denial of Service | Macro expansion with deep context stack | mitigate | TLS stack bounded to 16 frames (Plan 03-01 D-02); formatContextChain() is O(16) string building |
| T-03-06 | Tampering | Forensic context pointer manipulation | accept | Pointers set once at encoding start/end; no attacker-controlled mutation path in local CLI |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package dependencies in this phase |
</threat_model>

<verification>
After all tasks complete:
- `xmake build tests && xmake run tests "[logging][error_context]" && xmake run tests "[logging][snapshot]"` — all 21 tests green
- `xmake format -k check` exits 0
- `xmake run tests` — full suite green
</verification>

<success_criteria>
- [ ] LOG_ERROR/LOG_CRITICAL macros use do-while(0) block with context chain + snapshot
- [ ] captureEnvironmentSnapshot() implemented in setup.cpp with lock-free atomic pointer reads
- [ ] EncodingState has subprocessPid and subprocessCmdline fields
- [ ] ExecResult has pid field captured from bp::child::id() in exec2Impl
- [ ] 21 TDD tests pass (16 error_context + 5 snapshot)
- [ ] No regression in existing test suites
</success_criteria>

<output>
Create .planning/phases/03-forensics/03-02-SUMMARY.md when done
</output>
