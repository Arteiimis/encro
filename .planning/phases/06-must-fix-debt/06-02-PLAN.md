---
phase: 6
plan: 6-2
wave: 1
depends_on: null
files_modified:
  - tests/pack_service_tests.cpp
requirements_addressed: [DEBT-02]
autonomous: false
---

# Plan 6-2: Remove Redundant Assertion in pack_service_tests.cpp

## Objective

Remove the single redundant `CHECK(result.compact == true)` assertion at line 161 of `tests/pack_service_tests.cpp`. The compact preservation behavior (both `true` and `false`) is exhaustively tested by the first test case at lines 98-130. The second test (lines 132-162) tests v1.1 named helper delegation — its compact assertion at line 161 is vestigial.

DO NOT delete the entire test case. Keep both Test A and Test B intact. Only remove the single redundant CHECK.

## must_haves

- Line 161 `CHECK(result.compact == true)` removed from `pack_service_tests.cpp`
- Both test cases (Test A: "preserves compact from source plan", Test B: "delegates to named helpers") remain intact
- All other assertions in Test B (lines 157, 159) remain unchanged
- Test suite reports 909 assertions (was 910), all passing
- No other test files modified

## Tasks

<task id="01" type="execute">
<description>Remove redundant CHECK(result.compact == true) at line 161</description>

<read_first>
- tests/pack_service_tests.cpp (lines 98-162 — both test cases, identify line 161)
</read_first>

<action>
In `tests/pack_service_tests.cpp`, locate the second test case section titled "selectPackPlanIndexes delegates to named helpers instead of lambda-wrapping-lambda" (around line 132-162). Find line 161:
```cpp
CHECK(result.compact == true);  // ← REMOVE THIS LINE ONLY
```

Delete this single line. Preserve the surrounding assertions:
```cpp
// Keep these:
CHECK(result.zipNameForIndex(0) == "arch1.zip");
CHECK(result.progressLabelForIndex(0) == "Zipping archive 1");
// Remove line 161: CHECK(result.compact == true);
```

Do NOT modify the first test case (lines 98-130) or any other assertions in the file.
</action>

<acceptance_criteria>
- `tests/pack_service_tests.cpp` no longer contains `CHECK(result.compact == true)` at line 161 area
- The string `CHECK(result.compact` must NOT appear anywhere in the second test case (after the `// Test B` or test case section starting around line 132)
- The first test case (around line 98-130) still contains `CHECK(result.compact == true)` and `CHECK(result.compact == false)` — these must remain
- Verify with: `rg "CHECK\(result\.compact" tests/pack_service_tests.cpp` returns exactly 2 matches (both in first test case)
</acceptance_criteria>
</task>

## Verification

1. Run full test suite: `xmake run tests` — all 909 assertions pass (was 910, 1 removed)
2. Confirm first test case still passes both compact=true and compact=false
3. Confirm second test case still validates named helper delegation (zipNameForIndex, progressLabelForIndex)
