---
phase: 01-logging-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/logging/log_tags.h
  - src/logging/logging.h
  - src/logging/setup.h
  - src/logging/setup.cpp
  - tests/logging_infra_test.cpp
autonomous: true
requirements: [INF-01, INF-03, INF-04, INF-05, OBS-01, OBS-02, OBS-04]

must_haves:
  truths:
    - "log_tags.h 定义了 24 个 constexpr 标签常量，全部使用 dot-notation 层级命名（如 'video.encode', 'pack.zip'）"
    - "logging.h 提供 LOG_TRACE/DEBUG/INFO/WARN/ERROR/CRITICAL 宏，展开时自动将 file:line 注入消息体"
    - "DEFINE_LOGGER(tag) 宏展开为 static auto* const gLoggerPtr = spdlog::get(tag) — 每个 .cpp 一行注册"
    - "setup.h 声明 LogConfig 结构体和 logging::setup(config) / logging::shutdown() 函数"
    - "setup.cpp 创建共享 sink、为全部 24 个标签注册 async_logger、设置 default_logger"
    - "日志 pattern 为 '[%Y-%m-%dT%H:%M:%S.%e%z] [%^%l%$] [%n] %v' — 不含 %s:%#（源位置在消息体中，per D-03）"
  artifacts:
    - path: "src/logging/log_tags.h"
      provides: "24 个 constexpr 模块标签常量，dot-notation 层级命名"
      min_lines: 50
    - path: "src/logging/logging.h"
      provides: "DEFINE_LOGGER 宏 + LOG_* 6 级宏 + shortFile 辅助函数"
      min_lines: 80
    - path: "src/logging/setup.h"
      provides: "LogConfig struct, setup() 声明, shutdown() 声明"
      min_lines: 25
    - path: "src/logging/setup.cpp"
      provides: "共享 sink 创建、24 个 named async_logger 注册、default_logger 设置"
      min_lines: 100
    - path: "tests/logging_infra_test.cpp"
      provides: "TDD 测试：宏展开行为、标签常量格式、setup 注册验证"
      min_lines: 80
  key_links:
    - from: "logging.h LOG_INFO 宏"
      to: "spdlog::async_logger (via gLoggerPtr → spdlog::get(tag))"
      via: "SPDLOG_LOGGER_CALL"
      pattern: "SPDLOG_LOGGER_CALL\\(gLoggerPtr,"
    - from: "setup.cpp allModuleTags()"
      to: "log_tags.h 标签常量"
      via: "logtags:: 命名空间引用"
      pattern: "logtags::"
    - from: "DEFINE_LOGGER(logtags::XXX)"
      to: "spdlog::get(tag)"
      via: "static 缓存指针"
      pattern: "static auto\\* const gLoggerPtr"
---

<objective>
创建日志基础设施的四个新文件和一个 TDD 测试文件。这是 Phase 1 的地基层：log_tags.h（标签常量）、logging.h（宏 + DEFINE_LOGGER）、setup.h（LogConfig + API 声明）、setup.cpp（sink 创建 + logger 注册）。所有后续 plan 依赖这些文件。

Purpose: 建立统一的日志宏层和模块标签体系，让每条日志自动携带 source location 和 module tag。
Output: 5 个文件（4 个 src/logging/ + 1 个 tests/），通过编译和测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# 项目约定
@.planning/codebase/CONVENTIONS.md
# 架构
@.planning/codebase/ARCHITECTURE.md
# Phase 决策
@.planning/phases/01-logging-foundation/01-CONTEXT.md
# Phase 研究（宏设计、DEFINE_LOGGER 模式、setup.cpp 架构、标签映射）
@.planning/phases/01-logging-foundation/01-RESEARCH.md
# 现有日志初始化代码（参考 sink 创建、线程池、pattern 模式）
@src/app/prelude.cpp
# 构建系统（需要知道现有 target 结构和 add_files glob 模式）
@xmake.lua

<interfaces>
<!-- 关键类型和约定，executor 直接使用，无需代码库探索 -->

从 CONVENTIONS.md（项目强制规范）:
- 所有文件使用 `#pragma once`（不用 #ifndef guard）
- east const: `auto* const ptr`（const 在右边）
- trailing return type: `auto fn(params) -> ReturnType`（所有函数）
- 匿名 namespace 用于文件内部实现细节
- 命名空间: 小写无分隔符（如 `logtags`, `logging`）
- 常量: `k` + PascalCase（如 `kLogPattern`）
- Include 路径: 相对于 `src/`，使用正斜杠
- 头文件: `.h`，实现文件: `.cpp`

从 RESEARCH.md §1.2（宏设计）:
- D-03 源位置注入消息体: 格式 "[file.cpp:128] message"
- `logging::detail::shortFile(__FILE__)` 提取短文件名
- `fmt::format(__VA_ARGS__)` 在 call site 预格式化（避免 async TLS 问题）
- `SPDLOG_LOGGER_CALL(gLoggerPtr, level, "[{}:{}] {}", shortFile, __LINE__, fmt::format(...))`

从 RESEARCH.md §1.3（日志 Pattern）:
- Pattern: `[%Y-%m-%dT%H:%M:%S.%e%z] [%^%l%$] [%n] %v`
- 不含 `%s` 或 `%#` — 源位置在消息体中（D-03 优先于 D-10）

从 RESEARCH.md §4（setup.cpp 架构）:
- LogConfig: { verboseEnabled, verboseEchoEnabled, colorsEnabled, customLogDir }
- setup() 返回 `std::optional<fs::path>`（日志文件路径）
- 24 个标签全部在 allModuleTags() 中注册
- shared sink 模式：所有 named logger + default_logger 共享同一个 file sink 和 console sink

从 RESEARCH.md §7（log_tags.h 设计）:
- 24 个标签常量，命名空间 `logtags`
- 全小写，dot-notation 层级
- 命名模式: `inline constexpr auto VIDEO_ENCODE = "video.encode";`
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing tests for macro behavior, tag constants, and setup registration</name>
  <files>tests/logging_infra_test.cpp</files>
  <read_first>
在编写测试前务必先读取以下文件以理解：
- @.planning/phases/01-logging-foundation/01-CONTEXT.md — D-01 到 D-14 全部锁定决策，特别是 D-03（源位置注入消息体）、D-05（每文件一个 DEFINE_LOGGER）、D-07（标签常量列表）
- @.planning/phases/01-logging-foundation/01-RESEARCH.md — 第 1 节（宏设计）、第 2 节（DEFINE_LOGGER 模式）、第 4 节（setup.cpp 架构）、第 7 节（log_tags.h 设计）
- @.planning/codebase/CONVENTIONS.md — 命名规范、代码风格、east const、trailing return type
- @src/app/prelude.cpp — 现有 spdlog 初始化模式（线程池、sink 创建、pattern 设置），作为 setup.cpp 实现的参考
- @xmake.lua — 确认 tests target 的 add_files glob 和 includedirs 配置
  </read_first>
  <behavior>
测试必须覆盖以下行为（测试在 log_tags.h, logging.h, setup.h, setup.cpp 全部创建后才通过）：

Test 1 — 标签常量格式验证:
  - 检查 `logtags::APP_ENTRY` 等于 `"app.entry"`（字符串字面量）
  - 检查 `logtags::VIDEO_ENCODE` 等于 `"video.encode"`
  - 检查 `logtags::PACK_ZIP` 等于 `"pack.zip"`
  - 检查至少 5 个代表性标签常量使用 dot-notation 格式（包含一个点号，全小写）

Test 2 — DEFINE_LOGGER + LOG_INFO 宏展开:
  - 在测试文件中调用 `DEFINE_LOGGER(logtags::TEST_INFRA)` 创建一个测试专用 logger
  - 在测试文件所在作用域（全局/命名空间）中，DEFINE_LOGGER 展开后 `gLoggerPtr` 可访问且非空
  - 使用 `LOG_INFO("test message {}", 42)` 不触发编译错误
  - 验证输出消息字符串包含 `"test message 42"`（即 fmt::format 正确求值）

Test 3 — 源位置注入格式:
  - 调用 `LOG_INFO("hello")` 后，输出的消息字符串包含 `"[logging_infra_test.cpp:"` 前缀（即源文件:行号格式）
  - 模式匹配: 消息以 `[` 开头，后跟文件名、`:`、行号、`]`、空格、实际消息

Test 4 — logging::setup() 注册验证:
  - 构造 `LogConfig{.verboseEnabled = true, .verboseEchoEnabled = false, .colorsEnabled = true}`
  - 调用 `logging::setup(config)` 返回 `std::optional<fs::path>`（日志文件路径存在）
  - 验证 `spdlog::get("video.encode")` 返回非空 shared_ptr
  - 验证 `spdlog::get("app.entry")` 返回非空 shared_ptr
  - 验证 `spdlog::get("pack.zip")` 返回非空 shared_ptr
  - 验证所有 24 个标签均可通过 `spdlog::get(tag)` 检索到（非空）

Test 5 — logging::shutdown() 清理:
  - 调用 `logging::shutdown()` 后 spdlog 正常清理
  - 测试结束时临时日志文件被删除或可被清理
</behavior>
  <action>
创建 `tests/logging_infra_test.cpp`，编写上述 5 组测试用例。

测试文件结构：
- `#include <catch2/catch_all.hpp>`（Catch2 v3）
- `#include "logging/log_tags.h"` — 验证标签常量
- `#include "logging/logging.h"` — 验证宏展开
- `#include "logging/setup.h"` — 验证 setup/shutdown
- 使用 Catch2 `TEST_CASE` 宏分组
- 在测试文件中调用 `DEFINE_LOGGER(logtags::TEST_INFRA)` 以测试宏的完整链路
- 注意：log_tags.h 中额外定义一个 `inline constexpr auto TEST_INFRA = "test.infra";` 标签供测试使用

关键实现细节：
- 捕获 spdlog 输出的测试方式：创建一个临时文件 sink，通过 `spdlog::get("test.infra")` 获取 logger，检查写入文件的内容
- 测试结束后调用 `logging::shutdown()` 清理，删除临时文件和日志目录
- 使用 `fs::remove_all()` 清理临时目录
- 源位置验证：由于 `__LINE__` 是编译期常量，测试消息中的行号需要匹配实际的 `LOG_INFO` 调用行号（使用 `__LINE__` 偏移量或宽松匹配 `logging_infra_test.cpp:` 前缀）

此阶段预期编译失败 — log_tags.h, logging.h, setup.h, setup.cpp 尚不存在。测试文件存在但不可编译。
  </action>
  <verify>
    <automated>MISSING — 此 task 为 RED 阶段，预期编译失败。编写测试后运行 `xmake build tests` 确认编译失败（缺少被测头文件），即验证测试编写正确。</automated>
  </verify>
  <acceptance_criteria>
- tests/logging_infra_test.cpp 存在，包含 5 个 TEST_CASE
- 每个测试用例有明确的 REQUIRE/CHECK 断言
- 编译失败（xmake build tests），错误信息指示缺少 "logging/log_tags.h" 或 "logging/logging.h"
- 测试文件遵循 CONVENTIONS.md（east const, trailing return, #pragma once 不适用, 匿名 namespace 用于 helpers）
  </acceptance_criteria>
  <done>Failing test file committed. xmake build tests fails with missing header errors — RED phase complete.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement log_tags.h + logging.h — make macro tests pass</name>
  <files>src/logging/log_tags.h, src/logging/logging.h</files>
  <read_first>
在实现前务必先读取：
- @.planning/phases/01-logging-foundation/01-RESEARCH.md — 完整阅读第 1 节（macro design，含 shortFile 实现、LOG_* 宏展开）、第 2 节（DEFINE_LOGGER 模式）、第 7 节（log_tags.h 完整标签列表）
- @.planning/phases/01-logging-foundation/01-CONTEXT.md — D-01（自定义宏层，内部 SPDLOG_LOGGER_CALL）、D-02（LOG_INFO 命名不加前缀）、D-03（源位置注入消息体，不用 pattern flags）、D-04（短文件名）、D-05（每文件一个 DEFINE_LOGGER）、D-06（static 缓存裸指针）
- @.planning/codebase/CONVENTIONS.md — #pragma once, east const, trailing return, namespace 小写无分隔符, 常量 kPascalCase
- @src/app/prelude.cpp — 当前 spdlog pattern 格式和使用方式，确认现有 kLogPattern 变量名模式
  </read_first>
  <action>
实现两个头文件：

**文件 1: src/logging/log_tags.h**

定义命名空间 `logtags`，包含 24 个 `inline constexpr auto` 标签常量，使用 dot-notation 层级命名。

完整标签列表（24 个，from RESEARCH §7 + CONTEXT.md D-07）:
- APP_ENTRY = "app.entry"
- APP_PRELUDE = "app.prelude"
- APP_PIPELINE = "app.pipeline"
- CMD_CONFIG = "cmd.config"
- VIDEO_ENCODE = "video.encode"
- VIDEO_PROBE = "video.probe"
- VIDEO_INFO = "video.info"
- VIDEO_OUTPUT = "video.output"
- VIDEO_BATCH = "video.batch"
- VIDEO_PROGRESS = "video.progress"
- VIDEO_STATE = "video.state"
- VIDEO_PROCESS = "video.process"
- PICTURE_PROCESS = "picture.process"
- PICTURE_COMPRESS = "picture.compress"
- PACK_ZIP = "pack.zip"
- PACK_SERVICE = "pack.service"
- CORE_SCAN = "core.scan"
- CORE_JOB = "core.job"
- CORE_TASK = "core.task"
- CORE_PARALLEL = "core.parallel"
- INFRA_TOOLCHAIN = "infra.toolchain"
- INFRA_CRASH = "infra.crash"
- INFRA_SIGNAL = "infra.signal"
- UTILS_SUBPROCESS = "utils.subprocess"
- TEST_INFRA = "test.infra"（额外加一个，供测试使用）

用注释分组标记: `// ── app ──`, `// ── cmd ──`, `// ── video ──`, `// ── picture ──`, `// ── pack ──`, `// ── core ──`, `// ── infra ──`, `// ── utils ──`, `// ── test ──`

**文件 2: src/logging/logging.h**

包含以下内容：

a) 包含保护: `#pragma once`

b) 必要的 include:
- `#include "logging/log_tags.h"` — 标签常量
- `#include <spdlog/spdlog.h>` — SPDLOG_LOGGER_CALL, spdlog::level
- `#include <spdlog/async.h>` — async_logger
- `#include <fmt/format.h>` — fmt::format

c) `logging::detail::shortFile()` 辅助函数:
- 签名: `[[nodiscard]] inline auto shortFile(char const* path) noexcept -> char const*`
- 实现: 扫描 path 找到最后一个 `/` 或 `\`，返回其后的指针（仅文件名）
- 逻辑（from RESEARCH §1.2）:
  ```
  auto const* last = path;
  for (auto const* p = path; *p != '\0'; ++p) {
      if (*p == '/' || *p == '\\') { last = p + 1; }
  }
  return last;
  ```

d) DEFINE_LOGGER 宏（D-05, D-06）:
- `#define DEFINE_LOGGER(tag) static auto* const gLoggerPtr = spdlog::get(tag)`
- 使用裸指针 `auto* const`（非 shared_ptr），logger 生命周期由 spdlog registry 保证

e) 6 个日志宏（D-01, D-02, D-03）:
- `LOG_TRACE(...)` → `SPDLOG_LOGGER_CALL(gLoggerPtr, spdlog::level::trace, "[{}:{}] {}", logging::detail::shortFile(__FILE__), __LINE__, fmt::format(__VA_ARGS__))`
- `LOG_DEBUG(...)` → 同上，level::debug
- `LOG_INFO(...)` → 同上，level::info
- `LOG_WARN(...)` → 同上，level::warn
- `LOG_ERROR(...)` → 同上，level::err
- `LOG_CRITICAL(...)` → 同上，level::critical

每个宏的消息格式: `"[{}:{}] {}"` — 四个参数: shortFile, __LINE__, fmt::format result
- `shortFile(__FILE__)` 提取短文件名
- `__LINE__` 行号
- `fmt::format(__VA_ARGS__)` 预格式化的消息（在 call site 求值，避免 async TLS 问题 — Pitfall #3）

关键设计决策（from RESEARCH §1.2）:
- `fmt::format(__VA_ARGS__)` 在 call site 调用（caller thread），不在 async worker thread
- 源位置字符串完全复制到消息体中（无悬垂指针风险 — Pitfall #2）
- `SPDLOG_LOGGER_CALL` 内部处理的 `source_loc` 结构体仍被构造但 pattern 不使用其字段
- 不需要 `do { ... } while(0)` 包装 — `SPDLOG_LOGGER_CALL` 自身是完整表达式

f) 在文件末尾注释说明宏展开后的 logger 名称来源:
- `%n` pattern flag → named logger 名称 → 模块标签
- `%v` pattern flag → 消息体（已含 `[file:line] msg`）
  </action>
  <verify>
    <automated>xmake build tests</automated>
  </verify>
  <acceptance_criteria>
- src/logging/log_tags.h 存在，包含 25 个 inline constexpr auto 标签常量（24 个 + TEST_INFRA）
- 所有标签常量使用 dot-notation 格式（包含点号，全小写）
- 标签按模块分组，每组有 `// ── name ──` 注释
- src/logging/logging.h 存在，使用 #pragma once
- DEFINE_LOGGER 宏定义为 `static auto* const gLoggerPtr = spdlog::get(tag)`
- 6 个 LOG_* 宏全部定义，消息格式为 `"[{}:{}] {}"`
- shortFile() 函数正确处理 Windows (`\`) 和 POSIX (`/`) 路径分隔符
- 编译成功: `xmake build tests` 不报错（log_tags.h 和 logging.h 可被 test 文件 #include）
- tests/logging_infra_test.cpp 中的 Test 1, Test 2, Test 3 通过（标签常量、DEFINE_LOGGER、宏展开、源位置格式）
  </acceptance_criteria>
  <done>log_tags.h + logging.h 编译通过。TDD Test 1-3 通过（标签常量、宏、源位置）。GREEN phase complete.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3 (GREEN): Implement setup.h + setup.cpp — make setup registration tests pass</name>
  <files>src/logging/setup.h, src/logging/setup.cpp</files>
  <read_first>
在实现前务必先读取：
- @.planning/phases/01-logging-foundation/01-RESEARCH.md — 完整阅读第 4 节（setup.cpp 架构：LogConfig, setup(), shutdown(), allModuleTags(), resolveCommonLogDir, kLogPattern, shared sink 模式, default_logger 设置）
- @.planning/phases/01-logging-foundation/01-CONTEXT.md — D-11（level 颜色标记 %^%l%$）、D-12（所有 sink/logger 配置移至 setup.cpp）、D-13（公共头文件导出）
- @src/app/prelude.cpp — 行 24-128: resolveCommonLogDir() 实现（Windows LOCALAPPDATA）、setupLogging() 中的线程池初始化（init_thread_pool(8192, 1)）、sink 创建模式、logger 构造参数、flush_on 设置
- @.planning/codebase/CONVENTIONS.md — 匿名 namespace、east const、trailing return、include 顺序
  </read_first>
  <action>
实现 setup.h（声明）和 setup.cpp（实现）。

**文件 1: src/logging/setup.h**

声明:
- `struct LogConfig` — 4 个字段: `bool verboseEnabled{false}; bool verboseEchoEnabled{false}; bool colorsEnabled{true}; std::optional<std::filesystem::path> customLogDir;`
- `auto setup(LogConfig const& config) -> std::optional<std::filesystem::path>;` — 初始化日志系统，返回日志文件路径
- `auto shutdown() -> void;` — 清理: flush + 关闭所有 logger

**文件 2: src/logging/setup.cpp**

实现内容（严格遵循 RESEARCH §4 架构）:

a) Includes:
- `#include "logging/setup.h"`
- `#include "logging/log_tags.h"`
- spdlog headers: `<spdlog/async.h>`, `<spdlog/logger.h>`, `<spdlog/sinks/basic_file_sink.h>`, `<spdlog/sinks/stdout_color_sinks.h>`, `<spdlog/sinks/stdout_sinks.h>`, `<spdlog/spdlog.h>`
- stdlib: `<memory>`, `<mutex>`, `<vector>`, `<system_error>`, `<cstdlib>`
- 文件系统: `namespace fs = std::filesystem;`（在匿名 namespace 外部或内部，与 prelude.cpp 现有一致即可）

b) 匿名 namespace 内的私有组件:

- `resolveCommonLogDir()` — 从 prelude.cpp 行 39-57 **逐字迁移**。实现逻辑：
  - Windows: 读 `LOCALAPPDATA` 环境变量 → `%LOCALAPPDATA%/encro/logs`；fallback 到 `APPDATA`；fallback 到 `temp_directory_path()/encro/logs`
  - POSIX: `$HOME/.local/state/encro/logs`；fallback 到 `temp_directory_path()/encro/logs`
  - 使用 `_dupenv_s`（Windows）或 `std::getenv`（POSIX）
  - 返回 `fs::path`

- `kLogPattern` — 常量: `constexpr auto kLogPattern = "[%Y-%m-%dT%H:%M:%S.%e%z] [%^%l%$] [%n] %v";`
  - D-03 兼容: 不含 `%s` 或 `%#` — 源位置在消息体中
  - D-11 兼容: `%^%l%$` 提供 level 颜色标记

- `allModuleTags()` — 返回 `std::vector<char const*>`，包含全部 24 个标签:
  ```
  logtags::APP_ENTRY, logtags::APP_PRELUDE, logtags::APP_PIPELINE,
  logtags::CMD_CONFIG,
  logtags::VIDEO_ENCODE, logtags::VIDEO_PROBE, logtags::VIDEO_INFO,
  logtags::VIDEO_OUTPUT, logtags::VIDEO_BATCH, logtags::VIDEO_PROGRESS,
  logtags::VIDEO_STATE, logtags::VIDEO_PROCESS,
  logtags::PICTURE_PROCESS, logtags::PICTURE_COMPRESS,
  logtags::PACK_ZIP, logtags::PACK_SERVICE,
  logtags::CORE_SCAN, logtags::CORE_JOB, logtags::CORE_TASK, logtags::CORE_PARALLEL,
  logtags::INFRA_TOOLCHAIN, logtags::INFRA_CRASH, logtags::INFRA_SIGNAL,
  logtags::UTILS_SUBPROCESS
  ```

c) `logging::setup(config)` 实现（from RESEARCH §4.2）:

1. 如果 `!config.verboseEnabled`: `spdlog::set_level(spdlog::level::off)` + return `std::nullopt`
2. `resolveCommonLogDir()` → `fs::create_directories(logDir, ec)`; 如果失败: fallback 到 `fs::temp_directory_path() / "encro" / "logs"`
3. 日志文件路径: `logDir / "encro.verbose.log"`（Phase 1 保持现有文件名；Phase 2 改为时间戳命名）
4. 创建 file sink: `std::make_shared<spdlog::sinks::basic_file_sink_mt>(logFilePath.string(), true)`
5. 可选 console sink: 如果 `config.verboseEchoEnabled`: 检查 `config.colorsEnabled` 决定使用 `stdout_color_sink_mt` 还是 `stdout_sink_mt`
6. 线程池: 使用 `std::once_flag` + `std::call_once` 调用 `spdlog::init_thread_pool(8192, 1)`（from prelude.cpp 行 105-106）
7. 遍历 `allModuleTags()`，为每个标签创建 `spdlog::async_logger`:
   - 构造函数参数: tag name, sinks.begin(), sinks.end(), spdlog::thread_pool(), spdlog::async_overflow_policy::block
   - 设置: `set_pattern(kLogPattern)`, `set_level(spdlog::level::debug)`, `flush_on(spdlog::level::err)`
   - `spdlog::register_logger(std::move(logger))`
8. 创建 default_logger（名称为 "encro"）:
   - 与 named loggers 共享同一组 sink
   - `spdlog::set_default_logger(std::move(defaultLogger))`
   - `spdlog::set_level(spdlog::level::debug)`
9. 返回 `logFilePath`

d) `logging::shutdown()`:
- `spdlog::shutdown();`

**注意** — 细节：
- setup.cpp 使用 spdlog 直接 API（`spdlog::sinks::*`, `spdlog::async_logger` 等），**不使用** LOG_* 宏。因为 setup.cpp 是基础设施代码，不是业务代码（INF-05: 日志配置与业务逻辑严格分离）。
- prelude.cpp 中 setupLogging 的 `spdlog::warn("Cannot create log directory '{}': {}'...)` 调用 — 迁移到 setup.cpp 时仍使用 `spdlog::warn(...)`（因为在 default_logger 创建之前，named loggers 也未注册，无法使用 LOG_WARN）。
- `spdlog::debug("Verbose logging enabled.")` — 从 prelude.cpp 行 122 迁移到 setup.cpp，放在 default_logger 创建之后，改用 `spdlog::debug(...)` 或 LOG_DEBUG。由于 setup.cpp 不使用 DEFINE_LOGGER，这里用 `spdlog::default_logger_raw()->debug(...)` 或直接 `spdlog::debug(...)`（后者使用 default logger）。
- 迁移后，`spdlog::warn(...)` 和 `spdlog::debug(...)` 这两个调用在 setup.cpp 中是唯一保留的 spdlog 直接调用（除 crash_runtime.cpp 外）。
  </action>
  <verify>
    <automated>xmake build tests && xmake run tests</automated>
  </verify>
  <acceptance_criteria>
- src/logging/setup.h 存在，声明 LogConfig struct、setup()、shutdown()
- src/logging/setup.cpp 存在，包含 resolveCommonLogDir、kLogPattern、allModuleTags、setup()、shutdown()
- `xmake build tests` 编译成功（tests target 通过 add_files("src/**.cpp|main.cpp") 自动包含 setup.cpp）
- 运行 tests: Test 4（logging::setup 注册验证）通过 — 所有 24 个标签均可通过 spdlog::get() 检索
- 运行 tests: Test 5（logging::shutdown 清理）通过
- kLogPattern 确认为 `"[%Y-%m-%dT%H:%M:%S.%e%z] [%^%l%$] [%n] %v"` — 不含 %s 或 %#
  </acceptance_criteria>
  <done>setup.h + setup.cpp 编译通过。全部 5 个 TDD 测试通过。新基础设施可工作。GREEN phase complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 日志消息体 → spdlog pattern formatter | 消息体通过 `%v` 插入 pattern。用户提供的 fmt 参数可能包含控制字符。 |
| 环境变量 → resolveCommonLogDir() | LOCALAPPDATA/APPDATA/HOME 环境变量值用于构建日志目录路径。 |
| 外部 fmt 参数 → 宏展开 | LOG_INFO("msg: {}", userInput) — userInput 来自文件路径、FFmpeg 输出等外部来源。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | LOG_* 宏消息体 | mitigate | fmt::format("{}", msg) 不展开换行符 — 换行符作为字面 `\n` 出现在格式化字符串中。无法通过消息注入伪造日志条目。 |
| T-01-02 | Information Disclosure | resolveCommonLogDir() 路径 | accept | 路径写入日志文件（%LOCALAPPDATA% 用户级目录），不暴露给其他用户。Phase 3 将添加路径消毒。 |
| T-01-03 | Information Disclosure | __FILE__ 宏展开 | accept | 编译期路径内嵌在二进制中。Release 构建已通过 clang-cl 默认行为只含短文件名。 |
| T-01-SC | Tampering | spdlog/fmt from xmake-repo | mitigate | 包来自 xmake-repo。spdlog 和 fmt 已在项目中使用（非新依赖）。[ASSUMED] 包信任级别 — 已通过现有项目的编译和运行验证。 |
</threat_model>

<verification>
编译验证:
  `xmake build -m debug` — 全部 target 编译通过（encro + tests）
  `xmake run tests` — 所有 5 个 logging_infra 测试用例通过

TDD 红绿循环:
  - Task 1 (RED): 测试存在但编译失败
  - Task 2 (GREEN): log_tags.h + logging.h 实现后 Test 1-3 通过
  - Task 3 (GREEN): setup.h + setup.cpp 实现后 Test 4-5 通过
</verification>

<success_criteria>
- [ ] src/logging/log_tags.h: 25 个 constexpr 标签常量（24 + TEST_INFRA），全部 dot-notation 格式
- [ ] src/logging/logging.h: 6 个 LOG_* 宏 + DEFINE_LOGGER + shortFile helper
- [ ] src/logging/setup.h: LogConfig struct + setup() + shutdown() 声明
- [ ] src/logging/setup.cpp: resolveCommonLogDir + allModuleTags(24) + sink 创建 + logger 注册 + default_logger
- [ ] tests/logging_infra_test.cpp: 5 个测试用例全部通过
- [ ] 日志 pattern = `[%Y-%m-%dT%H:%M:%S.%e%z] [%^%l%$] [%n] %v`（不含 %s:%#）
- [ ] LOG_INFO("msg") 输出: `[timestamp] [info] [module.tag] [file.cpp:line] msg`
</success_criteria>

<output>
Create `.planning/phases/01-logging-foundation/01-01-SUMMARY.md` when done
</output>
