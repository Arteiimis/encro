---
phase: 01-logging-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - xmake.lua
  - src/app/prelude.cpp
autonomous: true
requirements: [INF-02, INF-05]

must_haves:
  truths:
    - "xmake.lua 在 release/releasedbg 模式下定义 SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_INFO，debug/coverage 模式下定义 SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_TRACE"
    - "prelude.cpp 中 setupLogging() 重构为委托调用 logging::setup(LogConfig)，不再直接创建 sink 或 logger"
    - "prelude.cpp 中 logConfigSummary() 的 13 个 spdlog::info 调用替换为 LOG_INFO"
    - "prelude.cpp 添加 DEFINE_LOGGER(logtags::APP_PRELUDE) 和必要的 include"
    - "crash_runtime.cpp 的 spdlog::default_logger_raw() 仍能访问到 default_logger（通过 logging::setup 中 set_default_logger 保证）"
  artifacts:
    - path: "xmake.lua"
      provides: "SPDLOG_ACTIVE_LEVEL 编译期定义（4 种 build mode）"
      contains: "SPDLOG_ACTIVE_LEVEL"
    - path: "src/app/prelude.cpp"
      provides: "重构后的 setupLogging + logConfigSummary（使用 LOG_* 宏）"
      min_lines: 150
  key_links:
    - from: "prelude.cpp setupLogging()"
      to: "logging::setup()"
      via: "logging::LogConfig 参数"
      pattern: "logging::setup\\("
    - from: "prelude.cpp logConfigSummary()"
      to: "LOG_INFO 宏"
      via: "DEFINE_LOGGER(logtags::APP_PRELUDE)"
      pattern: "LOG_INFO\\(.*\\)"
---

<objective>
更新构建系统配置（SPDLOG_ACTIVE_LEVEL）并重构 prelude.cpp，使其委托给新的 logging::setup() 基础设施。同时验证 crash_runtime.cpp 的兼容性。

Purpose: 激活编译期日志剥离（release 零开销），完成日志配置从 prelude.cpp 到 setup.cpp 的迁移。
Output: xmake.lua 修改 + prelude.cpp 重构 + crash_runtime.cpp 确认不需要变更。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# Phase 决策
@.planning/phases/01-logging-foundation/01-CONTEXT.md
# Phase 研究（prelude.cpp 变更详情、xmake.lua SPDLOG_ACTIVE_LEVEL 配置）
@.planning/phases/01-logging-foundation/01-RESEARCH.md
# 项目约定
@.planning/codebase/CONVENTIONS.md
# 当前 prelude.cpp（需要重构的文件）
@src/app/prelude.cpp
# 当前 xmake.lua（需要添加 SPDLOG_ACTIVE_LEVEL）
@xmake.lua
# 当前 crash_runtime.cpp（验证兼容性）
@src/infra/crash_runtime.cpp
# Plan 01-01 创建的头文件
@src/logging/logging.h
@src/logging/log_tags.h
@src/logging/setup.h

<interfaces>
从 Plan 01-01 创建的关键接口（本 plan 依赖和使用）:

From src/logging/setup.h:
```cpp
struct LogConfig {
    bool verboseEnabled{false};
    bool verboseEchoEnabled{false};
    bool colorsEnabled{true};
    std::optional<std::filesystem::path> customLogDir;
};
auto setup(LogConfig const& config) -> std::optional<std::filesystem::path>;
auto shutdown() -> void;
```

From src/logging/logging.h:
```cpp
// DEFINE_LOGGER 宏: static auto* const gLoggerPtr = spdlog::get(tag)
// LOG_INFO(...), LOG_DEBUG(...), LOG_WARN(...), LOG_ERROR(...), LOG_TRACE(...), LOG_CRITICAL(...)
```

From src/logging/log_tags.h:
```cpp
// 24 个标签常量, 包括 APP_PRELUDE = "app.prelude", 等
```

从 prelude.cpp 现有模式（本 plan 需要修改的内容）:
- 匿名 namespace 行 24-128: setupLogging() — 将被重构为委托调用
- prelude namespace 行 159-198: logConfigSummary() — 13 个 spdlog::info 调用将被替换
- StartupContext: {cmd, verboseLogFilePath} — 接口保持不变
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SPDLOG_ACTIVE_LEVEL defines to xmake.lua for all 4 build modes</name>
  <files>xmake.lua</files>
  <read_first>
在编辑前务必先读取：
- @xmake.lua — 当前完整构建配置（116 行），了解现有 mode 定义结构（release/releasedbg/debug/coverage）和 target 定义的位置
- @.planning/phases/01-logging-foundation/01-RESEARCH.md — 第 5 节（xmake.lua SPDLOG_ACTIVE_LEVEL 配置），了解每个 mode 的具体值
- @.planning/phases/01-logging-foundation/01-CONTEXT.md — D-14（SPDLOG_ACTIVE_LEVEL 具体配置）
  </read_first>
  <action>
在 xmake.lua 中添加 SPDLOG_ACTIVE_LEVEL 编译定义 — per D-14。

在现有 mode 相关配置区域（`add_cxxflags`, `set_policy`, `add_ldflags` 等行之后，`add_requires` 之前）插入以下代码块:

```lua
-- SPDLOG_ACTIVE_LEVEL per build mode (D-14)
-- release/releasedbg: strip TRACE and DEBUG at compile time
-- debug/coverage: keep all levels for development
if is_mode("release") then
    add_defines("SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_INFO")
elseif is_mode("releasedbg") then
    add_defines("SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_INFO")
elseif is_mode("debug") then
    add_defines("SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_TRACE")
elseif is_mode("coverage") then
    add_defines("SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_TRACE")
end
```

关键说明:
- `SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_INFO` (值为 2): release/releasedbg 模式下，LOG_TRACE 和 LOG_DEBUG 宏展开为 `(void)0` — 编译期完全剥离，零开销
- `SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_TRACE` (值为 0): debug/coverage 模式下保留所有日志级别
- spdlog level enum 值: TRACE=0, DEBUG=1, INFO=2, WARN=3, ERROR=4, CRITICAL=5, OFF=6
- 这些 defines 通过 `add_defines` 添加，对所有 target（encro + tests）生效
- `add_defines` 是全局的（非 per-target），在 `set_languages` 和 mode 配置之后添加

插入位置建议: 在 `set_toolset("ld", "lld-link")` 之后、`add_cxxflags` 所在行附近。
  </action>
  <verify>
    <automated>xmake build -m release</automated>
  </verify>
  <acceptance_criteria>
- xmake.lua 包含 `is_mode("release")` → `add_defines("SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_INFO")` 代码块
- xmake.lua 包含 `is_mode("releasedbg")` → `add_defines("SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_INFO")` 代码块
- xmake.lua 包含 `is_mode("debug")` → `add_defines("SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_TRACE")` 代码块
- xmake.lua 包含 `is_mode("coverage")` → `add_defines("SPDLOG_ACTIVE_LEVEL=SPDLOG_LEVEL_TRACE")` 代码块
- `xmake build -m release` 编译成功（SPDLOG_ACTIVE_LEVEL 定义为 SPDLOG_LEVEL_INFO）
- `xmake build -m debug` 编译成功（SPDLOG_ACTIVE_LEVEL 定义为 SPDLOG_LEVEL_TRACE）
  </acceptance_criteria>
  <done>SPDLOG_ACTIVE_LEVEL 在所有 4 种 build mode 中正确定义。release/releasedbg 剥离 trace+debug。</done>
</task>

<task type="auto">
  <name>Task 2: Refactor prelude.cpp — setupLogging() delegates to logging::setup(), logConfigSummary() uses LOG_* macros</name>
  <files>src/app/prelude.cpp</files>
  <read_first>
在编辑前务必先读取：
- @src/app/prelude.cpp — 完整当前文件（201 行），理解 setupLogging()（行 59-128，匿名 namespace）和 logConfigSummary()（行 159-198，prelude namespace）的当前实现
- @.planning/phases/01-logging-foundation/01-RESEARCH.md — 第 4.3 节（prelude.cpp 变更说明），了解 setupLogging 重构为委托调用、logConfigSummary 的迁移模式
- @.planning/phases/01-logging-foundation/01-CONTEXT.md — D-12（所有 sink/logger 配置移至 setup.cpp）
- @src/logging/setup.h — 确认 LogConfig 字段名、setup() 签名
- @src/logging/logging.h — 确认 LOG_INFO 宏签名
- @src/logging/log_tags.h — 确认 APP_PRELUDE 标签常量名
- @.planning/codebase/CONVENTIONS.md — include 顺序规范
  </read_first>
  <action>
对 prelude.cpp 进行以下三组修改:

**修改 1 — Includes 调整:**

移除:
- `#include <spdlog/async.h>`
- `#include <spdlog/logger.h>`
- `#include <spdlog/sinks/basic_file_sink.h>`
- `#include <spdlog/sinks/stdout_color_sinks.h>`
- `#include <spdlog/sinks/stdout_sinks.h>`
（这些 sink/async 头文件不再需要 — 已移至 setup.cpp）

添加:
- `#include "logging/logging.h"` — LOG_* 宏和 DEFINE_LOGGER
- `#include "logging/log_tags.h"` — APP_PRELUDE 标签常量
- `#include "logging/setup.h"` — logging::setup() 和 LogConfig

保留:
- `#include "app/prelude.h"` — own header
- `#include "infra/terminal.h"` — terminal::println
- `<spdlog/spdlog.h>` — 仍然需要（setupLogging 中 spdlog::set_level(spdlog::level::off) 调用；如果 verbose 未启用时的快速路径）

**修改 2 — 匿名 namespace 中 setupLogging() 重构:**

移除匿名 namespace 中的:
- `resolveCommonLogDir()` 函数（行 39-57）— 已迁移到 setup.cpp，从 prelude.cpp 删除
- `setupLogging()` 的完整实现（行 59-128 中的 sink 创建、线程池初始化、logger 构造）— 替换为委托调用

新的 `setupLogging()` 实现:
```cpp
auto setupLogging(CmdParseResult const& cmd) -> std::optional<fs::path> {
    if (!cmd.verbose) {
        spdlog::set_level(spdlog::level::off);
        if (cmd.verboseEcho) {
            terminal::println(
                Warning,
                "Warning: --verbose-echo requires --verbose; option ignored."
            );
        }
        return std::nullopt;
    }

    auto const logConfig = logging::LogConfig{
        .verboseEnabled     = cmd.verbose,
        .verboseEchoEnabled = cmd.verboseEcho,
        .colorsEnabled      = terminal::colorsEnabled(),
    };

    auto const logFilePath = logging::setup(logConfig);

    // 保留终端 hint 输出（prelude 的逻辑，不是 logging 基础设施的逻辑）
    if (logFilePath.has_value() && !cmd.verboseEcho) {
        terminal::println(
            Hint,
            "Verbose log file: {}",
            terminal::path(logFilePath.value())
        );
    }

    return logFilePath;
}
```

注意:
- 不再需要 `readWindowsEnvPath` 函数 — 已随 resolveCommonLogDir 迁移到 setup.cpp
- 不再需要 anonymous namespace 中的 `<memory>`, `<mutex>`, `<system_error>`, `<vector>` includes（如果仅用于已删除的代码）
- `#include <cstdlib>` 仍可能需要（如果 readWindowsEnvPath 被删除且无其他使用）
- 确认 anonymous namespace 中剩余的 includes 仅包含当前仍需要的

**修改 3 — prelude::logConfigSummary() 宏迁移:**

在 prelude namespace 开始之前（anonymous namespace 之后）添加:
```cpp
DEFINE_LOGGER(logtags::APP_PRELUDE)
```

在 logConfigSummary() 函数中（行 159-198），将 13 个 `spdlog::info(...)` 调用替换为 `LOG_INFO(...)`:

机械替换:
- `spdlog::info("Automatic 'yes to all' enabled.");` → `LOG_INFO("Automatic 'yes to all' enabled.");`
- `spdlog::info("Recursive directory search enabled.");` → `LOG_INFO("Recursive directory search enabled.");`
- `spdlog::info("Pack output enabled for video processing.");` → `LOG_INFO("Pack output enabled for video processing.");`
- `spdlog::info("Pack-only mode enabled.");` → `LOG_INFO("Pack-only mode enabled.");`
- `spdlog::info("Resume mode enabled.");` → `LOG_INFO("Resume mode enabled.");`
- `spdlog::info("Restart mode enabled.");` → `LOG_INFO("Restart mode enabled.");`
- `spdlog::info("Collision-safe file naming disabled for unique flat outputs.");` → `LOG_INFO("Collision-safe file naming disabled for unique flat outputs.");`
- `spdlog::info("Picture folder summary images enabled.");` → `LOG_INFO("Picture folder summary images enabled.");`
- `spdlog::info("Using custom FFmpeg install directory: {}", config.ffmpegInstallDir.value().string());` → `LOG_INFO("Using custom FFmpeg install directory: {}", config.ffmpegInstallDir.value().string());`
- `spdlog::info("Using custom output path: {}", config.outputPath.value().string());` → `LOG_INFO("Using custom output path: {}", config.outputPath.value().string());`
- `spdlog::info("Using custom state file: {}", config.stateFilePath.value().string());` → `LOG_INFO("Using custom state file: {}", config.stateFilePath.value().string());`
- `spdlog::info("Using custom max parallel jobs: {}", config.maxParallelJobs.value());` → `LOG_INFO("Using custom max parallel jobs: {}", config.maxParallelJobs.value());`
- 多行的 `spdlog::info(` 调用也替换为 `LOG_INFO(` — 保持原有缩进和换行格式

注意: 保留所有行原有的参数内容和缩进风格。仅将函数名从 `spdlog::info` 改为 `LOG_INFO`。

**清理检查:**
- 确认 `<spdlog/spdlog.h>` include 仍然需要（用于 `spdlog::set_level(spdlog::level::off)`）
- 确认 `<cstdlib>` include 仍然需要（如果 readWindowsEnvPath 和 resolveCommonLogDir 已删除，可能不再需要）
- 确认不再有未使用的 include（`<memory>`, `<mutex>`, `<system_error>` 等）
- DEFINE_LOGGER 放在 `#include` 块之后、prelude namespace 之前（Claude's Discretion location for DEFINE_LOGGER）
  </action>
  <verify>
    <automated>xmake build -m debug</automated>
  </verify>
  <acceptance_criteria>
- prelude.cpp 中 resolveCommonLogDir() 函数已删除（迁移到 setup.cpp）
- setupLogging() 函数体简化为委托调用 logging::setup(LogConfig{...})
- prelude.cpp 中不再包含 spdlog sink 头文件（basic_file_sink.h, stdout_color_sinks.h, stdout_sinks.h）
- prelude.cpp 中不再包含 async.h 和 logger.h
- DEFINE_LOGGER(logtags::APP_PRELUDE) 出现在 prelude namespace 之前
- logConfigSummary() 中 13 个 spdlog::info 全部替换为 LOG_INFO
- logConfigSummary() 中 0 个 spdlog:: 直接调用残留
- `xmake build -m debug` 编译成功（encro target）
- verbose log file hint 的终端输出仍然工作（通过 terminal::println）
  </acceptance_criteria>
  <done>prelude.cpp 重构完成。setupLogging 委托给 logging::setup。logConfigSummary 使用 LOG_INFO 宏。</done>
</task>

<task type="auto">
  <name>Task 3: Verify crash_runtime.cpp default_logger compatibility with new logging::setup()</name>
  <files>src/infra/crash_runtime.cpp</files>
  <read_first>
在验证前务必先读取：
- @src/infra/crash_runtime.cpp — 当前完整文件（122 行），关注 tryWriteToLogger()（行 29-38）中的 spdlog::default_logger_raw() 调用
- @.planning/phases/01-logging-foundation/01-RESEARCH.md — 第 3.3 节（crash_runtime.cpp 特殊处理），确认 Phase 1 不添加 DEFINE_LOGGER
- @src/logging/setup.cpp — 确认 setup() 中 set_default_logger() 调用存在
  </read_first>
  <action>
验证 crash_runtime.cpp 与新的 logging::setup() 兼容 — 不修改 crash_runtime.cpp 本身。

验证清单:
1. 确认 crash_runtime.cpp 第 29-38 行 `tryWriteToLogger()` 使用 `spdlog::default_logger_raw()` — 保持不变
2. 确认 setup.cpp 中 `logging::setup()` 调用了 `spdlog::set_default_logger(std::move(defaultLogger))` — 确保 crash handler 能找到 default logger
3. 确认 crash_runtime.cpp 不添加 DEFINE_LOGGER — per RESEARCH §3.3（crash handler 使用 default_logger_raw，不使用 named logger 或 LOG_* 宏）
4. 确认 crash_runtime.cpp 保留 `#include <spdlog/spdlog.h>` — 直接使用 spdlog API 是允许的（crash handler 需要绕过宏层，直接访问 logger）

如果验证发现任何不兼容: 记录到 SUMMARY.md 并标记为 Phase 2 待改进项（per Pitfall #10 — crash handler + per-run files）。

关键验证点:
- `spdlog::default_logger_raw()` 不会在 crash handler 调用时返回 nullptr（前提: logging::setup() 先于 crash 被调用，这在正常流程中成立）
- `logger->critical("{}", message)` + `logger->flush()` 仍能正常写入日志文件（共享 file sink）
- stdout_color_sink_mt 在 crash handler 上下文中可能不安全（信号处理中调用 mutex），但 stderr fallback（writeToStderr）作为备份路径保留

无需对 crash_runtime.cpp 做任何代码修改。仅验证并记录结果。
  </action>
  <verify>
    <automated>grep -c "spdlog::default_logger_raw()" src/infra/crash_runtime.cpp</automated>
  </verify>
  <acceptance_criteria>
- crash_runtime.cpp 保持原样，tryWriteToLogger() 仍使用 spdlog::default_logger_raw()
- crash_runtime.cpp 不含 DEFINE_LOGGER
- setup.cpp 中 set_default_logger() 调用存在，确保 crash handler 路径工作
- grep 确认: crash_runtime.cpp 中 spdlog::default_logger_raw() 调用存在（零修改）
- Phase 2 改进标记: 在 SUMMARY.md 中记录 "crash handler 直接文件追加写入" 作为 Phase 2 待改进项（per FILE-04）
  </acceptance_criteria>
  <done>crash_runtime.cpp 兼容性已验证。无需修改。default_logger_raw() 路径保持工作。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cmd.verbose / cmd.verboseEcho → LogConfig | CLI 参数控制日志行为。无效组合（verboseEcho 无 verbose）已处理。 |
| terminal::colorsEnabled() → LogConfig.colorsEnabled | 终端颜色能力检测结果流入日志配置。仅影响 stdout sink 类型选择。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Denial of Service | SPDLOG_ACTIVE_LEVEL 定义 | accept | 编译期宏定义，无运行时风险。错误的值可能导致级别剥离不正确，但不影响功能正确性。 |
| T-01-02-02 | Information Disclosure | prelude.cpp logConfigSummary() | accept | 配置摘要信息（输出路径、并行数等）写入日志。这些是用户显式配置的值，属于正常操作日志。 |
| T-01-02-03 | Tampering | setupLogging() 委托调用 | accept | LogConfig 字段从 CLI 解析结果复制，无外部输入路径。 |
</threat_model>

<verification>
编译验证:
  `xmake build -m debug` — encro target 编译通过
  `xmake build -m release` — encro target 编译通过（SPDLOG_ACTIVE_LEVEL=INFO）

功能验证:
  `xmake run encro --verbose` — 日志文件生成，包含 `[app.prelude]` 模块标签
  `xmake run encro --help` — 正常输出帮助文本（无日志）
</verification>

<success_criteria>
- [ ] xmake.lua: 4 种 build mode 的 SPDLOG_ACTIVE_LEVEL 定义正确
- [ ] prelude.cpp: setupLogging() 简化为委托调用 logging::setup(LogConfig)
- [ ] prelude.cpp: logConfigSummary() 全部 13 个 spdlog::info → LOG_INFO
- [ ] prelude.cpp: 添加了 DEFINE_LOGGER(logtags::APP_PRELUDE)
- [ ] prelude.cpp: 移除了 sink/async 头文件 include
- [ ] crash_runtime.cpp: 无需修改（已验证兼容）
- [ ] debug 和 release 两种 mode 均编译通过
</success_criteria>

<output>
Create `.planning/phases/01-logging-foundation/01-02-SUMMARY.md` when done
</output>
