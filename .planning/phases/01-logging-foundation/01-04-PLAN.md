---
phase: 01-logging-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/pipeline.cpp
  - src/cmd/config_builder.cpp
  - src/video/video_progress_parser.cpp
  - src/video/video_output_planning.cpp
  - src/pack/pack_service.cpp
  - src/core/media_scanner.cpp
  - src/core/job_state.cpp
  - src/core/task_executor.cpp
  - src/core/parallel.cpp
  - src/infra/stop_signal.cpp
autonomous: true
requirements: [INF-04]

must_haves:
  truths:
    - "10 个文件各添加 DEFINE_LOGGER(logtags::XXX) — 为 Phase 2-4 的日志调用做准备（Q2 决议: 全部文件现在添加，避免后续回填）"
    - "每个文件添加 #include \"logging/logging.h\" 和 #include \"logging/log_tags.h\""
    - "无 spdlog:: 调用替换（这些文件当前不使用 spdlog）"
    - "所有 DEFINE_LOGGER 调用使用 logtags:: 常量（非字符串字面量）"
  artifacts:
    - path: "src/app/pipeline.cpp"
      provides: "DEFINE_LOGGER(logtags::APP_PIPELINE)"
    - path: "src/core/media_scanner.cpp"
      provides: "DEFINE_LOGGER(logtags::CORE_SCAN)"
  key_links:
    - from: "每个 .cpp 文件的 DEFINE_LOGGER"
      to: "spdlog::get(tag) via logging::setup() 注册"
      via: "static 缓存裸指针"
      pattern: "static auto\\* const gLoggerPtr"
---

<objective>
为 10 个当前不使用 spdlog 的源文件添加 DEFINE_LOGGER 和必要的 include — 每个文件仅添加 3 行（2 个 include + 1 个 DEFINE_LOGGER），无日志调用替换。

Purpose: 完成 Q2 决议 — 所有有标签映射的源文件现在添加 DEFINE_LOGGER，避免 Phase 2-4 回填。为后续日志调用提供即用型 logger 指针。
Output: 10 个修改后的 .cpp 文件，每个文件增加 2-3 行。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# Phase 决策
@.planning/phases/01-logging-foundation/01-CONTEXT.md
# Phase 研究（第 7 节: 标签到源文件的映射）
@.planning/phases/01-logging-foundation/01-RESEARCH.md
# 项目约定（include 顺序、DEFINE_LOGGER 位置）
@.planning/codebase/CONVENTIONS.md
# Plan 01-01 创建的宏和标签
@src/logging/logging.h
@src/logging/log_tags.h

<interfaces>
From src/logging/logging.h:
```cpp
#define DEFINE_LOGGER(tag) static auto* const gLoggerPtr = spdlog::get(tag)
```

From src/logging/log_tags.h — 本 plan 使用的标签常量:
- APP_PIPELINE = "app.pipeline"
- CMD_CONFIG = "cmd.config"
- VIDEO_PROGRESS = "video.progress"
- VIDEO_OUTPUT = "video.output"
- PACK_SERVICE = "pack.service"
- CORE_SCAN = "core.scan"
- CORE_JOB = "core.job"
- CORE_TASK = "core.task"
- CORE_PARALLEL = "core.parallel"
- INFRA_SIGNAL = "infra.signal"

每个文件的修改模式（完全相同）:
```cpp
// 在现有 #include 块末尾（project headers 区域，third-party headers 之前）添加:
#include "logging/log_tags.h"
#include "logging/logging.h"

// 在 include 块之后、namespace/using/匿名 namespace 之前添加:
DEFINE_LOGGER(logtags::XXX)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DEFINE_LOGGER to app, cmd, and infra files — pipeline.cpp, config_builder.cpp, stop_signal.cpp</name>
  <files>src/app/pipeline.cpp, src/cmd/config_builder.cpp, src/infra/stop_signal.cpp</files>
  <read_first>
在编辑每个文件前务必先读取:
- @src/app/pipeline.cpp — 了解现有 include 结构（当前不含 spdlog），确认 DEFINE_LOGGER 插入位置
- @src/cmd/config_builder.cpp — 了解现有 include 结构，确认插入位置
- @src/infra/stop_signal.cpp — 了解现有 include 结构（信号处理，当前不含 spdlog）
- @.planning/codebase/CONVENTIONS.md — include 顺序: own header first, project headers (by module), third-party, stdlib
- @src/logging/log_tags.h — 确认 APP_PIPELINE, CMD_CONFIG, INFRA_SIGNAL 常量名
  </read_first>
  <action>
对以下 3 个文件执行相同的机械添加操作。

**文件 1: src/app/pipeline.cpp** — 标签: `logtags::APP_PIPELINE`
- 当前 include 结构（行 1-12）:
  ```cpp
  #include "app/pipeline.h"           // own header
  #include "core/job_state.h"         // project: core
  #include "infra/terminal.h"         // project: infra
  #include "infra/stop_signal.h"      // project: infra
  #include "pack/pack.h"              // project: pack
  #include "picture/picture_process.h"// project: picture
  #include "video/video_process.h"    // project: video
  #include <algorithm>                // stdlib
  #include <filesystem>               // stdlib
  ```
- 在 project headers 区域末尾（video/video_process.h 之后）、stdlib includes 之前添加:
  ```cpp
  #include "logging/log_tags.h"
  #include "logging/logging.h"
  ```
- 在 `namespace fs = std::filesystem;` 之后、`using enum terminal::MessageKind;` 之后、`namespace pipeline {` 之前添加:
  ```cpp
  DEFINE_LOGGER(logtags::APP_PIPELINE)
  ```
- 无其他修改（本文件当前不使用 spdlog，无调用点替换）

**文件 2: src/cmd/config_builder.cpp** — 标签: `logtags::CMD_CONFIG`
- 读取文件确认 include 结构和模块级声明位置
- 在 project headers 区域末尾添加 `#include "logging/log_tags.h"` 和 `#include "logging/logging.h"`
- 在 include 块之后添加 `DEFINE_LOGGER(logtags::CMD_CONFIG)`
- 注意: 如果文件中有 `#include <spdlog/spdlog.h>` 或其他 spdlog 头文件，单独处理（可能从依赖链间接使用 — 仅添加新 include，不移除现有 include）
- 无其他修改

**文件 3: src/infra/stop_signal.cpp** — 标签: `logtags::INFRA_SIGNAL`
- 读取文件确认现有 include 结构
- 添加 `#include "logging/log_tags.h"` 和 `#include "logging/logging.h"`
- 添加 `DEFINE_LOGGER(logtags::INFRA_SIGNAL)`
- **关键约束:** stop_signal.cpp 处理 SIGINT/Ctrl+C 信号 — 不在信号处理器中使用 LOG_* 宏（信号处理中 spdlog 内部 mutex 可能导致死锁 — PITFALLS.md integration gotcha #3）。DEFINE_LOGGER 仅为文件中的常规代码（安装/重置 handler 函数）提供日志能力。信号处理器中的代码只设置原子标志，不调用日志。
- 无其他修改

**每个文件的操作清单:**
1. 读取文件，定位 include 块结束位置（最后一个 `#include` 行）
2. 在 project header includes 和 stdlib/third-party includes 之间插入 2 个新 include
3. 在 include 块之后的第一处模块级声明之前插入 DEFINE_LOGGER
4. 确保 DEFINE_LOGGER 在匿名 namespace 之前（per Claude's Discretion: DEFINE_LOGGER 紧接 #include 之后）
  </action>
  <verify>
    <automated>xmake build -m debug</automated>
  </verify>
  <acceptance_criteria>
- pipeline.cpp 包含 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`
- pipeline.cpp 包含 `DEFINE_LOGGER(logtags::APP_PIPELINE)`（使用常量，非字符串字面量）
- config_builder.cpp 包含正确的 include + DEFINE_LOGGER(logtags::CMD_CONFIG)
- stop_signal.cpp 包含正确的 include + DEFINE_LOGGER(logtags::INFRA_SIGNAL)
- 所有文件 `xmake build -m debug` 编译成功
- 零逻辑变更（仅添加 include 和 DEFINE_LOGGER，不修改任何函数体）
  </acceptance_criteria>
  <done>pipeline.cpp, config_builder.cpp, stop_signal.cpp 完成 DEFINE_LOGGER 添加。编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: Add DEFINE_LOGGER to video module files — video_progress_parser.cpp, video_output_planning.cpp</name>
  <files>src/video/video_progress_parser.cpp, src/video/video_output_planning.cpp</files>
  <read_first>
在编辑每个文件前务必先读取:
- @src/video/video_progress_parser.cpp — 了解现有 include 结构，确认插入位置
- @src/video/video_output_planning.cpp — 了解现有 include 结构
- @src/logging/log_tags.h — 确认 VIDEO_PROGRESS, VIDEO_OUTPUT 常量名
  </read_first>
  <action>
对以下 2 个 video 模块文件执行相同的 DEFINE_LOGGER 添加。

**文件 1: src/video/video_progress_parser.cpp** — 标签: `logtags::VIDEO_PROGRESS`
- 添加 `#include "logging/log_tags.h"` 和 `#include "logging/logging.h"`
- 添加 `DEFINE_LOGGER(logtags::VIDEO_PROGRESS)`
- 注意: 本文件负责解析 ffmpeg `-progress` 输出（帧数解析），当前无 spdlog 调用。DEFINE_LOGGER 为未来添加进度解析日志做准备。

**文件 2: src/video/video_output_planning.cpp** — 标签: `logtags::VIDEO_OUTPUT`
- 添加 `#include "logging/log_tags.h"` 和 `#include "logging/logging.h"`
- 添加 `DEFINE_LOGGER(logtags::VIDEO_OUTPUT)`
- 注意: 本文件负责计算输出文件路径（Keep layout mirroring），当前无 spdlog 调用。

**每个文件的操作:**
1. 读取确认 include 结构
2. 在 project headers 末尾添加 2 个 logging include
3. 在 include 块之后添加 DEFINE_LOGGER
4. 无其他修改
  </action>
  <verify>
    <automated>xmake build -m debug</automated>
  </verify>
  <acceptance_criteria>
- video_progress_parser.cpp 包含 DEFINE_LOGGER(logtags::VIDEO_PROGRESS)
- video_output_planning.cpp 包含 DEFINE_LOGGER(logtags::VIDEO_OUTPUT)
- 两个文件编译通过
- 零逻辑变更
  </acceptance_criteria>
  <done>video_progress_parser.cpp, video_output_planning.cpp 完成 DEFINE_LOGGER 添加。</done>
</task>

<task type="auto">
  <name>Task 3: Add DEFINE_LOGGER to pack and core files — pack_service.cpp, media_scanner.cpp, job_state.cpp, task_executor.cpp, parallel.cpp</name>
  <files>src/pack/pack_service.cpp, src/core/media_scanner.cpp, src/core/job_state.cpp, src/core/task_executor.cpp, src/core/parallel.cpp</files>
  <read_first>
在编辑每个文件前务必先读取:
- @src/pack/pack_service.cpp — PackService 编排层，了解 include 结构
- @src/core/media_scanner.cpp — 文件扫描器，了解 include 结构
- @src/core/job_state.cpp — jobstate::Store JSON 持久化，了解 include 结构
- @src/core/task_executor.cpp — 通用并行任务执行器，了解 include 结构
- @src/core/parallel.cpp — runIndexedTasks 简单并行抽象，了解 include 结构
- @src/logging/log_tags.h — 确认 PACK_SERVICE, CORE_SCAN, CORE_JOB, CORE_TASK, CORE_PARALLEL 常量名
  </read_first>
  <action>
对以下 5 个 pack 和 core 模块文件执行相同的 DEFINE_LOGGER 添加。

**文件 1: src/pack/pack_service.cpp** — 标签: `logtags::PACK_SERVICE`
- 添加 `#include "logging/log_tags.h"` 和 `#include "logging/logging.h"`
- 添加 `DEFINE_LOGGER(logtags::PACK_SERVICE)`

**文件 2: src/core/media_scanner.cpp** — 标签: `logtags::CORE_SCAN`
- 添加 include + DEFINE_LOGGER(logtags::CORE_SCAN)

**文件 3: src/core/job_state.cpp** — 标签: `logtags::CORE_JOB`
- 添加 include + DEFINE_LOGGER(logtags::CORE_JOB)
- 注意: job_state.cpp 涉及 JSON 序列化和 mutex 锁定的操作 — 未来在这些位置添加日志时需注意不记录敏感状态数据

**文件 4: src/core/task_executor.cpp** — 标签: `logtags::CORE_TASK`
- 添加 include + DEFINE_LOGGER(logtags::CORE_TASK)
- 注意: 当前 include 结构已知（from CONVENTIONS.md import 示例）:
  ```cpp
  #include "core/task_executor.h"     // own header
  #include "core/parallel.h"           // project: core
  #include "infra/stop_signal.h"       // project: infra
  #include <algorithm>                  // stdlib
  ```
- 在 project headers 末尾（`#include "infra/stop_signal.h"` 之后）、stdlib includes 之前添加 logging includes

**文件 5: src/core/parallel.cpp** — 标签: `logtags::CORE_PARALLEL`
- 添加 include + DEFINE_LOGGER(logtags::CORE_PARALLEL)

**每个文件的操作:**
1. 读取确认 include 结构
2. 在 project headers 区域末尾添加 2 个 logging include
3. 在 include 块之后添加 DEFINE_LOGGER
4. 无其他修改

**注意:** 这些文件当前全部不使用 spdlog — 仅添加基础设施，不替换任何调用。编译后的行为与修改前完全一致（DEFINE_LOGGER 中的 `static` 变量在首次日志调用前不会初始化，零运行时开销）。
  </action>
  <verify>
    <automated>xmake build -m debug && xmake build -m release</automated>
  </verify>
  <acceptance_criteria>
- 所有 5 个文件包含 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`
- 每个文件有正确的 `DEFINE_LOGGER(logtags::XXX)`（使用常量）
- `xmake build -m debug` 编译成功
- `xmake build -m release` 编译成功
- 零逻辑变更（仅添加基础设施代码）
  </acceptance_criteria>
  <done>5 个 pack/core 文件完成 DEFINE_LOGGER 添加。所有 10 个文件编译通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新边界 | 本 plan 仅添加静态 logger 指针定义，无新数据流或外部输入。DEFINE_LOGGER 中的 spdlog::get() 调用在首次日志使用时触发，此时 logger 已被 setup() 注册。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Denial of Service | static 初始化竞争 | accept | C++11 magic statics 保证线程安全的 static 局部变量初始化。spdlog::get() 在 logging::setup() 之后调用（setup 在 main 早期完成），logger 已存在。 |
| T-01-04-02 | Elevation of Privilege | 信号处理器中调用日志 | mitigate | stop_signal.cpp: 信号处理器中仅设置原子标志，不调用 LOG_* 宏。DEFINE_LOGGER 仅为 installHandler/reset 等常规函数提供日志能力。已在 Task 1 action 中明确标注此约束。 |
</threat_model>

<verification>
编译验证:
  `xmake build -m debug` — encro target 编译通过
  `xmake build -m release` — encro target 编译通过

硬编码标签检测:
  `grep -rn 'DEFINE_LOGGER("' src/ --include="*.cpp"`
  → 预期零结果（所有调用应使用 logtags:: 常量）

DEFINE_LOGGER 覆盖验证:
  `grep -rn "DEFINE_LOGGER" src/ --include="*.cpp" | wc -l`
  → 预期: 21 个 DEFINE_LOGGER 调用（10 个来自本 plan + 11 个来自 Plan 01-03 + 1 个来自 Plan 01-02 的 prelude.cpp）— 总共 22 个 DEFINE_LOGGER
  注意: crash_runtime.cpp 不包含 DEFINE_LOGGER（per RESEARCH §3.3）
</verification>

<success_criteria>
- [ ] 10 个文件全部添加了 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`
- [ ] 10 个文件全部添加了 `DEFINE_LOGGER(logtags::XXX)`（使用常量，非字符串字面量）
- [ ] 零逻辑变更 — 仅添加基础设施代码
- [ ] debug 和 release 两种 mode 均编译通过
- [ ] 全部 22 个标签常量对应的 .cpp 文件有 DEFINE_LOGGER（video.probe 标签除外 — 已注册但无对应文件）
</success_criteria>

<output>
Create `.planning/phases/01-logging-foundation/01-04-SUMMARY.md` when done
</output>
