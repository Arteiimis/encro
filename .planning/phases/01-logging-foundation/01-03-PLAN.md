---
phase: 01-logging-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/app_entry.cpp
  - src/infra/toolchain.cpp
  - src/utils/utils.cpp
  - src/video/video_encode_runner.cpp
  - src/video/video_batch_execution.cpp
  - src/video/video_process.cpp
  - src/video/video_info.cpp
  - src/video/video_encoding_state.cpp
  - src/picture/picture_compress.cpp
  - src/picture/picture_process.cpp
  - src/pack/packer.cpp
autonomous: true
requirements: [INF-01, INF-04, OBS-01, OBS-02]

must_haves:
  truths:
    - "所有 11 个文件中 spdlog::debug/info/warn/error 调用替换为对应的 LOG_DEBUG/INFO/WARN/ERROR 宏"
    - "每个文件添加 DEFINE_LOGGER(logtags::XXX) — 缓存 logger 指针，零每次调用开销"
    - "每个文件添加 #include \"logging/logging.h\" 和 #include \"logging/log_tags.h\""
    - "文件中原有的 #include <spdlog/spdlog.h> 移除（如果不再有其他 spdlog API 使用）"
    - "零 spdlog:: 直接调用残留（通过 grep 验证）"
  artifacts:
    - path: "src/app/app_entry.cpp"
      provides: "failWithHint 中的 spdlog::error → LOG_ERROR"
    - path: "src/video/video_encode_runner.cpp"
      provides: "14 个编码 runner 日志调用点 → LOG_* 宏"
    - path: "src/video/video_process.cpp"
      provides: "21 个视频处理 pipeline 日志调用点 → LOG_* 宏"
    - path: "src/picture/picture_compress.cpp"
      provides: "21 个图片压缩日志调用点 → LOG_* 宏"
  key_links:
    - from: "video_encode_runner.cpp LOG_DEBUG"
      to: "SPDLOG_LOGGER_CALL(gLoggerPtr, level::debug, ...)  via 宏展开"
      via: "DEFINE_LOGGER(logtags::VIDEO_ENCODE)"
      pattern: "LOG_DEBUG\\(.*\\)"
    - from: "utils.cpp LOG_INFO"
      to: "子进程日志输出 (module tag = utils.subprocess)"
      via: "%n pattern flag"
      pattern: "LOG_INFO\\(.*\\)"
---

<objective>
将 11 个当前直接使用 spdlog API 的源文件迁移到新的 LOG_* 宏系统。每个文件添加 DEFINE_LOGGER 和必要的 include，将 spdlog::x() 调用机械替换为 LOG_X()。

Purpose: 激活源位置自动捕获（OBS-01）和模块标签（OBS-02），完成 INF-01（所有源文件使用宏替代直接 spdlog 调用）。
Output: 11 个修改后的 .cpp 文件，零 spdlog::debug/info/warn/error 调用残留（setup.cpp 和 crash_runtime.cpp 除外）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# Phase 决策
@.planning/phases/01-logging-foundation/01-CONTEXT.md
# Phase 研究（第 3 节: 文件迁移清单和迁移模式、每个文件的标签映射）
@.planning/phases/01-logging-foundation/01-RESEARCH.md
# 项目约定
@.planning/codebase/CONVENTIONS.md
# Plan 01-01 创建的宏和标签（本 plan 依赖）
@src/logging/logging.h
@src/logging/log_tags.h

<interfaces>
从 Plan 01-01 创建的关键接口:

From src/logging/logging.h:
```cpp
#define DEFINE_LOGGER(tag) static auto* const gLoggerPtr = spdlog::get(tag)
#define LOG_TRACE(...) SPDLOG_LOGGER_CALL(gLoggerPtr, spdlog::level::trace, "[{}:{}] {}", ...)
#define LOG_DEBUG(...) SPDLOG_LOGGER_CALL(gLoggerPtr, spdlog::level::debug, "[{}:{}] {}", ...)
#define LOG_INFO(...)  SPDLOG_LOGGER_CALL(gLoggerPtr, spdlog::level::info,  "[{}:{}] {}", ...)
#define LOG_WARN(...)  SPDLOG_LOGGER_CALL(gLoggerPtr, spdlog::level::warn,  "[{}:{}] {}", ...)
#define LOG_ERROR(...) SPDLOG_LOGGER_CALL(gLoggerPtr, spdlog::level::err,   "[{}:{}] {}", ...)
#define LOG_CRITICAL(...) SPDLOG_LOGGER_CALL(gLoggerPtr, spdlog::level::critical, "[{}:{}] {}", ...)
```

From src/logging/log_tags.h — 本 plan 使用的标签常量:
- APP_ENTRY = "app.entry"
- INFRA_TOOLCHAIN = "infra.toolchain"
- UTILS_SUBPROCESS = "utils.subprocess"
- VIDEO_ENCODE = "video.encode"
- VIDEO_BATCH = "video.batch"
- VIDEO_PROCESS = "video.process"
- VIDEO_INFO = "video.info"
- VIDEO_STATE = "video.state"
- PICTURE_PROCESS = "picture.process"
- PICTURE_COMPRESS = "picture.compress"
- PACK_ZIP = "pack.zip"

迁移模式（from RESEARCH §3.2）:
1. 文件顶部添加: #include "logging/logging.h" + #include "logging/log_tags.h"
2. 紧接 include 块之后添加: DEFINE_LOGGER(logtags::XXX)
3. 机械替换: spdlog::debug → LOG_DEBUG, spdlog::info → LOG_INFO, etc.
4. 移除 #include <spdlog/spdlog.h>（如果文件中无其他 spdlog API 使用）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate app, infra, and utils files — app_entry.cpp, toolchain.cpp, utils.cpp</name>
  <files>src/app/app_entry.cpp, src/infra/toolchain.cpp, src/utils/utils.cpp</files>
  <read_first>
在编辑每个文件前务必先读取该文件:
- @src/app/app_entry.cpp — 1 个 spdlog::error 在 failWithHint() 行 82，理解 anonymous namespace 中包含 spdlog 调用的函数
- @src/infra/toolchain.cpp — 2 个 spdlog::info 在 toolchain::resolve() 中行 25-26
- @src/utils/utils.cpp — 13 个 spdlog 调用（debug/info/warn），分布在 exec2() 和相关 helper 中
- @.planning/codebase/CONVENTIONS.md — include 顺序规范（own header first, project headers by module, third-party, stdlib）
- @src/logging/log_tags.h — 确认 APP_ENTRY, INFRA_TOOLCHAIN, UTILS_SUBPROCESS 常量名
  </read_first>
  <action>
对以下 3 个文件执行机械迁移:

**文件 1: src/app/app_entry.cpp**
标签: `logtags::APP_ENTRY`

- 在现有 includes 中添加 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`（放在 `<spdlog/spdlog.h>` 之前或之后，按 include 顺序规范: project headers 在 third-party 之前）
- 在 `using enum terminal::MessageKind;` 之后、`namespace {` 之前添加: `DEFINE_LOGGER(logtags::APP_ENTRY)`
- failWithHint() 行 82: `spdlog::error("{}", message);` → `LOG_ERROR("{}", message);`
- 移除 `#include <spdlog/spdlog.h>`（app_entry.cpp 中无其他 spdlog API 使用；注意验证: 只有 failWithHint 中的 spdlog::error 调用和 logConfigSummary 的调用 — 等等，prelude::logConfigSummary 在 app_entry.cpp 中被调用但实现不在这个文件。确认 app_entry.cpp 本身不再需要 spdlog）

**文件 2: src/infra/toolchain.cpp**
标签: `logtags::INFRA_TOOLCHAIN`

- 添加 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`
- 添加 `DEFINE_LOGGER(logtags::INFRA_TOOLCHAIN)`（include 之后、namespace 之前）
- 行 25: `spdlog::info("Using FFmpeg at: {}", out.ffmpegPath.value().string());` → `LOG_INFO("Using FFmpeg at: {}", out.ffmpegPath.value().string());`
- 行 26: `spdlog::info("Using FFprobe at: {}", out.ffprobePath.value().string());` → `LOG_INFO("Using FFprobe at: {}", out.ffprobePath.value().string());`
- 移除 `#include <spdlog/spdlog.h>`（如果 toolchain.cpp 中无其他 spdlog API 使用）

**文件 3: src/utils/utils.cpp**
标签: `logtags::UTILS_SUBPROCESS`

- 添加 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`
- 添加 `DEFINE_LOGGER(logtags::UTILS_SUBPROCESS)`（include 之后、namespace 之前）
- 全部 13 个 spdlog 调用替换:
  - 行 40: `spdlog::debug("Executing command: {}", cmd)` → `LOG_DEBUG("Executing command: {}", cmd)`
  - 行 98: `spdlog::debug(...)` → `LOG_DEBUG(...)`
  - 行 139: `spdlog::info("Terminating child process...")` → `LOG_INFO("Terminating child process...")`
  - 行 143: `spdlog::warn(...)` → `LOG_WARN(...)`
  - 行 153: `spdlog::warn(...)` → `LOG_WARN(...)`
  - 行 210: `spdlog::debug("Failed to close...")` → `LOG_DEBUG("Failed to close...")`
  - 行 235: `spdlog::info("Terminating child process...")` → `LOG_INFO("Terminating child process...")`
  - 行 240: `spdlog::warn(...)` → `LOG_WARN(...)`
  - 行 251: `spdlog::warn(...)` → `LOG_WARN(...)`
  - 行 263: `spdlog::warn(...)` → `LOG_WARN(...)`
  （仅替换函数名 spdlog::debug → LOG_DEBUG, spdlog::info → LOG_INFO, spdlog::warn → LOG_WARN — 保持参数和格式完全不变）
- 移除 `#include <spdlog/spdlog.h>`（如果 utils.cpp 中无其他 spdlog API 使用）
  </action>
  <verify>
    <automated>xmake build -m debug</automated>
  </verify>
  <acceptance_criteria>
- 每个文件包含 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`
- 每个文件在 include 块之后有 `DEFINE_LOGGER(logtags::XXX)` 调用（使用正确的标签常量名，不是字符串字面量）
- 零 `spdlog::debug(` 调用残留（在 app_entry.cpp, toolchain.cpp, utils.cpp 中）
- 零 `spdlog::info(` 调用残留
- 零 `spdlog::error(` 调用残留
- 零 `spdlog::warn(` 调用残留
- `xmake build -m debug` 编译成功
- 编译后快速 grep 验证: `grep "spdlog::debug\|spdlog::info\|spdlog::warn\|spdlog::error" src/app/app_entry.cpp src/infra/toolchain.cpp src/utils/utils.cpp` → 零结果（排除注释）
  </acceptance_criteria>
  <done>app_entry.cpp, toolchain.cpp, utils.cpp 迁移完成。零 spdlog 直接调用残留。</done>
</task>

<task type="auto">
  <name>Task 2: Migrate video module files — video_encode_runner.cpp, video_batch_execution.cpp, video_process.cpp, video_info.cpp, video_encoding_state.cpp</name>
  <files>src/video/video_encode_runner.cpp, src/video/video_batch_execution.cpp, src/video/video_process.cpp, src/video/video_info.cpp, src/video/video_encoding_state.cpp</files>
  <read_first>
在编辑每个文件前务必先读取该文件:
- @src/video/video_encode_runner.cpp — 14 个 spdlog 调用（error/debug/warn/info），分布在 encodeVideo() 函数中
- @src/video/video_batch_execution.cpp — 12 个 spdlog 调用（debug/info/warn），分布在多个函数中
- @src/video/video_process.cpp — 21 个 spdlog 调用（info/debug/error），分布在 handlePathEncoding 等多个函数中
- @src/video/video_info.cpp — 12 个 spdlog 调用（debug/warn），分布在 ffprobe 信息获取函数中
- @src/video/video_encoding_state.cpp — 1 个 spdlog::info 在状态更新函数中（行 85）
- @src/logging/log_tags.h — 确认 VIDEO_ENCODE, VIDEO_BATCH, VIDEO_PROCESS, VIDEO_INFO, VIDEO_STATE 常量名
- @.planning/codebase/CONVENTIONS.md — include 顺序规范
  </read_first>
  <action>
对以下 5 个 video 模块文件执行机械迁移。每个文件的迁移模式相同:
1. 添加 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`（project headers 区域，在 third-party includes 之前）
2. 在 `#include` 块之后、文件级声明（`namespace fs`, `using`, `namespace {`）之前添加 `DEFINE_LOGGER(logtags::XXX)`
3. 机械替换所有 `spdlog::debug/info/warn/error/critical` → `LOG_DEBUG/INFO/WARN/ERROR/CRITICAL`
4. 如果文件不再需要 `#include <spdlog/spdlog.h>`（文件中无其他 spdlog API 使用），移除该 include

**文件 1: src/video/video_encode_runner.cpp** — 标签: `logtags::VIDEO_ENCODE`
- 14 个调用点: 1 × spdlog::error(行53), 3 × spdlog::debug(128,163,312,323), 1 × spdlog::error(145), 1 × spdlog::warn(153), 1 × spdlog::info(181), 1 × spdlog::debug(189), 2 × spdlog::error(215,224), 1 × spdlog::debug(235), 2 × spdlog::warn(256,289)
- 全部替换为 LOG_* 对应级别

**文件 2: src/video/video_batch_execution.cpp** — 标签: `logtags::VIDEO_BATCH`
- 12 个调用点: spdlog::debug(128,256,320), spdlog::info(197,235,272,295,314,342,387), spdlog::warn(207,274)
- 全部替换为 LOG_* 对应级别

**文件 3: src/video/video_process.cpp** — 标签: `logtags::VIDEO_PROCESS`
- 21 个调用点: spdlog::info(169,176,189,196,388,411,437,452,481,491,499,532), spdlog::debug(350), spdlog::error(262,286,432,513,525)
- 全部替换为 LOG_* 对应级别

**文件 4: src/video/video_info.cpp** — 标签: `logtags::VIDEO_INFO`
- 12 个调用点: spdlog::debug(124,141,153,208,287,299,320,361,369), spdlog::warn(409)
- 全部替换为 LOG_* 对应级别

**文件 5: src/video/video_encoding_state.cpp** — 标签: `logtags::VIDEO_STATE`
- 1 个调用点: spdlog::info(85)
- 替换为 LOG_INFO(...)
- 注意: 这个文件可能当前没有 `#include <spdlog/spdlog.h>` — 如果原本是从其他头文件间接引入的，需要添加或确认 include 路径

**每个文件的 include 调整规则:**
- 搜索每个文件中的 `#include <spdlog/spdlog.h>` — 如果存在且文件中无其他 spdlog API 使用（构造 logger、访问 registry 等），移除该 include
- video_encode_runner.cpp 如果包含 spdlog 头文件，检查是否还有其他 spdlog 符号使用
- 确保移除 spdlog include 后编译不报错（因为 logging.h 内部已包含 spdlog）

**机械替换注意事项:**
- 仅替换函数/宏名称，保持格式字符串和参数完全不变
- `spdlog::debug(` → `LOG_DEBUG(`
- `spdlog::info(` → `LOG_INFO(`
- `spdlog::warn(` → `LOG_WARN(`
- `spdlog::error(` → `LOG_ERROR(`
- 多行调用的第一行替换函数名，后续行保持缩进不变
- 不要修改日志消息内容、不要调整日志级别
  </action>
  <verify>
    <automated>xmake build -m debug</automated>
  </verify>
  <acceptance_criteria>
- 所有 5 个 video 文件包含 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`
- 每个文件有正确的 `DEFINE_LOGGER(logtags::VIDEO_XXX)` 调用（使用常量，不是字符串字面量）
- 零 `spdlog::debug/info/warn/error/critical(` 调用残留（通过 grep 验证）
- `xmake build -m debug` 编译成功（encro target）
- 编译后快速 grep: `grep "spdlog::debug\|spdlog::info\|spdlog::warn\|spdlog::error" src/video/*.cpp` → 仅注释中出现或零结果
  </acceptance_criteria>
  <done>5 个 video 文件迁移完成。~60 个 spdlog 调用全部替换为 LOG_* 宏。</done>
</task>

<task type="auto">
  <name>Task 3: Migrate picture and pack files — picture_compress.cpp, picture_process.cpp, packer.cpp</name>
  <files>src/picture/picture_compress.cpp, src/picture/picture_process.cpp, src/pack/packer.cpp</files>
  <read_first>
在编辑每个文件前务必先读取该文件:
- @src/picture/picture_compress.cpp — 21 个 spdlog 调用（warn/info/debug），分布在 compressImageBatch 等多个函数中
- @src/picture/picture_process.cpp — 1 个 spdlog::error 在 runPicturePackWorkflow 附近（行 580）
- @src/pack/packer.cpp — 2 个 spdlog::debug（行 416, 473）
- @src/logging/log_tags.h — 确认 PICTURE_PROCESS, PICTURE_COMPRESS, PACK_ZIP 常量名
  </read_first>
  <action>
对以下 3 个文件执行机械迁移:

**文件 1: src/picture/picture_compress.cpp** — 标签: `logtags::PICTURE_COMPRESS`
- 21 个调用点分布在多个函数中（compressImageBatch 及相关 helper）
- 从 grep 结果: spdlog::warn(行38,49,140,149), spdlog::info(行82,207,225,258,274,324,340,345), spdlog::debug(行136,157)
- 全部替换为 LOG_WARN/LOG_INFO/LOG_DEBUG
- 添加 `#include "logging/logging.h"` + `#include "logging/log_tags.h"` + `DEFINE_LOGGER(logtags::PICTURE_COMPRESS)`
- 移除 `#include <spdlog/spdlog.h>`（如果无其他 spdlog API）

**文件 2: src/picture/picture_process.cpp** — 标签: `logtags::PICTURE_PROCESS`
- 1 个调用点: `spdlog::error(errMsg)` (行 580) → `LOG_ERROR("{}", errMsg)` 
- 注意: 原始代码是 `spdlog::error(errMsg)` 还是 `spdlog::error("{}", errMsg)` — 请读取确认后保留相同参数格式
- 添加 include + DEFINE_LOGGER
- 移除 `#include <spdlog/spdlog.h>` 如果不再需要

**文件 3: src/pack/packer.cpp** — 标签: `logtags::PACK_ZIP`
- 2 个调用点: spdlog::debug(行416,473)
- 全部替换为 LOG_DEBUG
- 添加 include + DEFINE_LOGGER
- 移除 `#include <spdlog/spdlog.h>` 如果不再需要

**注意事项:**
- picture_compress.cpp 有最多的调用点（21 个），仔细确认每个替换都正确
- packer.cpp 可能除了 spdlog::debug 外还有其他 spdlog 使用（检查 `Packer` 类中是否有自定义 logger/sink 创建）
- 如果文件中有 `spdlog::level::` 枚举使用（非调用），保留相关 include
  </action>
  <verify>
    <automated>xmake build -m debug</automated>
  </verify>
  <acceptance_criteria>
- 所有 3 个文件包含 `#include "logging/logging.h"` 和 `#include "logging/log_tags.h"`
- 每个文件有正确的 `DEFINE_LOGGER(logtags::XXX)` 调用
- 零 `spdlog::debug/info/warn/error(` 调用残留
- `xmake build -m debug` 编译成功（encro target）
- 编译后: `grep "spdlog::debug\|spdlog::info\|spdlog::warn\|spdlog::error" src/picture/*.cpp src/pack/packer.cpp` → 零结果
  </acceptance_criteria>
  <done>3 个 picture/pack 文件迁移完成。~24 个 spdlog 调用替换为 LOG_* 宏。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 日志消息参数 → LOG_* 宏 fmt::format | 用户提供的格式化参数（文件路径、FFmpeg 输出）进入日志消息。fmt::format 安全处理格式字符串。 |
| DEFINE_LOGGER 静态初始化 → spdlog::get() | 首次日志调用时触发 static 初始化。此时 logging::setup() 已完成，logger 已注册。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | 机械替换引入的拼写错误 | mitigate | grep 验证零 spdlog:: 残留 + 编译验证 + 人工 code review。 |
| T-01-03-02 | Denial of Service | LOG_* 宏 fmt::format 性能 | accept | fmt::format 在 call site 预格式化（vs spdlog 原生延迟格式化）。CLI 规模下开销可忽略（每个文件编码一次日志，非每帧）。Phase 2 可添加 should_log 守卫优化。 |
</threat_model>

<verification>
编译验证:
  `xmake build -m debug` — encro target 编译通过
  `xmake build -m release` — encro target 编译通过（SPDLOG_ACTIVE_LEVEL=INFO 剥离 TRACE/DEBUG）

残留检测:
  `grep -rn "spdlog::debug\|spdlog::info\|spdlog::warn\|spdlog::error\|spdlog::critical" src/ --include="*.cpp" | grep -v "setup.cpp" | grep -v "crash_runtime.cpp"`
  → 预期零结果（setup.cpp 和 crash_runtime.cpp 是仅有的合法直接 spdlog 用户）

硬编码标签检测:
  `grep -rn 'DEFINE_LOGGER("' src/ --include="*.cpp"`
  → 预期零结果（所有调用应使用 logtags:: 常量，不是字符串字面量）
</verification>

<success_criteria>
- [ ] 11 个文件全部迁移到 LOG_* 宏
- [ ] 每个文件有正确的 DEFINE_LOGGER(logtags::XXX)（使用常量，非字符串）
- [ ] 零 spdlog::debug/info/warn/error 调用残留（排除 setup.cpp 和 crash_runtime.cpp）
- [ ] debug 和 release 两种 mode 均编译通过
- [ ] 所有替换保持原有参数内容和缩进风格不变
</success_criteria>

<output>
Create `.planning/phases/01-logging-foundation/01-03-SUMMARY.md` when done
</output>
