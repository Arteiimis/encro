---
phase: 19-cli11-migration
plan: 02
type: execute
wave: 3
depends_on: [19-01]
files_modified:
  - src/cmd/config_builder.h
  - src/cmd/config_builder.cpp
autonomous: true
requirements:
  - CLI11-02

must_haves:
  truths:
    - "buildConfig() accepts CmdParseResult const& instead of variables_map const&"
    - "All 29 vm.count()/vm.at()/getParamStr() references in config_builder.cpp replaced with direct CmdParseResult field access"
    - "Config validation errors remain byte-identical to pre-migration messages"
    - "AppConfig produced by buildConfig(result) is identical to pre-migration buildConfig(vm) for the same inputs"
  artifacts:
    - path: "src/cmd/config_builder.h"
      provides: "Updated buildConfig signature accepting CmdParseResult const&"
      min_lines: 16
    - path: "src/cmd/config_builder.cpp"
      provides: "All internal helpers and buildConfig body adapted to CmdParseResult fields"
      min_lines: 390
  key_links:
    - from: "src/cmd/config_builder.h"
      to: "src/cmd/cmd.h"
      via: "#include \"cmd/cmd.h\" replacing boost include"
      pattern: "#include \"cmd/cmd.h\""
    - from: "config_builder.cpp readProcessType()"
      to: "result.processType"
      via: "direct field access replacing getParamStr(vm, \"type\")"
      pattern: "result\\.processType"
    - from: "config_builder.cpp readOutputFormat()"
      to: "result.outputFormat"
      via: "direct field access replacing getParamStr(vm, \"output-format\")"
      pattern: "result\\.outputFormat"
    - from: "config_builder.cpp readForceNameConflictHandling()"
      to: "result.forceConflictHandling"
      via: "direct field access replacing getParamStr+vm.count"
      pattern: "result\\.forceConflictHandling"
---

<objective>
Migrate `src/cmd/config_builder.h` and `src/cmd/config_builder.cpp` from boost::program_options `variables_map` to the new `CmdParseResult` struct.

Purpose: Replace all 29 vm references with direct struct field access. The `buildConfig()` function signature changes from `variables_map const&` to `CmdParseResult const&`. All internal helper functions (`readProcessType`, `readOutputFormat`, `readMaxParallelJobs`, `readOutputLayout`, `readForceNameConflictHandling`, `readPictureFolderSummary`) are adapted. Error messages are preserved exactly (per D-07).
Output: Updated config_builder that compiles with zero boost::program_options dependencies and produces identical `AppConfig` results.
</objective>

<execution_context>
@C:/Users/LEGION/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LEGION/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/cmd/config_builder.h
@src/cmd/config_builder.cpp
@src/cmd/cmd.h
@src/core/app_context.h
@.planning/phases/19-cli11-migration/19-CONTEXT.md

<interfaces>
<!-- New CmdParseResult struct (from Plan 1) — all fields available for direct access -->
All 26 option fields are directly accessible on CmdParseResult:
- General: `result.help`, `result.verbose`, `result.verboseEcho`, `result.fullProgress`, `result.color`, `result.yesToAll`
- IO: `result.input`, `result.inputs`, `result.output`, `result.stateFile`, `result.outputFormat`, `result.flat`, `result.keep`, `result.forceConflictHandling`, `result.folderSummary`, `result.recursive`
- Processing: `result.processType`, `result.maxJobs`, `result.resume`, `result.restart`, `result.ffmpegPath`, `result.compress`, `result.imageQuality`
- FileOp: `result.pack`, `result.packOnly`, `result.overwrite`

<!-- Current buildConfig signature (to be changed) -->
From src/cmd/config_builder.h (current):
```cpp
auto buildConfig(boost::program_options::variables_map const& vm)
  -> eh::Result<appctx::AppConfig>;
```

<!-- AppConfig struct — unchanged, this is the output -->
From src/core/app_context.h:
```cpp
struct AppConfig {
  bool yesToAll = false;
  bool recursive = false;
  bool packOutput = false;
  bool packOnly = false;
  bool resumeState = false;
  bool restartState = false;
  bool forceNameConflictHandling = true;
  bool pictureFolderSummary = false;
  bool compressImages = false;
  std::optional<int> imageQuality;
  bool verbose = false;
  bool verboseEcho = false;
  bool fullProgress = false;
  std::optional<std::size_t> maxParallelJobs;
  OutputLayout outputLayout = OutputLayout::Flat;
  std::string processType = "video";
  std::string outputFormat = "mp4";
  fs::path inputPath;
  std::vector<fs::path> inputPaths;
  std::optional<fs::path> outputPath;
  std::optional<fs::path> stateFilePath;
  std::optional<fs::path> ffmpegInstallDir;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task: Rewrite config_builder.h — change buildConfig signature to accept CmdParseResult</name>
  <files>src/cmd/config_builder.h</files>
  <read_first>src/cmd/config_builder.h, src/cmd/cmd.h</read_first>
  <action>
Replace the contents of `src/cmd/config_builder.h` with:

```cpp
#pragma once

#include "cmd/cmd.h"
#include "core/app_context.h"
#include "core/error_handle.h"

namespace cmd {

auto buildConfig(CmdParseResult const& result)
  -> eh::Result<appctx::AppConfig>;

}  // namespace cmd
```

Key changes from current:
1. Replace `#include <boost/program_options/variables_map.hpp>` with `#include "cmd/cmd.h"` (which declares CmdParseResult)
2. Replace `boost::program_options::variables_map const& vm` parameter with `CmdParseResult const& result`
3. The `cmd` namespace prefix on `buildConfig` is retained

No other changes. The `eh::Result<appctx::AppConfig>` return type is unchanged.
</action>
  <acceptance_criteria>
1. `grep -c "boost/program_options" src/cmd/config_builder.h` returns 0
2. `grep -c '#include "cmd/cmd.h"' src/cmd/config_builder.h` returns 1
3. `grep -c "CmdParseResult const&" src/cmd/config_builder.h` returns 1
4. `grep -c "boost::program_options::variables_map" src/cmd/config_builder.h` returns 0
5. File is 13-17 lines total
</acceptance_criteria>
  <verify>
    <automated>rg --no-heading -c "boost/program_options" src/cmd/config_builder.h</automated>
  </verify>
  <done>config_builder.h includes only cmd.h (no boost::po headers). buildConfig accepts CmdParseResult const&.</done>
</task>

<task type="auto">
  <name>Task: Rewrite config_builder.cpp — 29 vm references migrated to CmdParseResult fields</name>
  <files>src/cmd/config_builder.cpp</files>
  <read_first>src/cmd/config_builder.cpp, src/cmd/cmd.h, src/core/app_context.h</read_first>
  <action>
Migrate all 29 `vm.count()`/`vm.at()`/`getParamStr(vm, ...)` references in `config_builder.cpp` to direct `CmdParseResult` field access. The parameter name changes from `vm` to `result`.

**Error messages must be preserved EXACTLY** — you are changing HOW values are accessed but NOT what error strings look like (per D-07).

**Specific changes to each function:**

### readProcessType() — lines 60-77
- Signature: `auto readProcessType(CmdParseResult const& result) -> eh::Result<std::string>`
- `if (!vm.count("type"))` → remove guard (result.processType always has default "video")
- `getParamStr(vm, "type")` → `result.processType`
- Valid type check against `result.processType` instead

### readOutputFormat() — lines 79-93
- Signature: `auto readOutputFormat(CmdParseResult const& result) -> eh::Result<std::string>`
- `if (!vm.count("output-format"))` → remove guard (result.outputFormat always has default "mp4")
- `getParamStr(vm, "output-format")` → `result.outputFormat`
- Valid format check against `result.outputFormat`

### readMaxParallelJobs() — lines 95-103
- Signature: `auto readMaxParallelJobs(CmdParseResult const& result) -> eh::Result<std::optional<std::size_t>>`
- `if (!vm.count("jobs"))` → `if (!result.maxJobs.has_value())`
- `vm.at("jobs").as<std::size_t>()` → `result.maxJobs.value()`
- Zero check remains identical

### readOutputLayout() — lines 105-117
- Signature: `auto readOutputLayout(CmdParseResult const& result) -> eh::Result<appctx::OutputLayout>`
- `vm.count("flat") > 0` → `result.flat`
- `vm.count("keep") > 0` → `result.keep`
- Error message: `"--flat and --keep cannot be used together."` — unchanged

### readForceNameConflictHandling() — lines 119-132
- Signature: `auto readForceNameConflictHandling(CmdParseResult const& result) -> eh::Result<bool>`
- `if (!vm.count("force-conflict-handling"))` → `if (result.forceConflictHandling.empty())` (but it always has default "y", so the guard can stay or be removed)
- Actually: since `forceConflictHandling` has a default "y", we should NOT check for "not present". Instead check the value directly.
- `getParamStr(vm, "force-conflict-handling")` → `result.forceConflictHandling`
- Lowercase transform + y/n check unchanged

### readPictureFolderSummary() — lines 134-137
- Signature: `auto readPictureFolderSummary(CmdParseResult const& result) -> eh::Result<bool>`
- `vm.count("folder-summary") > 0` → `result.folderSummary`

### buildConfig() — lines 280-412
All direct vm references in the function body:

**Line 284:** `vm.count("resume") > 0` → `result.resume`
**Line 285:** `vm.count("restart") > 0` → `result.restart`
**Line 291:** `readProcessType(vm)` → `readProcessType(result)`
**Line 295:** `readOutputFormat(vm)` → `readOutputFormat(result)`
**Line 299:** `readMaxParallelJobs(vm)` → `readMaxParallelJobs(result)`
**Line 303:** `readOutputLayout(vm)` → `readOutputLayout(result)`
**Line 307:** `readForceNameConflictHandling(vm)` → `readForceNameConflictHandling(result)`
**Line 311:** `readPictureFolderSummary(vm)` → `readPictureFolderSummary(result)`
**Line 317:** `vm.count("compress") > 0` → `result.compress`
**Line 323:** `vm.count("image-quality")` → `result.imageQuality.has_value()`
**Line 324:** `vm.at("image-quality").as<int>()` → `result.imageQuality.value()`
**Line 335:** `vm.count("yes") > 0` → `result.yesToAll`
**Line 336:** `vm.count("recursive") > 0` → `result.recursive`
**Line 337:** `vm.count("pack") > 0` → `result.pack`
**Line 338:** `vm.count("pack-only") > 0` → `result.packOnly`
**Line 339:** `vm.count("verbose") > 0` → `result.verbose`
**Line 340:** `vm.count("verbose-echo") > 0` → `result.verboseEcho`
**Line 341:** `vm.count("full-progress") > 0` → `result.fullProgress`
**Line 343-346:** `vm.count("state-file")` → `result.stateFile.has_value()`, `getParamStr(vm, "state-file")` → `result.stateFile.value()`
**Line 348-353:** `vm.count("ffmpeg-path")` → `result.ffmpegPath.has_value()`, `getParamStr(vm, "ffmpeg-path")` → `result.ffmpegPath.value()`
**Line 356-357:** `vm.count("input") > 0` → `result.input.has_value()`, `vm.count("inputs") > 0` → `result.inputs.has_value()`
**Line 376:** `vm.at("inputs").as<std::vector<std::string>>()` → `result.inputs.value()`
**Line 392:** `getParamStr(vm, "input")` → `result.input.value()`
**Line 398:** `vm.count("output")` → `result.output.has_value()`
**Line 399:** `getParamStr(vm, "output")` → `result.output.value()`

**Remove `#include "utils/utils.h"`** if no other utils functions are used (verify: only `getParamStr` was used from utils.h). The `namespace fs = std::filesystem` and `using pathroots::*` declarations remain.

**Remove `getParamStr` from the codebase entirely** — it is no longer called. This is handled in Plan 3 (utils.cpp/h cleanup) but if the executor needs a clean compile, the function simply won't be called.

All config validation error messages remain unchanged. The `return eh::makeError(...)` calls with strings like `"--resume and --restart cannot be used together."`, `"Invalid process type: ..."`, `"--compress is only supported when --type is picture."`, `"--image-quality must be between 2 and 31."`, `"--image-quality requires --compress to be enabled."`, `"Use either -i/--input or -I/--inputs, not both."`, `"Input path is required."`, etc. are preserved verbatim.

The `eh::Result<appctx::AppConfig>` return type is unchanged and `AppConfig` struct population is identical.
</action>
  <acceptance_criteria>
1. `grep -c "boost/program_options" src/cmd/config_builder.cpp` returns 0
2. `grep -c "vm\.count\|vm\.at\|getParamStr" src/cmd/config_builder.cpp` returns 0
3. `grep -c "result\." src/cmd/config_builder.cpp` returns at least 30 (all vm references replaced)
4. `grep -c "CmdParseResult const&" src/cmd/config_builder.cpp` returns at least 7 (each helper + buildConfig)
5. `grep -c "Invalid process type" src/cmd/config_builder.cpp` returns 1 (error message unchanged)
6. `grep -c "--flat and --keep cannot be used together" src/cmd/config_builder.cpp` returns 1
7. `grep -c "--resume and --restart cannot be used together" src/cmd/config_builder.cpp` returns 1
8. `grep -c "--image-quality must be between 2 and 31" src/cmd/config_builder.cpp` returns 1
9. `grep -c "#include \"utils/utils.h\"" src/cmd/config_builder.cpp` returns 0 or 1 — only if other utils functions used. If 1, utils.h must not cause boost::po dependency
10. File is 380-420 lines total
</acceptance_criteria>
  <verify>
    <automated>rg --no-heading -c "boost/program_options\|vm\.count\|vm\.at\|getParamStr" src/cmd/config_builder.cpp</automated>
  </verify>
  <done>config_builder.cpp compiles with zero boost::po references. All 29 vm access points replaced by CmdParseResult field reads. All 6 helper functions accept CmdParseResult const&. All error messages identical to pre-migration. buildConfig(result) produces same AppConfig as buildConfig(vm) did.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CmdParseResult → buildConfig() | Parsed values cross from CLI layer into config layer |
| buildConfig() → AppConfig | Validated values cross into application config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Tampering | config_builder validation | mitigate | All input values validated against allowed ranges and type constraints before populating AppConfig — bounds checks, format validation, mutual exclusion rules preserved verbatim from pre-migration code |
| T-19-07 | Information Disclosure | error messages | accept | Error messages are user-facing CLI output — no sensitive data exposed |
</threat_model>

<verification>
After Plan 1 is complete, build with this plan's changes:

```bash
xmake build encro
```

Must compile successfully. No linker errors from missing boost::program_options symbols in config_builder.
</verification>

<success_criteria>
1. `xmake build encro` compiles with config_builder using CmdParseResult
2. `grep -r "boost/program_options\|variables_map" src/cmd/config_builder.*` returns no results
3. All 6 internal helper functions accept `CmdParseResult const& result`
4. All 30+ vm access points replaced by `result.xxx` field reads
5. All config validation error strings are byte-identical to pre-migration
</success_criteria>

<output>
After completion, create `.planning/phases/19-cli11-migration/19-02-SUMMARY.md`
</output>
