---
phase: 7
plan: 7-1
feature_id: STRUCT-02
title: Split video_batch_execution.cpp
status: completed
verdict: executed
dependencies: []
wave: 1
files_modified:
  - src/video/video_batch_execution.h
  - src/video/video_batch_execution.cpp
  - src/video/video_encoding_state.cpp
assertions_baseline: 909
test_cases_baseline: 215
---

# Plan 7-1: STRUCT-02 — Split video_batch_execution.cpp

## Description

Split `src/video/video_batch_execution.cpp` (804 lines) into 2 compilation units:
1. **`video_encoding_state.cpp`** (NEW) — state management, progress I/O, monitor thread
2. **`video_batch_execution.cpp`** (MODIFIED) — task execution, entry points, no-progress path

Add `videobatch::detail` namespace to `video_batch_execution.h` with struct definitions for `EncodingProgressState` and `EncodingExecutionContext` so both `.cpp` files compile independently.

## Task Breakdown

### Task 01: Add `videobatch::detail` struct definitions to header
- Add `EncodingProgressState` struct (96 lines) with methods:
  - `makeInitialSnapshot`, `createOverallBar`, `makeSlotBars`
- Add `EncodingExecutionContext` struct (146 lines) with methods:
  - `counters()`, `slots()`, `progress()`, `loadShared()`, `pendingTotal()`, `overallTotal()`, `finished()`, `markFinished()`, `barIndex()`, `barIndexOpt()`, `setActive()`, `clearActive()`, `activeStates()`, `barEncodingStart()`, `barEncodingStatus()`, `barIdle()`, `updateOverall()`, `finalizeState()`
- Declare `startEncodingMonitor(EncodingExecutionContext&) -> std::jthread`

### Task 02: Create video_encoding_state.cpp (NEW)
- Extract from `video_batch_execution.cpp` (now in anonymous namespace):
  - `noteStopRequest` (8 lines)
  - `truncateForProgressLabel` (3 lines)
  - `getStateLabel` (4 lines)
  - `tryReadProgressData` (3 lines)
  - `getEncodingProgress` (30 lines)
  - `monitorEncodingProgress` (108 lines)
- Implement `videobatch::detail::startEncodingMonitor` (3 lines)
- Total: ~191 lines

### Task 03: Strip extracted code from video_batch_execution.cpp
- Remove all state management helpers moved to `video_encoding_state.cpp`
- Keep (in anonymous namespace): `noteStopRequest` (duplicated, needed by `runEncodingTask`), `markRunningNoProgress`, `finalizeEncodeResult`, `truncateForProgressLabel` (duplicated, needed by `makeSlotLabel`), `makeSlotLabel`, `reportEncodingStatus`, `createEncodingState`, `runEncodingTask`, `runEncodingWithoutProgress`
- Keep (public): `videobatch::runEncodingTasks`
- Total: ~403 lines (was 804)

## Verification Gates

1. `xmake build` succeeds (both `.cpp` files compile independently)
2. All 909 assertions pass across 215 test cases
3. 4 E2E flows produce identical binary output

## Safety

- No public API broken — `videobatch::runEncodingTasks` signature unchanged
- New `.cpp` file uses `videobatch::detail` namespace from header for struct access
- Anonymous namespace helpers duplicated where needed (e.g., `noteStopRequest`, `truncateForProgressLabel`) — standard C++ pattern for TU-local helpers
- `startEncodingMonitor` moved from free function in anonymous namespace to `videobatch::detail::startEncodingMonitor`
- `monitorEncodingProgress` stays in anonymous namespace (called by `startEncodingMonitor` via lambda)

## Expected Result

- Build: PASS
- Tests: 909 assertions, 215 test cases — all PASS
- Zero behavioral change
