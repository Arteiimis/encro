---
phase: "10-dependency-injection-testability"
plan: "05"
type: "execute"
wave: 5
depends_on: "10-04"
files_modified:
  - "xmake.lua"
autonomous: true
requirements:
  - "DI-06"
---

<objective>
Verify the complete Phase 10 delivery: build compiles with all new and modified files, all 909 existing assertions pass, new MockPacker test assertions pass, zero regressions. Register the new test file in xmake build configuration.
Purpose: Final quality gate — ensures all deliverables work together.
Output: Build passes, test suite passes with 909+ assertions, ready for Phase 11.
</objective>

<tasks>

<task id="05-01" type="execute">
<description>Register new test file in xmake build configuration</description>
<files>xmake.lua</files>

<read_first>
- xmake.lua (build configuration — find where existing test files are registered)
- tests/pack_service_mock_tests.cpp (new test file from plan 04)
</read_first>

<action>
Find the test sources configuration in `xmake.lua`. Look for patterns like:
- `add_files("tests/pack_service_tests.cpp")` or
- `add_files("tests/*.cpp")` (glob pattern — if glob, no change needed)

If tests are added individually: Add `tests/pack_service_mock_tests.cpp` alongside existing test source listings.

If tests use a glob pattern like `tests/*.cpp` or `tests/**/*.cpp`: The new file is automatically picked up. No change needed.

Verify by grepping `xmake.lua` for test file registration patterns.
</action>

<acceptance_criteria>
- [ ] `tests/pack_service_mock_tests.cpp` is compiled by xmake (if individual registration needed, add it)
- [ ] `xmake build` compiles the new test file
- [ ] New test file appears in build output (if verbose)
</acceptance_criteria>
</task>

<task id="05-02" type="execute">
<description>Build and run all tests — verify 909+ assertions pass</description>
<files>(all files in scope)</files>

<read_first>
- .planning/phases/10-dependency-injection-testability/CONTEXT.md (success criteria)
</read_first>

<action>
Run the full build and test suite:

```bash
xmake build
xmake run tests
```

Verify output:
1. Build: zero errors, zero warnings (or warnings ≤ baseline)
2. Tests: All existing 215 test cases pass (909 assertions from pre-Phase 10 baseline)
3. Tests: New MockPacker test cases pass (~10 test cases, ~30-40 assertions)
4. Total: 225+ test cases, 939+ assertions

If any test fails:
- Check regression: is the failure in existing tests? → Fix plan 02/03 implementation
- Check mock tests: is the failure in new tests? → Verify MockPacker capture logic
- Check compile: does any file not compile? → Fix include paths or type mismatches

Only proceed when ALL assertions pass.
</action>

<acceptance_criteria>
- [ ] `xmake build` exits 0 with no compile errors
- [ ] `xmake run tests` exits 0 with all tests passing
- [ ] Output shows: "All tests passed" or equivalent Catch2 summary with 0 failures
- [ ] Total assertions ≥ 909 (existing) + new mock assertions
- [ ] No test case failures, no unexpected exceptions
</acceptance_criteria>
</task>

<task id="05-03" type="execute">
<description>Structural verification — confirm all deliverables exist</description>
<files>src/pack/ipacker.h, tests/packer_mock.h, src/pack/packer.h, src/pack/packer.cpp, src/pack/pack_service.h, src/pack/pack_service.cpp, src/pack/pack_facade.h, tests/pack_service_mock_tests.cpp</files>

<read_first>
- CONTEXT.md (deliverables list)
</read_first>

<action>
Verify each deliverable exists and meets spec:

| # | Deliverable | Status Check |
|---|-------------|-------------|
| 1 | `src/pack/ipacker.h` | Exists, contains `class IPacker` with 3 pure virtual methods |
| 2 | `tests/packer_mock.h` | Exists, contains `class MockPacker final : public IPacker` |
| 3 | `Packer : public IPacker` | `packer.h` line contains `class Packer final : public IPacker` |
| 4 | ZipWriter RAII | `packer.cpp` contains `struct ZipWriter` in anonymous namespace |
| 5 | PackService DI | `pack_service.h` has `explicit PackService(std::unique_ptr<IPacker>)` |
| 6 | Facade updated | `pack_facade.h` uses `std::make_unique<pack::Packer>()` (no `Packer&` constructor calls) |
| 7 | Mock tests | `tests/pack_service_mock_tests.cpp` exists with MockPacker-based tests |
| 8 | Tests pass | 909+ assertions pass |

Run verification commands:
```bash
grep "class IPacker" src/pack/ipacker.h
grep "class MockPacker final : public IPacker" tests/packer_mock.h
grep "class Packer final : public IPacker" src/pack/packer.h
grep "struct ZipWriter" src/pack/packer.cpp
grep "std::unique_ptr<IPacker>" src/pack/pack_service.h
grep "make_unique<pack::Packer>" src/pack/pack_facade.h
grep "packer_\." src/pack/pack_service.cpp  # should return ZERO
grep "Packer& packer_" src/pack/pack_service.h  # should return ZERO
```
</action>

<acceptance_criteria>
- [ ] All 8 deliverables confirmed by grep verification
- [ ] `grep "class IPacker" src/pack/ipacker.h` returns match
- [ ] `grep "class MockPacker final : public IPacker" tests/packer_mock.h` returns match
- [ ] `grep "class Packer final : public IPacker" src/pack/packer.h` returns match
- [ ] `grep "struct ZipWriter" src/pack/packer.cpp` returns match
- [ ] `grep "std::unique_ptr<IPacker>" src/pack/pack_service.h` returns match
- [ ] `grep "make_unique<pack::Packer>" src/pack/pack_facade.h` returns ≥4 matches
- [ ] `grep "packer_\." src/pack/pack_service.cpp` returns ZERO matches (all migrated to `packer_->`)
- [ ] `grep "Packer&" src/pack/pack_service.h` returns ZERO matches
</acceptance_criteria>
</task>

</tasks>

<verification>
- `xmake build && xmake run tests` — all compile + all tests pass
- Structural grep confirms all deliverables
- Manual review: existing test cases in `pack_service_tests.cpp` and `packer_tests.cpp` unchanged
</verification>

<success_criteria>
- Build: zero errors, zero warnings
- Tests: 909+ assertions pass (all existing + new mock assertions)
- All 8 deliverables present and verified by grep
- Ready for Phase 11: Consumer Migration & Cleanup
</success_criteria>
