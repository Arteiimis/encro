---
phase: "10-dependency-injection-testability"
plan: "03"
type: "execute"
wave: 3
depends_on: "10-02"
files_modified:
  - "src/pack/pack_service.h"
  - "src/pack/pack_service.cpp"
  - "src/pack/pack_facade.h"
  - "tests/pack_service_tests.cpp"
autonomous: true
requirements:
  - "DI-03"
---

<objective>
Migrate PackService from `Packer&` reference member to `std::unique_ptr<IPacker>` constructor-injected dependency. Update all internal call sites from `packer_.` to `packer_->`. Update pack_facade.h static instances to use `std::make_unique<pack::Packer>()`. Update existing test setup in pack_service_tests.cpp.
Purpose: Complete the DI boundary — PackService depends on IPacker abstraction, not concrete Packer.
Output: All files compile, existing 909 assertions pass, facade consumers unaffected.
</objective>

<tasks>

<task id="03-01" type="execute">
<description>Update pack_service.h — constructor takes unique_ptr<IPacker></description>
<files>src/pack/pack_service.h</files>

<read_first>
- src/pack/pack_service.h (current class definition, lines 21-75)
- src/pack/ipacker.h (IPacker interface)
</read_first>

<action>
Modify `src/pack/pack_service.h`:

1. Add `#include <memory>` after existing includes (required for `unique_ptr`)

2. Change forward declaration (line 19):
   - Remove: `class Packer;  // forward declaration`
   - Add: `class IPacker;  // forward declaration`

3. Change constructor (line 23):
   From: `explicit PackService(Packer& packer);`
   To:   `explicit PackService(std::unique_ptr<IPacker> packer);`

4. Change private member (line 62):
   From: `Packer& packer_;`
   To:   `std::unique_ptr<IPacker> packer_;`

5. Remove `#include "pack/packer.h"` from any transitive includes if present — (check: pack_service.h does NOT currently include packer.h directly, it uses forward declaration. Good.)

Full changes:
- Add `#include <memory>` after `#include "pack/pack_types.h"` (line 5)
- Replace line 19 `class Packer; // forward declaration` with `class IPacker; // forward declaration`
- Replace line 23 constructor signature
- Replace line 62 member declaration
</action>

<acceptance_criteria>
- [ ] `pack_service.h` contains `#include <memory>`
- [ ] Forward declaration: `class IPacker;` (not `class Packer;`)
- [ ] Constructor: `explicit PackService(std::unique_ptr<IPacker> packer);`
- [ ] Member: `std::unique_ptr<IPacker> packer_;`
- [ ] No remaining reference to `Packer` class in pack_service.h
- [ ] `grep "Packer" src/pack/pack_service.h` returns no matches
</acceptance_criteria>
</task>

<task id="03-02" type="execute">
<description>Update pack_service.cpp — packer_ calls and constructor</description>
<files>src/pack/pack_service.cpp</files>

<read_first>
- src/pack/pack_service.cpp (lines 25, 198-231, 317-318, 509-516, 530-539 — all packer_ call sites)
- src/pack/pack_service.h (updated after task 03-01)
</read_first>

<action>
Modify `src/pack/pack_service.cpp`:

1. Line 25: Constructor initialization changes from:
```cpp
PackService::PackService(Packer& packer): packer_(packer) { }
```
to:
```cpp
PackService::PackService(std::unique_ptr<IPacker> packer)
    : packer_(std::move(packer)) { }
```

2. All member access `packer_.` → `packer_->`:
   - Line 198: `auto const packRes = packer_->packFilesToZip(`
   - Line 317: `auto const packRes = packer_->packFilesToZip(`
   - Line 509: `auto const planRes = packer_->buildDirectoryPackPlan(`
   - Line 530: `auto const planRes = packer_->buildDirectoryPackPlan(`

3. Keep `#include "pack/packer.h"` — it's still needed for `PackService::buildGroupOrdinalRanges` which uses `PackFileEntry`. Actually — `pack_service.cpp` already includes `pack/packer.h` (line 8). Keep this include since other code may transitively need it.

4. Ensure no remaining `packer_.` references — all should be `packer_->`.
</action>

<acceptance_criteria>
- [ ] Constructor: `PackService::PackService(std::unique_ptr<IPacker> packer) : packer_(std::move(packer)) { }`
- [ ] All `packer_.` → `packer_->` (4 call sites confirmed by grep)
- [ ] `grep "packer_\." src/pack/pack_service.cpp` returns zero matches
- [ ] `grep "packer_->" src/pack/pack_service.cpp` returns 4 matches (lines 198, 317, 509, 530)
- [ ] File compiles — `xmake build` succeeds
</acceptance_criteria>
</task>

<task id="03-03" type="execute">
<description>Update pack_facade.h — static instances use make_unique</description>
<files>src/pack/pack_facade.h</files>

<read_first>
- src/pack/pack_facade.h (full file, 260 lines — every static instance that creates PackService)
- src/pack/pack_service.h (new constructor signature, task 03-01)
</read_first>

<action>
Modify `src/pack/pack_facade.h`:

Add `#include <memory>` after existing includes (required for `std::make_unique`).

The facade has 12 `[[deprecated]]` wrapper functions. Each one with a `static pack::Packer packer;` local must be updated. There are two patterns:

**Pattern A** — functions that use both Packer and PackService (5 functions):
```cpp
// Before:
static pack::Packer packer;
static pack::PackService service(packer);

// After:
#include <memory>
static pack::PackService service(std::make_unique<pack::Packer>());
```

Functions to update (Pattern A):
- `runPackPlan` (lines 67-71)
- `packGroups` (lines 75-79)
- `packAllFilesInDirectory` (lines 84-101)
- `runDirectoryPackWorkflow` (lines 105-110)
- `packFilesToZip` — first overload (lines 115-130)

**Pattern B** — functions that call Packer methods directly via the static packer (7 functions):
```cpp
// Before:
static pack::Packer packer;
static pack::PackService service(packer);
return packer.groupFilesBySize(...);

// After:
static pack::Packer packer;
return packer.groupFilesBySize(...);
```
Note: `service` is not used, only `packer` — remove the unused `service` line.

Functions to update (Pattern B):
- `packFilesToZip` — second overload (lines 134-143)
- `packFilesToZip` — third overload (lines 146-155)
- `groupFilesBySize` (lines 159-167)
- `groupPackFiles` (lines 170-184)
- `groupPackFilesWithSubparts` (lines 187-201)
- `groupPackEntries` (lines 204-218)
- `groupPackEntriesWithSubparts` (lines 221-235)
- `buildDirectoryPackPlan` (lines 238-258)

For Pattern B: The `static pack::PackService service(packer);` line must be removed OR updated to Pattern A format. Since Packer methods (grouping) are called directly on `packer`, the `service` object is unused. **Remove the unused service line.**

Wait — actually, does packing build pass if we have an unused static local? In C++, yes — unused variables may generate warnings but not errors. However, with the constructor change, `static pack::PackService service(packer);` won't compile because `PackService` now takes `unique_ptr<IPacker>`, not `Packer&`. So we MUST update or remove these lines.

**Decision for Pattern B:** Remove the `service` line entirely. Replace both lines:
```cpp
static pack::Packer packer;
static pack::PackService service(packer);
```
with just:
```cpp
static pack::Packer packer;
```
Since `packer` is used, `service` is not.

Let me recount. Actually let me look at each function carefully:

1. Lines 115-130: Uses `packer.` directly AND the service is not used in this function. Wait — it has `static pack::Packer packer; static pack::PackService service(packer);` but returns `packer.packFilesToZip(...)`. So service is unused. → Pattern B (remove service line)

2. Lines 134-143: Same pattern → Pattern B

3. Lines 146-155: Same → Pattern B

4. Lines 159-167: Uses `packer.` only → Pattern B

And so on for all grouping functions and the buildDirectoryPackPlan wrapper.

Actually, only `runPackPlan`, `packGroups`, `packAllFilesInDirectory`, and `runDirectoryPackWorkflow` use `service.`. The rest use `packer.` directly.

Wait — let me re-examine:
- Lines 67-71 `runPackPlan`: uses `service.runPackPlan(ctx, plan)` → Pattern A (needs service with new constructor)
- Lines 75-79 `packGroups`: uses `service.packGroups(plan)` → Pattern A
- Lines 84-101 `packAllFilesInDirectory`: uses `service.packAllFilesInDirectory(...)` → Pattern A
- Lines 105-110 `runDirectoryPackWorkflow`: uses `service.runDirectoryPackWorkflow(ctx, dirPath)` → Pattern A

For Pattern A: 
```cpp
[[deprecated("Use PackService::runPackPlan")]]
inline auto runPackPlan(appctx::AppContext& ctx, pack::PackPlan const& plan)
  -> eh::Result<pack::PackRunResult> {
  static pack::PackService service(std::make_unique<pack::Packer>());
  return service.runPackPlan(ctx, plan);
}
```

All other facade functions (Pattern B) — remove the service line entirely, keep only packer.

**But wait — there's a complication.** If we have `static pack::PackService service(std::make_unique<pack::Packer>());` in multiple functions, each function gets its own static instance. These are fine — independent packer instances with no shared state needed.

And for Pattern B functions where service is unused: the `static pack::PackService service(packer);` line would fail to compile because `PackService(Packer&)` no longer exists. So we either:
1. Replace with `std::make_unique<pack::Packer>()` pattern (service unused but valid)
2. Remove the line entirely

Option 2 is cleaner. But wait — if we remove the `#include "pack/pack_service.h"` from facade.h, will the facade still compile? The facade needs `pack/pack_service.h` for Pattern A functions that call `service.`. So keep the include but remove unused static locals.

**Actually, looking more carefully at the facade:**

For the `buildDirectoryPackPlan` facade (lines 238-258):
```cpp
static pack::Packer packer;
static pack::PackService service(packer);
return packer.buildDirectoryPackPlan(...);
```
This calls `packer.` directly, not `service.`. So remove the `service` line.

But wait — `buildDirectoryPackPlan` is a method of `Packer`, and `Packer` now inherits from `IPacker`. The facade calls `packer.buildDirectoryPackPlan(...)` directly — this is fine since `packer` is still `Packer` type (not `IPacker`), so it has access to all Packer methods.

OK, I'll write the comprehensive action for the facade update.
</action>

<acceptance_criteria>
- [ ] `pack_facade.h` includes `#include <memory>` (after line 9)
- [ ] Functions using `service.` (runPackPlan, packGroups, packAllFilesInDirectory, runDirectoryPackWorkflow): replaced with `static pack::PackService service(std::make_unique<pack::Packer>());`
- [ ] Functions using only `packer.`: `static pack::PackService service(packer);` line removed, only `static pack::Packer packer;` remains
- [ ] No remaining `PackService(packer)` constructor calls (Packer& constructor no longer exists)
- [ ] `grep "static pack::PackService service(packer)" src/pack/pack_facade.h` returns zero matches
- [ ] `grep "make_unique<pack::Packer>" src/pack/pack_facade.h` returns 4 matches (one per Pattern A function)
- [ ] File compiles
</acceptance_criteria>
</task>

<task id="03-04" type="execute">
<description>Update pack_service_tests.cpp — unique_ptr test setup</description>
<files>tests/pack_service_tests.cpp</files>

<read_first>
- tests/pack_service_tests.cpp (lines 19-20 — test setup; all test cases that use testService)
- src/pack/pack_service.h (new constructor signature, task 03-01)
</read_first>

<action>
Modify `tests/pack_service_tests.cpp`:

Lines 19-20, change from:
```cpp
pack::Packer testPacker;
pack::PackService testService(testPacker);
```
to:
```cpp
auto testPacker = std::make_unique<pack::Packer>();
pack::PackService testService(std::move(testPacker));
```

Add `#include <memory>` at the top of the file (after existing includes, before Catch2 include).

All test cases use `testService` by reference (already defined in anonymous namespace). No test case directly references `testPacker` after line 20. Verify with grep: `grep "testPacker" tests/pack_service_tests.cpp` — should only appear in the setup line.
</action>

<acceptance_criteria>
- [ ] `tests/pack_service_tests.cpp` includes `#include <memory>`
- [ ] Test setup: `auto testPacker = std::make_unique<pack::Packer>();`
- [ ] Test setup: `pack::PackService testService(std::move(testPacker));`
- [ ] `grep "pack::Packer testPacker" tests/pack_service_tests.cpp` returns zero matches
- [ ] `grep "std::make_unique<pack::Packer>" tests/pack_service_tests.cpp` returns 1 match
- [ ] File compiles
</acceptance_criteria>
</task>

</tasks>

<verification>
- `xmake build` — all modified files compile
- `xmake run tests` — all 909 existing assertions pass (existing integration tests preserved)
- `grep "packer_\." src/pack/pack_service.cpp` — returns zero (all changed to `packer_->`)
- `grep "Packer&" src/pack/pack_service.h` — returns zero
- `grep "PackService(packer)" src/pack/pack_facade.h` — returns zero
</verification>

<success_criteria>
- PackService constructor takes `std::unique_ptr<IPacker>` — DI injection complete
- All internal `packer_.` → `packer_->` (4 call sites)
- Facade updated — static instances use `make_unique<Packer>()`
- Existing tests updated — use `make_unique` for test setup
- 909 assertions pass — zero regressions
- Build passes with no warnings
</success_criteria>
