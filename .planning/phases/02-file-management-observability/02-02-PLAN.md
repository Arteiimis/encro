---
phase: 02-file-management-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logging/logging.h
  - tests/logging_scoped_timer_test.cpp
autonomous: true
requirements:
  - OBS-03

must_haves:
  truths:
    - "ScopedTimer(\"stage\") logs '[stage] begin' on construction at LOG_INFO level"
    - "ScopedTimer destructor logs '[stage] completed in Xms' at LOG_INFO level"
    - "Moving a ScopedTimer transfers timing ownership — source becomes no-op on destruction"
    - "Nested ScopedTimer instances produce correctly indented begin/complete pairs in logs"
    - "ScopedTimer destructor is noexcept and always completes even during exception unwinding"
  artifacts:
    - path: "src/logging/logging.h"
      provides: "ScopedTimer class alongside existing LOG_* macros and DEFINE_LOGGER"
      min_lines: 150
      contains: "class ScopedTimer"
    - path: "tests/logging_scoped_timer_test.cpp"
      provides: "TDD test suite for ScopedTimer construction, destruction, move semantics, nesting, noexcept safety"
      min_lines: 150
  key_links:
    - from: "ScopedTimer constructor"
      to: "LOG_INFO macro"
      via: "fmt::format into LOG_INFO at construction time"
      pattern: "LOG_INFO.*begin"
    - from: "ScopedTimer destructor"
      to: "LOG_INFO macro"
      via: "steady_clock::now() - start_ for elapsed, formatted into LOG_INFO"
      pattern: "LOG_INFO.*completed in.*ms"
    - from: "ScopedTimer move constructor"
      to: "movedFrom_ flag"
      via: "transfer start_ and stageName_, set source.movedFrom_ = true"
      pattern: "movedFrom_"
---

<objective>
Add a RAII ScopedTimer class to src/logging/logging.h. Every scope-level timing measurement becomes a single-line declaration: `ScopedTimer timer("stage_name")`. The constructor logs entry, the destructor logs elapsed duration. Move-only semantics prevent double-logging. Nesting is naturally supported — each instance holds its own start time.
Purpose: Fulfill OBS-03 — pipeline stages automatically record entry/exit and elapsed time, replacing manual `elapsedMs` anti-patterns in the codebase.
Output: ScopedTimer class in logging.h + comprehensive TDD test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-file-management-observability/02-CONTEXT.md
@.planning/research/PITFALLS.md

<read_first>
  <file path="src/logging/logging.h">Current state: contains DEFINE_LOGGER macro (line 44), LOG_* macros (lines 67-120), and logging::detail::shortFile() helper. ScopedTimer is not yet implemented. The file uses #pragma once. Includes spdlog/async.h, spdlog/spdlog.h, fmt/format.h.</file>
  <file path="tests/logging_infra_test.cpp">Reference for existing test patterns: Catch2 TEST_CASE, anonymous namespace helpers, CAPTURE for debug output, temp directory patterns.</file>
  <file path="src/logging/log_tags.h">Existing tag constants. TEST_INFRA tag is available for test logger registration.</file>
</read_first>

<interfaces>
<!-- The LOG_* macros that ScopedTimer will use internally -->

From src/logging/logging.h (existing, unchanged):
```cpp
#define LOG_INFO(...) \
    SPDLOG_LOGGER_CALL( \
        loggerPtr(), \
        spdlog::level::info, \
        "[{}:{}] {}", \
        logging::detail::shortFile(__FILE__), \
        __LINE__, \
        fmt::format(__VA_ARGS__))

#define DEFINE_LOGGER(tag) \
    static auto loggerPtr() noexcept -> spdlog::logger* { ... }
```

From src/logging/log_tags.h:
```cpp
namespace logtags {
    inline constexpr auto TEST_INFRA = "test.infra";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test for ScopedTimer behavior (RED)</name>
  <files>tests/logging_scoped_timer_test.cpp</files>
  <read_first>tests/logging_infra_test.cpp, src/logging/logging.h, src/logging/log_tags.h, .planning/phases/02-file-management-observability/02-CONTEXT.md</read_first>
  <behavior>
    - Test 1 "ScopedTimer logs begin on construction": Create a ScopedTimer("test_stage") with a capturing ostream logger. Assert the output stream contains "test_stage begin" (or equivalent entry message). Verify the log line appears BEFORE any completion message.
    - Test 2 "ScopedTimer logs elapsed time on destruction": Let the ScopedTimer go out of scope. Assert the output stream contains "test_stage completed in" followed by a duration in milliseconds. Verify the completion line appears AFTER the begin line.
    - Test 3 "ScopedTimer move transfers ownership": Move-construct a new ScopedTimer from an existing one. Let the original (now moved-from) go out of scope first — assert no completion message appears. Let the moved-to timer go out of scope — assert exactly one completion message appears.
    - Test 4 "ScopedTimer is not copyable": Use static_assert or a concept check to verify ScopedTimer is not copy-constructible or copy-assignable. This test case compiles or fails at compile time.
    - Test 5 "Nested ScopedTimer produces correct ordering": Create outer timer "outer", then inner timer "inner". Let inner go out of scope first, then outer. Assert output has "outer begin", "inner begin", "inner completed in", "outer completed in" in that order.
    - Test 6 "ScopedTimer destructor is noexcept": static_assert that std::is_nothrow_destructible_v<ScopedTimer> is true.
  </behavior>
  <action>
    Create tests/logging_scoped_timer_test.cpp. Use the same pattern as tests/logging_infra_test.cpp: include "logging/log_tags.h", "logging/logging.h", `<catch2/catch_all.hpp>`, anonymous namespace helpers.

    Create a helper function `registerCapturingLoggerForTimer(char const* name)` that returns a pair of `{logger, std::ostringstream*}` — identical pattern to the one in logging_infra_test.cpp. Use `TEST_INFRA` tag or a dedicated test tag.

    Since ScopedTimer internally uses LOG_INFO, the test logger needs to be registered with spdlog and picked up by `loggerPtr()` (the DEFINE_LOGGER macro). The DEFINE_LOGGER at file scope will call `loggerPtr()` which checks spdlog::get(tag). Register the test logger with the tag name that matches the DEFINE_LOGGER call in the test file.

    For Test 1: Create ScopedTimer, flush the logger, check oss->str(). Since LOG_INFO uses fmt::format, the begin/complete messages will be formatted strings. Check for substring presence.

    For Test 2: Wrap ScopedTimer in a block scope `{ ScopedTimer t(...); ... }`, after block ends, flush and check oss->str().

    For Test 3: Use std::optional<ScopedTimer> to demonstrate move semantics. Create ScopedTimer in optional, move-construct a second optional from it, then clear them in controlled order.

    For Test 4: static_assert(!std::is_copy_constructible_v<ScopedTimer>) and static_assert(!std::is_copy_assignable_v<ScopedTimer>).

    For Test 5: Nested blocks.

    For Test 6: static_assert(std::is_nothrow_destructible_v<ScopedTimer>).

    After all tests: ensure spdlog registry is cleaned up (drop test loggers).
  </action>
  <verify>
    <automated>xmake build tests && xmake run tests "[logging][scoped_timer]" 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Tests compile but FAIL because ScopedTimer class does not exist yet (compilation error expected)
    - Alternatively, if class exists as stub, all 6 assertions fail at runtime
    - Test binary reports failures for all ScopedTimer-related TEST_CASEs
  </acceptance_criteria>
  <done>Test file committed with 6 FAILING test cases that define the complete ScopedTimer contract: entry/exit logging, move semantics, nesting, noexcept safety, non-copyable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ScopedTimer class — make all tests pass (GREEN)</name>
  <files>src/logging/logging.h</files>
  <read_first>src/logging/logging.h, tests/logging_scoped_timer_test.cpp, .planning/phases/02-file-management-observability/02-CONTEXT.md</read_first>
  <behavior>
    - Behavior 1 (D-08): Constructor takes `std::string_view stageName`. Records `std::chrono::steady_clock::now()` as start_. Logs `"[stageName] begin"` via LOG_INFO. Uses `fmt::format` to construct the message string.
    - Behavior 2 (D-08): Destructor computes `elapsed = steady_clock::now() - start_`. Converts to milliseconds using `std::chrono::duration_cast<std::chrono::milliseconds>`. Logs `"[stageName] completed in {}ms"` via LOG_INFO. Destructor is `noexcept`.
    - Behavior 3 (D-10): Uses `std::chrono::steady_clock` exclusively for duration measurement. Never mixes with system_clock. Pitfall #4 prevention.
    - Behavior 4 (D-11): Destructor noexcept guarantees timing is recorded even during stack unwinding from exceptions.
    - Behavior 5 (D-12): Each ScopedTimer holds independent start_ timepoint. Nesting works naturally because C++ destruction order is reverse of construction — inner scope destructors run before outer scope destructors.
    - Behavior 6 (move-only, Claude's Discretion): Delete copy constructor and copy assignment. Implement move constructor and move assignment — transfer stageName_ and start_, set source.movedFrom_ = true. Destructor checks movedFrom_ flag — if true, skip logging (no double-logging from moved-from instances).
  </behavior>
  <action>
    Add to src/logging/logging.h, after the LOG_CRITICAL macro definition (after line 120) and before the final #endif:

    Add a comment section `// ── ScopedTimer (D-08~D-12: RAII stage timing) ──`.

    Define class ScopedTimer in namespace `logging` (not in anonymous namespace — it's a public API):
    - Private members: `std::string_view stageName_`, `std::chrono::steady_clock::time_point start_`, `bool movedFrom_{false}`.
    - Public constructor: `explicit ScopedTimer(std::string_view stageName)`. Bodies of constructor and destructor use LOG_INFO with format strings `"{} begin"` and `"{} completed in {}ms"` where the first arg is `stageName_` and the second (destructor only) is `elapsed.count()`.
    - Deleted copy constructor: `ScopedTimer(ScopedTimer const&) = delete`.
    - Deleted copy assignment: `auto operator=(ScopedTimer const&) -> ScopedTimer& = delete`.
    - Move constructor: `ScopedTimer(ScopedTimer&& other) noexcept`. Transfers stageName_, start_, sets other.movedFrom_ = true.
    - Move assignment: `auto operator=(ScopedTimer&& other) noexcept -> ScopedTimer&`. Check for self-assignment. Transfer and set other.movedFrom_.
    - Destructor: `~ScopedTimer() noexcept`. Check `if (movedFrom_) return;`. Compute elapsed_ms via `std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::steady_clock::now() - start_).count()`. Call LOG_INFO.

    Include `<chrono>` at the top of logging.h (if not already present).

    The constructor body:
    ```
    ScopedTimer(std::string_view stageName) : stageName_(stageName), start_(std::chrono::steady_clock::now()) {
        LOG_INFO("{} begin", stageName_);
    }
    ```

    The destructor body:
    ```
    ~ScopedTimer() noexcept {
        if (movedFrom_) return;
        auto const elapsed = std::chrono::duration_cast<std::chrono::milliseconds>(
            std::chrono::steady_clock::now() - start_).count();
        LOG_INFO("{} completed in {}ms", stageName_, elapsed);
    }
    ```

    Implementation is entirely inline in the header (class in header, member functions defined inline within the class body). This follows the established pattern of logging.h being header-only except for the shortFile helper.

    CONVENTIONS: east const, trailing return type on deleted/move operators, `noexcept` on destructor and move operations, `explicit` on single-argument constructor, `[[nodiscard]]` not needed (no return value from constructor).
  </action>
  <verify>
    <automated>xmake build tests && xmake run tests "[logging][scoped_timer]"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 Task 1 tests PASS: begin message on construction, elapsed message on destruction, move transfers ownership, non-copyable, nested ordering correct, noexcept destructor
    - `xmake build encro` compiles without errors (logging.h included in all source files via DEFINE_LOGGER)
    - No new warnings from clang-cl about the new class
  </acceptance_criteria>
  <done>ScopedTimer class is implemented in logging.h, all 6 TDD tests pass, ready for pipeline instrumentation.</done>
</task>

<task type="auto">
  <name>Task 3: Add timing display format test and edge case hardening</name>
  <files>tests/logging_scoped_timer_test.cpp, src/logging/logging.h</files>
  <read_first>src/logging/logging.h, tests/logging_scoped_timer_test.cpp</read_first>
  <behavior>
    - Test 7 "ScopedTimer with empty stage name logs correctly": Create ScopedTimer("") — should still produce begin/complete messages (edge case, not crash or throw).
    - Test 8 "ScopedTimer move assignment works": Use move assignment operator (not just move constructor) and verify ownership transfer.
    - Test 9 "self-move-assignment is safe": Move-assign a ScopedTimer to itself (via std::move on same variable). Should not crash or produce duplicate output.
  </behavior>
  <action>
    Add 3 more TEST_CASEs to tests/logging_scoped_timer_test.cpp:
    - Test 7: ScopedTimer with empty string_view stage name. Verify begin and complete messages still appear without crashing.
    - Test 8: Create two ScopedTimer instances, move-assign one to the other. Verify the moved-from one produces no completion message, the moved-to one produces exactly one.
    - Test 9: Self-move-assignment. Create ScopedTimer t("self"); t = std::move(t); Let it destruct. Verify no crash, exactly one completion message.

    If any test reveals implementation issues, fix ScopedTimer in logging.h. The move assignment operator must handle self-assignment (check `this != &other` before transferring).
  </action>
  <verify>
    <automated>xmake build tests && xmake run tests "[logging][scoped_timer]"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 tests pass
    - Self-move-assignment is safe (no double-free, no double-logging)
    - Empty stage name does not crash or produce garbled output
  </acceptance_criteria>
  <done>ScopedTimer implementation hardened: edge cases (self-move, empty name, move assignment) all tested and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stageName string_view lifetime | Caller passes a string_view that must remain valid for the ScopedTimer's lifetime. All callers use string literals or constexpr strings — safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service | ScopedTimer logging via spdlog async queue | accept | ScopedTimer only produces 2 log lines per stage (begin + complete). 4-6 stages per pipeline run = 8-12 additional lines. Queue capacity (8192) is more than sufficient. |
| T-02-06 | Information Disclosure | Elapsed time in log output | accept | Timing data is performance telemetry, not sensitive. Included in log by design for debugging. |
| T-02-07 | Tampering | stageName passed from caller | accept | stageName is a string_view from the caller. If caller passes malicious input, it appears in the log — same as any log message. No privilege boundary crossed. |
| T-02-SC | Tampering | No new packages installed | accept | ScopedTimer uses only C++ standard library (chrono) and existing project deps (spdlog, fmt). Zero new dependencies. |
</threat_model>

<verification>
- `xmake build tests && xmake run tests "[logging][scoped_timer]"` — all 9 tests pass
- `xmake build encro` — production binary compiles
</verification>

<success_criteria>
- [x] ScopedTimer("stage") logs begin message on construction at LOG_INFO level (OBS-03)
- [x] ScopedTimer destructor logs completed-in-Xms message at LOG_INFO level (OBS-03)
- [x] Move semantics prevent double-logging from moved-from instances
- [x] Nested ScopedTimer instances produce correctly ordered begin/complete pairs
- [x] Destructor is noexcept, survives exception unwinding without data loss
- [x] steady_clock used exclusively for duration measurement (Pitfall #4 prevention)
</success_criteria>

<output>
Create .planning/phases/02-file-management-observability/02-02-SUMMARY.md when done
</output>
