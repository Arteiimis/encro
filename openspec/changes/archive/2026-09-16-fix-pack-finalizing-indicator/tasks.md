## 1. Test-enabling plumbing (behaviour-neutral)

- [x] 1.1 Promote the "bars can render" check to `ProgressContext::renderable()` (`src/core/progress.h/.cpp`): true only when the context holds at least one bar, stdout is a terminal, and output is not quiet; no behaviour change for existing callers. Verify `xmake test-report --tag="[progress]"` stays green.
- [x] 1.2 Add the optional `PackPlan::onBeforeArchiveClose` hook (`src/pack/pack_plan_internal.h`, null by default), thread it through `Packer::packFilesToZip` (compact overload) and invoke it once per archive after the finalizing counter increment and before `zip.close()`; forward the field in `pack::internal::selectPackPlanIndexes` alongside the other plan fields. Verify `xmake test-report --tag="[pack-service]"` and `--tag="[packer]"` stay green (unset hook is a no-op).

## 2. Red regression tests (fail on current code)

- [x] 2.1 `[pack-service]` case "compact postfix keeps the packing label while an archive finalizes": two groups — a gated group of a few files whose `onBeforeArchiveClose` signals entry and blocks on a test-owned latch, and a second group with a few hundred entries so it is still packing when the gated archive enters finalization (the gated group finishes its entries in microseconds, the second group needs milliseconds); record status texts under a mutex, since the callback runs on a packing thread while the test thread polls. Release the latch from that same polling loop once the predicate holds or the `waitUntil` deadline expires — a window that never produces the expected text must fail the assertions rather than hang the run (`testutils::waitUntil` polling, no `sleep_for`). Assert that at least one text carries a ` | Finalizing` part, that at least one text published in the window shows a file counter above its value at window entry (the reproduction the flip needs), that every text recorded before the completion text contains the packing label and none starts with `Finalizing`, and that the run ends with `Packed: archive 2/2 complete`. Verify it fails on current code (bare `Finalizing <frame>` texts replace the label, and no counter advances are published inside the window).
- [x] 2.2 `[pack-service]` case "compact finalizing frame steps on a fixed cadence": same two-group gated setup; hold the window until a `steady_clock` deadline about 500 ms out (polled with `testutils::waitUntil`, no `sleep_for`), then release. From the recorded texts filter those containing `| Finalizing` and take the sequence of their frame characters, counting changes: assert at most `measuredWindowMs / 120 + 1` changes (5 for a 500 ms window), the second group's continuing packing updates not raising that count, and at least two distinct frames shown. Verify it fails on current code (the frame changes on every repaint).
- [x] 2.3 Destination rule: in `tests/infra/progress_tests.cpp`, assert inside a `testutils::StdoutCapture` scope (mirroring the existing non-terminal test there) that a context holding a bar reports `renderable() == false`; in the 2.1/2.2 setup additionally assert that indicator texts were delivered although bars are not renderable — a status-text consumer alone must keep the indicator alive. Verify: the `[progress]` assertion cannot pass before 1.1 (the method does not exist), keying the start rule on bars alone would leave 2.1/2.2 with zero indicator texts, and a compact run with no status-text consumer records no indicator text at all (the destination-negative case).

## 3. Implementation

- [x] 3.1 Give the compact line a single composer: store the current packing label in the progress state, always publish on packing updates (delete the `finalizingCount == 0` suppression guard), and compose `label | Finalizing <frame>` while the counter is above zero. Verify 2.1 passes and the sequence test reworked in 3.4 still matches its expected order.
- [x] 3.2 Derive the frame from the steady clock instead of a per-repaint counter, so a publish cannot advance the frame beyond the 120 ms elapsed-time cadence. Verify 2.2's frame-change bound passes while the second group keeps publishing.
- [x] 3.3 Restore the unconditional 120 ms cadence in the spinner loop (wait every iteration, keeping the wake-on-stop wait) and start the spinner only when `ProgressContext::renderable()` or a status-text consumer is installed. Verify 2.2's frame-change bound and 2.3 pass.
- [x] 3.4 Update the existing `packGroups compact mode emits archive and file status text` case (`tests/pack_service_tests.cpp`): filter texts containing `| Finalizing` instead of those starting with `Finalizing `, refresh its comment (the indicator now appends to the label at a 120 ms frame cadence), keep the exact-sequence assertion, and assert the filtered texts carry the label with the archives-completed-so-far counter (`Packing: archive 0/1` for its single group). Verify it passes.

## 4. Verification

- [x] 4.1 `xmake fmt -k` reports clean.
- [x] 4.2 `xmake tidy` reports no new findings (requires an up-to-date `build/compile_commands.json`).
- [x] 4.3 `xmake test-report` full suite green, including the focused `--tag="[pack-service]"` and `--tag="[progress]"` runs. The e2e suite is deliberately not run: no process-boundary, fake-tool, or CLI-surface behaviour changes.
- [x] 4.4 Manual TTY check (bars only render on a terminal): `xmake run encro -z <input-dir> -o <out-dir>` on a tree that produces 3+ archives; confirm the line keeps `Packing: archive k/N [file n/m]` with a ` | Finalizing <frame>` tail that steps every 120 ms while archives finalize, and that it never flips to a bare `Finalizing`; repeat with `> out.txt` and with `--quiet` to confirm no bars and no spinning. Deviation: the non-TTY half is verified (`xmake run encro <dir> -z -o <out>` with piped output packs 200 files into one archive, prints the usual two lines, and emits no bar text); the interactive half is verified by the user on a real terminal (reported 2026-09-16).
