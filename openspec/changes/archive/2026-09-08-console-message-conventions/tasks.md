## 1. Terminal rendering core

- [x] 1.1 Add failing tests to the terminal output tests (`tests/infra/terminal_tests.cpp`, tag `[terminal]`) covering the kind contract: Error/Warning/Hint render `error: `/`warning: `/`hint: ` prefixes, Success/Info/Heading/Prompt render bare text, no bracketed badge appears, and each kind writes to its stream (stdout for Success/Info/Heading/Prompt, stderr for Error/Warning/Hint) with streams captured separately. Then implement the kind→(prefix, color, stream) table and dispatch in `src/infra/terminal.{h,cpp}` (design D1/D2) and verify with `xmake test-report --tag="[terminal]"`
- [x] 1.2 Extend the color-mode tests to pin prefix survival: with `--color never`, `NO_COLOR`, and non-TTY streams the `error:` prefix literal is still present in the rendered text; with `--color always` the prefix is wrapped in color codes exactly once with no duplicated `Error:` marker (design D2/D3). Verify with the same suite

## 2. Call-site sweep

- [x] 2.1 Add failing tests that a top-level failure (for example a nonexistent input) prints `error: Invalid arguments: ...` on stderr with no doubled `Error:` literal, that the log hint keeps its stderr stream and gains the `hint:` prefix, and that the help hint (`Run encro -h for help...`) moves to stderr (update the e2e assertion that captures it on stdout today). Then route `failWithHint`, its wrappers, and `printHelpHint` in `src/app/app_entry.cpp` through the Error/Hint kinds (design D1/D3) and verify with `xmake test-report --tag="[appentry]"` plus the e2e failure tests (covered by e2e `[cli]` failure-path assertions; the `[appentry]` unit tag keeps its existing pure-function coverage)
- [x] 2.2 Add failing tests for the in-pipeline diagnostics — scan failure, probe failure, and mid-run warnings appear on stderr; the run-summary block (count line, failed-file list, attention entries) stays on stdout as one unit; the duplicated `Error:` literals at the scan-failure and probe-failure sites are gone. Then sweep the `terminal::println(Error/Warning/...)` call sites in `src/video`, `src/picture`, `src/preview`, `src/organize` (organize's diagnostic sites included), `src/app/pipeline.cpp`, and the startup warnings in `src/logging/setup.cpp` to the kind-dispatched entry point (streams follow the kind; design D1/D3), updating text/stream assertions in the unit and e2e suites; verify with `xmake test-report` (full unit pass)
- [x] 2.3 Add failing tests that `config` and `completion` diagnostics render `error:` / `warning:` prefixes on stderr and their hint/success lines land on the right stream (badge→prefix per design D5). Then update `src/cmd/config_command.cpp` and `src/cmd/completion_install.cpp`, and verify with `xmake test-report --tag="[config]"` and `ENCRO_TEST_COMPLETION=1 xmake test-report --tag="[completion]"` (the install assertions skip without the env var) (config_command already used kind-dispatched stderr calls, so it inherits the prefix centrally; only completion_install's hint lines moved to stderr)
- [x] 2.4 Add a failing prompt-rendering test asserting the question line has no leading badge and ends with `(Y/n): ` on stdout; then drop the `[?]` badge from the prompt path in `src/utils/utils.cpp` (`readUserIpt`) and update prompt assertions; verify with the prompt/console tests (the badge drop is central: renderMessage gives Prompt no prefix, so readUserIpt needed no edit)

## 3. End-to-end verification

- [x] 3.1 Run `xmake build e2e_tests && xmake run e2e_tests` and fix remaining badge/stream assertion drift, then `xmake test-parallel` to confirm no regressions across unit + e2e suites
