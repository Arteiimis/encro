# Tasks: stabilize-parallel-tests

## 1. Shared test-sync foundations

- [x] 1.1 Add `testutils::waitUntil(predicate, timeout, pollInterval)` to `tests/test_utils.h` (deadline as hang guard, trailing predicate re-check) with coverage in `tests/test_utils_tests.cpp` (true when predicate holds before deadline without waiting it out; false after deadline; no silent pass). Verify: `xmake test-report --tag="[test-utils]"` passes.
- [x] 1.2 TDD the job-state clock hook: write failing tests in `tests/job_state_tests.cpp` that set a synthetic clock, mark running/interrupted around a pushed timestamp delta, and assert exact accumulated values (equality, not bounds); then implement the free-function hook (`setClockForTest`/reset with an atomic function pointer, mirroring `stop_signal`'s test-hook idiom) plus a scoped guard in `tests/test_utils.h`, keeping `system_clock` as the default clock. Verify: `xmake test-report --tag="[job-state]"` passes.
- [x] 1.3 Inject the source root into the tests target as a compile definition (`ENCRO_TEST_SOURCE_DIR`, same mechanism as `FAKE_TOOL_EXE_PATH`) so in-suite meta-checks can locate `tests/` sources from the built binary. Verify: build succeeds and a temporary probe test lists `tests/test_utils.h` under the injected path.

## 2. Fake tool: gate from call index

- [x] 2.1 TDD `ENCRO_FAKE_FFMPEG_GATE_FROM_CALL`: failing tests in `tests/fake_tool_tests.cpp` covering (a) gate-from-call=2 — first invocation completes normally, second blocks after logging its invocation, creating the gate file releases it; (b) from-call unset — every invocation gates. The "blocked while logged" proof needs an async harness (spawn the second invocation on a `std::jthread`) and `ENCRO_FAKE_TOOL_LOG_FILE` pointed at a temp path — set it explicitly; existing cases in this file never configure the invocation log. Verify: new cases fail for the right reason before implementation.
- [x] 2.2 Implement `ENCRO_FAKE_FFMPEG_GATE_FROM_CALL` in `tests/e2e/fake_media_tool.cpp`, honoring the design D3 ordering constraint: reorder `runFakeFfmpeg` to `appendInvocationLog` → `-version` early-return → `nextScheduledCallIndex` → `waitForGateFile(index)`, so the index resolves before the gate, `-version` probes neither gate nor consume an index, and gated invocations consume an index (no existing test combines count file + gate). 30 s fail-safe and whole-run gate behavior preserved. Verify: `xmake test-report --tag="[fake-tool]"` passes.
- [x] 2.3 Add a regression case in `tests/fake_tool_tests.cpp` proving `-version` output returns immediately under a configured gate and does not shift subsequent invocation indices. Verify: covered by the `[fake-tool]` run above.

## 3. Convert stop-window tests (poll-proof, stop, release)

- [x] 3.1 `tests/app/pipeline_picture_tests.cpp` — all 6 cancel-mid-batch cases: set `ENCRO_FAKE_TOOL_LOG_FILE` (these tests configure no invocation log today — needed for the poll proof), replace the 1200 ms sleep with gate-from-call=2 + poll the invocation log until the second invocation is recorded, then `requestStop()`, then create the gate file; drop the 7000 ms `CALL_PLAN` delays. Verify: `xmake test-report --tag="[pipeline]"` passes and the cancel cases no longer depend on any fixed delay.
- [x] 3.2 `tests/video/video_encode_runner_tests.cpp` stop-window case: hold the first attempt on the existing whole-run gate, poll the invocation log for proof it started, stop, release; remove the 250 ms sleep and the "assertions only when the file exists" hedge. Verify: `xmake test-report --tag="[video-encode-runner]"` passes.
- [x] 3.3 `tests/utils_tests.cpp` exec2 stop cases — all three: (a,b) the two 150 ms cases — children touch a flag file before their long wait, poll the flag before requesting stop, replace the PowerShell child with `cmd`/`ping` (same pipe-holding semantics, no cold-start variance); (c) the partial-output case (~line 277) — child writes the flag on its first tick-loop iteration, poll it, then stop; relax `elapsed < 2s/5s` to 30 s hang guards. Verify: `xmake test-report --tag="[utils]"` passes.
- [x] 3.4 `tests/infra/stop_signal_tests.cpp`: wake test — drop the `< 1s` elapsed bound (`waitForStop(5s)` returning true is the assertion) and mark the 30 ms signal-delay sleep `sleep-ok`; watchdog test — replace the local 10 ms poll loop with the shared `waitUntil` (fourth copy from design D2). Verify: `xmake test-report --tag="[stop-signal]"` passes.

## 4. Convert async-effect waits and measurement sleeps

- [x] 4.1 `tests/video/video_batch_execution_tests.cpp` monitor stat-skip case (design D5): poll `state->lastFrameCount` (under its mutex) until the first parse result (10) before rewriting the same-size file; replace the 400+500 ms pair with one marked opportunity-to-parse margin ≥ the 250 ms parse throttle. Verify: `xmake test-report --tag="[video-batch-execution]"` passes.
- [x] 4.2 Same file, remaining monitor cases: throttle case keeps the 40 ms append cadence as the input signal (marked `sleep-ok`) and replaces the trailing 400 ms wait with a poll for the final frame value, keeping the wide distinctness bounds; monitor-exits-after-stop case — make the monitor's start observable by pointing an active state at a progress file (as the stat-skip case does) and polling `lastFrameCount` for the first parse instead of the 100 ms pre-stop sleep, then stop and keep an explicit generous elapsed bound around the join (a raw `join()` has no hang guard; Catch2 has no per-test timeout). Verify: monitor cases pass under a loaded parallel run.
- [x] 4.3 `tests/job_state_tests.cpp` and `tests/video/video_batch_execution_tests.cpp` elapsed-accumulation cases: drive the synthetic clock from task 1.2 with exact equality assertions; delete the 20–30 ms measurement sleeps. Verify: `[job-state]` and `[video-batch-execution]` tags pass with no measurement sleeps left in either file.
- [x] 4.4 `tests/logging_file_mgmt_tests.cpp` periodic-flush case: delete the negative "line not on disk yet" check (races the global 1 s flusher), keep the positive appearance poll via `waitUntil`. Verify: `xmake test-report --tag="[file_mgmt]"` passes.
- [x] 4.5 Deduplicate poll loops per design D2: replace the local `waitUntil` in `tests/e2e/encro_e2e_tests.cpp`, rewrite `waitUntilLogContains` in `tests/video/video_batch_execution_tests.cpp` as a predicate over a raw `ifstream` (missing file ⇒ false) delegating to the shared helper. Verify: e2e build + `xmake run e2e_tests` passes.

## 5. Guardrails

- [x] 5.1 Meta-check as a `TEST_CASE` in `tests/test_utils_tests.cpp`: scan `tests/**/*.cpp` (excluding `tests/e2e/fake_media_tool.cpp`, `tests/e2e/e2e_test_utils.cpp`) via `ENCRO_TEST_SOURCE_DIR` for `sleep_for` occurrences lacking a `// sleep-ok:` marker on the same line; fail with file:line. Census-driven close-out: enumerate every remaining occurrence from the scan and convert or mark each — expected marked survivors per design D7 (`task_executor` 20 ms workload shaping, `pack_service` 50 ms callback stall, `stop_signal` 30 ms signal delay, `video_batch` 40 ms cadence + D5 margin, `probe_cache` 5 ms ordering, e2e 20 ms mtime gap). Verify: check passes green with zero unmarked occurrences; a deliberately unmarked sleep makes it fail naming file:line; remove the probe.
- [x] 5.2 `plugins/test_parallel/xmake.lua`: append Catch2's duration flags to shard argv in `spawn_shards` (`--durations yes` / `--min-duration`, verified against the pinned Catch2 v3.15.2 CLI) without disturbing `shard_status` verdict parsing. Verify: full `xmake test-parallel` passes and shard logs contain per-test durations.

## 6. Final verification and docs

- [ ] 6.1 Run `xmake test-parallel` three times consecutively — all runs green, no timing failures; confirm total wall time is not worse than the plugin-documented baseline (~13 s; unit ~9.4 s, e2e ~12.3 s overlapped). Record the outcome in the change notes.
- [ ] 6.2 Document the convention in `AGENTS.md` (Testing section): tests synchronize by polling observable state, never fixed sleeps; any `sleep_for` needs a `// sleep-ok: <reason>` marker (enforced by the meta-check). Verify: docs match the implemented rule.
