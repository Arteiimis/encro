## 1. Palette core (commit 1)

- [x] 1.1 Add `enum class Role { Default, Muted, Accent, Good, Warn, Bad }` to `src/infra/terminal.h` with two mappers — `roleStyle(Role)` to `fmt::text_style` (`Default` → empty, `Muted` → faint, `Accent` → cyan, `Good`/`Warn`/`Bad` → green/yellow/red) and the bar-side mapper to `indicators::Color` that design D1 describes (see 2.1). No accessor wraps bold: headings pass `fmt::emphasis::bold` (D11). Add a unit test asserting each role's exact escape sequence for the text mapper. Verify: `xmake test-report --tag="[terminal]"` passes and the new cases go red if a role's slot is changed.
- [x] 1.2 Replace `styleFor(MessageKind)` with `kindStyleFor(MessageKind)` per design D2, returning one `{Role, StyleSite}` pair (the review pass merged the two switches) and mapping all fifteen then-current kinds so no kind is left unmapped. Verify: the project builds, `rg "styleFor\(" src` returns nothing outside the removed declaration, and the compiler reports no unhandled enum case in the new switches.
- [x] 1.3 Rewrite `renderMessage` to the D3 recipe: `None` returns the text untouched, `Prefix` styles only the `error:` / `warning:` / `hint:` text, `LeadingVerb` styles only the first word, and verb styling is skipped when the message already begins with an escape sequence. Verify: a new test renders `found <count> file(s) under <path>` through a narration kind and asserts the count and path spans are the accent sequence while every other character is unstyled — this is the regression test for the truncation defect and must go red if body wrapping is reintroduced.
- [x] 1.4 Collapse the three token styles onto one accent style: keep the string-taking helper (rename `terminal::value()` to an accent helper), implement `terminal::path()` and `terminal::count()` as thin wrappers over it, delete `styleForToken`, and remove the floral-white, golden-rod and light-sky-blue values. Verify: a test asserts a rendered path, a rendered count and a rendered pre-formatted string produce the identical escape sequence, `rg "38;2;" src` returns nothing, and the one existing `value()` call site in `src/video/video_batch_execution.cpp` now renders the output-format token in the accent color instead of the removed near-white.
- [x] 1.5 Route the console path arguments in `src/cmd/completion_install.cpp` through the accent helper (they are forward-slashed strings from its own `forwardSlashes()`, so they cannot use `terminal::path()` — see design D8). Leave the two call sites that build shell-profile lines alone: they write file content, not console output. Verify: with `ENCRO_TEST_COMPLETION=1`, `encro completion install --shell bash --color always` accents each printed path, and the paths keep their forward slashes.
- [x] 1.6 Replace `terminal::styledText` with the `styled(stream, fmt::text_style, text)` primitive per design D11 and rewrite its eleven `src/cmd/cmd.cpp` call sites against `accent(...)`, `roleStyle(Role::Accent) | emphasis::faint` (option defaults), `fmt::emphasis::bold` (section headings) and `roleStyle(Role::Muted)` (the brief-tier hint); the sites that pass text through unstyled collapse to the plain string. Move the two `styledText` cases in `tests/infra/terminal_tests.cpp` and the one in `tests/test_utils_tests.cpp` to `styled`. Verify: the project builds with no `styledText` reference remaining, and `encro -hh --color always` shows headings bold, option and subcommand names accented, and defaults accented-and-faint.
- [x] 1.7 Add a test helper that walks a rendered line's escape sequences and reports whether any styled span contains another, then assert it over one sample each of a help page, a narration line, a summary line, an error line and a warning line. Verify: the helper reports no nested span for every sample, and it reports one when a scratch edit reintroduces a body-wide style (revert the scratch edit afterwards).

## 2. Progress bars (commit 1)

- [x] 2.1 Replace `progress::Tone` with `Role` in `src/core/progress.{h,cpp}`, mapping `Default`/`Active`/`Overall`/`Idle` → `Accent`, `Packing`/`Finalizing` → `Warn`, `Success` → `Good`, `Failure` → `Bad` per design D7, and set `addBar`'s default argument to `Role::Accent` (not `Role::Default` — `src/organize/pipeline.cpp`'s `addBar("Analyzing")` relies on the default while wanting an active bar). Set no font style on the bar path, and leave no role that resolves to no color while colors are enabled: an uncolored bar inherits the preceding bar's color in the same frame. Verify: `xmake test-report --tag="[progress]"` passes against the updated mapping test, and `encro organize`'s analyzing bar still renders in the accent color.
- [x] 2.2 Make the colors-disabled path resolve to `indicators::Color::unspecified` with no font style instead of `Color::white`, and flip the two `resolveColor` cases in `tests/infra/progress_tests.cpp` accordingly. Verify: the test asserts `unspecified` (not `white`) for every role when the palette is disabled, and it fails if the old fallback returns.
- [x] 2.3 Update every `addBar` / `setRole` call site in `src/video/*`, `src/picture/*`, `src/pack/*`, `src/preview/*` and `src/organize/*` to the new role names, and rename the setter `setTone` → `setRole` so no name still claims a `Tone` type. Verify: the project builds with no `Tone` reference remaining, and `xmake test-report` is green.
- [x] 2.4 Confirm no colored frame leaves a bar uncolored. Verify: with colors enabled, a `-F` encode run's frame shows the overall, idle and active bars all in the accent color and the packing bar in yellow, with no bar appearing in a neighboring bar's color; with `--color never`, no bar sets a foreground and the whole frame renders in the terminal's default foreground on both a light and a dark theme.

## 3. Help surfaces (commit 1)

- [x] 3.1 Render help section headings with bold and no color in `formatHelpSection`, `formatGroupHeader` and the commands section of `formatCommandsSection`, and suppress the bold when styling is disabled. Verify: `encro -h --color always` shows a bold sequence before each heading and a color escape only on option and subcommand names and their defaults; `encro -h --color never` contains no escape sequence at all.
- [x] 3.2 Confirm help layout is unchanged by the new styling. Verify: the output of `encro -hh --color always` and `encro -hh --color never` is line-identical after stripping escape sequences, including under `COLUMNS=72`.

## 4. Attention group (commit 1)

- [x] 4.1 Implement the diagnostic-group requirement in `src/video/video_process.cpp`: one `warning: N item(s) need attention` announcement naming the count, then each item as an indented unprefixed `<subject>: <reason>` line in the default foreground. This is also what satisfies the modified `plan-output-formatting` requirement, whose text now refers to the attention block. Verify: the `post-encode` orchestration test drives a floor-unreachable run (mp4 output, sized inputs, a long fake duration) and asserts exactly one `warning: ` occurrence on stdout, that it names the count, that both item lines are indented and unprefixed in the raw capture, and that no item line carries a color escape.
- [x] 4.2 Confirm the group still travels with the run summary. Verify: a run producing an attention group with stdout redirected to a file contains the announcement and its items together in that file (the `console-output-conventions` redirection scenario).
- [x] 4.3 Commit sections 1–4 with their tests and the checkboxes above, as `refactor(cli): converge terminal colors on six semantic roles`. Verify: the commit builds and `xmake test-report` is green from a clean checkout of that commit alone.

## 5. Dead surface removal (commit 2)

- [x] 5.1 Delete `MessageKind::Heading` (no call sites) and its entries in the role, prefix, stream and quiet-gate switches. Verify: `xmake test-report` is green and a `--quiet` run suppresses the same narration lines as before.
- [x] 5.2 Delete `MessageKind::Usage` (its one call site on the app-description line already renders plain text, so no call-site change is needed). Verify: `encro -h` renders the description unchanged and no `Usage` kind remains.
- [x] 5.3 Point `src/app/app_entry.cpp`'s version line at `Plain` and delete `MessageKind::Version`. Verify: `encro --version` still prints under `--quiet` and renders unstyled.
- [x] 5.4 Point `src/utils/utils.cpp`'s prompt line at `Plain` and delete `MessageKind::Prompt`. Verify: the confirmation prompt still prints under `--quiet` and ends in `(Y/n): `; its prose carries no prefix and no verb styling, while any embedded accent-styled value (an output-format token in the encode prompt) keeps its own accent span.
- [x] 5.5 Drop the four deleted kinds from `kKinds` in `tests/infra/terminal_tests.cpp` and from `suppressedByQuiet`, and confirm the behavior cases in that file still cover the rewritten rendering (the table-to-behavior rewrite landed in commit 1, where deleting `styleFor` forced it). Verify: `xmake test-report --tag="[terminal]"` is green, the table lists exactly the eleven surviving kinds, and flipping the `renderMessage` body-wrap back to the old behavior turns at least one case red.
- [x] 5.6 Confirm the surviving kind set is exactly the eleven kinds with live call sites after commit 2 (the four help kinds are still present here and the four reported kinds are gone). Verify: the enum has eleven members and `rg "MessageKind::" src` resolves to no removed name.
- [x] 5.7 Commit section 5 with its tests and the checkboxes above, as `refactor(cli): drop unused message kinds and token styles`. Verify: the commit builds and `xmake test-report` is green from a clean checkout of that commit alone.

## 6. Help kinds removal (commit 3)

- [x] 6.1 Delete `MessageKind::OptionGroup`, `OptionName`, `OptionDefault` and `OptionDesc`, whose only route to the screen was the kind-keyed `styledText` that commit 1 removed; the help formatter now styles at its own call sites. Drop the stale "Phase 20 additions" marker with them. Verify: `rg "MessageKind::(OptionGroup|OptionName|OptionDefault|OptionDesc)" src` returns nothing, and `encro -hh --color always` still renders bold headings, accented option and subcommand names, and accented-plus-faint defaults.
- [x] 6.2 Drop the four kinds from `kKinds` in `tests/infra/terminal_tests.cpp` and from every switch in `src/infra/terminal.cpp`. Verify: the enum has seven members, every surviving kind has at least one production call site, and `xmake test-report` is green.
- [x] 6.3 Commit section 6 with its tests and the checkboxes above, as `refactor(cli): drop help message kinds`. Verify: the commit builds and `xmake test-report` is green from a clean checkout of that commit alone.

## 7. Verification and review

- [ ] 7.1 Run the full unit suite and confirm no failures: `xmake test-report`.
- [ ] 7.2 Run the reporter-mode probe and confirm zero failures: `build/windows/x64/release/tests.exe -r console -s`.
- [ ] 7.3 Run the parallel shard suite and confirm every shard's coverage check passes: `xmake test-parallel`.
- [ ] 7.4 Confirm the change set validates before archive: `openspec validate simplify-cli-color-palette --strict`.
- [ ] 7.5 Check the light-background contract on a real terminal: switch the terminal to a light theme, run `-h`, a `-z` pack run, an encode run with `-F` progress, and an organize run, and confirm no text is invisible or below roughly 3:1 against the background.
- [ ] 7.6 Confirm redirection invariance: a run's stdout with `--color always` and with `--color never` is line-identical after stripping escape sequences.
- [ ] 7.7 Run formatting and static analysis clean: `xmake fmt -k` and `xmake tidy`.
- [ ] 7.8 Run the `code-review` skill against this change, passing `openspec/changes/simplify-cli-color-palette/` as the spec path, with a third leanness sub-agent briefed with the `ponytail-review` tag rules verbatim; triage the findings, apply the accepted ones, and run the fix-verify loop (max two rounds).
- [ ] 7.9 Archive the change with the specs sync step, so the new `terminal-color-palette` capability lands in the main specs and `console-output-conventions` and `plan-output-formatting` pick up their modified requirements.
