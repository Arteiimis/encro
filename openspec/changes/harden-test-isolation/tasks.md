# Tasks: harden-test-isolation

## 1. Shared guards and capture helper

- [x] 1.1 Add `testutils::captureStdout(callable)` to `tests/test_utils.h` on top of the RAII `FileCapture` and delete the raw `_dup2` copy in `tests/utils_tests.cpp:24-63`; cover it in `tests/test_utils_tests.cpp` with (a) a normal capture, (b) a callable that throws, asserting the stream works again afterwards. Verify: `xmake test-report --tag="[test-utils]"` passes.
- [x] 1.2 Add `testutils::shutdownLogging()`, which calls `logging::shutdown()` and re-installs the run's sink-less default logger (see `tests/test_main.cpp:127`, and `docs/backlog.md:75-92` for the flake this prevents), with a case in `tests/test_utils_tests.cpp` asserting that a `LOG_*` fallback after the call does not write to captured stderr; apply it to every test call site of `logging::shutdown()` (24 sites, including `tests/logging_*_tests.cpp`, `tests/infra/stop_signal_tests.cpp:115`, `tests/infra/crash_runtime_tests.cpp:342`). Verify: `xmake test-report --tag="[logging]"` and `--tag="[file_mgmt]"` pass, and `xmake test-report --tag="[crash]"` plus `--tag="[stop-signal]"` pass for the two non-logging sites.
- [x] 1.3 Add a scoped crash-context-provider guard and use it in `tests/infra/crash_runtime_tests.cpp:175` so the provider installed in `tests/test_main.cpp:119` is back when the case ends; cover the restore in a `[crash]` case. Verify: `xmake test-report --tag="[crash]"` passes.
- [x] 1.4 Add a scoped `std::cin` buffer guard and use it at all seven statement-based `std::cin.rdbuf` sites: `tests/utils_tests.cpp:107,124,134,146,159`, `tests/picture/picture_process_tests.cpp:377` and `tests/video/video_batch_execution_tests.cpp:373`. Verify: `xmake test-report --tag="[utils]"`, `--tag="[picture-process]"` and `--tag="[video-batch-execution]"` pass.
- [x] 1.5 Make the terminal guard shared and use it: move the colour-mode guard that today lives inside `tests/infra/terminal_tests.cpp` into `tests/test_utils.h`, then replace every statement-based colour-mode reset in `tests/cmd_cmd_tests.cpp` with it — the 7 `terminal::configure(...)` sets (lines 354, 367, 387, 390, 401, 637, 641) and the 9 `terminal::reset()` calls (lines 362, 372, 393, 404, 417, 428, 442, 639, 643), including the three that clean up after the `configureFromColorString` cases. The force-exit watchdog case in `tests/infra/stop_signal_tests.cpp:103-124` disarms the armed deadline and grace period through its guard, so a mid-case assertion failure cannot leave the watchdog armed. Verify: `xmake test-report --tag="[cmd]"`, `--tag="[terminal]"` and `--tag="[stop-signal]"` pass, and breaking one of the edited cases mid-way (temporarily `REQUIRE(false)`) still leaves the colour mode and watchdog clean for the rest of the run.

## 2. Assertions outside redirect windows

- [x] 2.1 `tests/utils_tests.cpp`: keep the captured result in a variable and assert after the capture scope (the prompt cases at `:113` and the interrupted-prompt case at `:169`), so a reporter that echoes successful assertions cannot land in the captured file. Verify: `build\windows\x64\release\tests.exe -r console -s "readUserIpt*"` passes and reports the same assertion count as without `-s`.
- [ ] 2.2 `tests/infra/terminal_tests.cpp` (`messageln routes each kind to its own stream`): construct captures so no capture helper assertion runs while another stream is redirected, and assert on the captured text only after both redirects end. Verify: `build\windows\x64\release\tests.exe -r console -s "[terminal]"` and `build\windows\x64\release\tests.exe -r console "[terminal]"` both pass with identical assertion counts.
- [ ] 2.3 Make `testutils::FileCapture` assert-free: the constructor records whether it duplicated the descriptor and opened the capture file instead of asserting, and exposes that state for the caller to check after the redirect has ended. `testutils::captureStdout` checks it internally (after its own scope), and every direct user of `StdoutCapture`/`StderrCapture`/`FileCapture` checks it after its capture scope: `tests/infra/progress_tests.cpp`, `tests/infra/terminal_tests.cpp`, `tests/utils_tests.cpp`, `tests/packer_tests.cpp`, `tests/preview/preview_process_tests.cpp`, `tests/app/pipeline_picture_tests.cpp`, `tests/app/pipeline_pack_only_tests.cpp`, `tests/cmd_config_tests.cpp`, `tests/cmd_completion_command_tests.cpp`, `tests/cmd_completion_install_tests.cpp`, `tests/infra/crash_runtime_tests.cpp`, `tests/video/encode_probe_tests.cpp`, `tests/video/video_process_orchestration_tests.cpp`, `tests/test_utils_tests.cpp`. Verify: `build\windows\x64\release\tests.exe -r console -s` reports 0 failures (before this task it reports 5) and `xmake test-report` still reports 0 failures.
- [ ] 2.4 Make the reporter-mode requirement checkable without reading source: add the `-s` probe from 2.1-2.3 to the verification notes in `AGENTS.md` (Testing section) so the next change that touches capture code re-runs it. Verify: `xmake fmt -k` passes and the note names the exact command.

## 3. Per-run private fixture paths

- [ ] 3.1 `tests/test_utils.h:44-45`: `TempDir` derives its name from process id, a per-process counter and the clock; cover that two fixtures created in the same tick differ and that the name contains the process id. Verify: `xmake test-report --tag="[test-utils]"` passes.
- [ ] 3.2 `tests/pack_execute_tests.cpp:54`: use a `TempDir` instead of the shared `%TEMP%/encro_test_empty`. Verify: `xmake test-report --tag="[pack][execute]"` passes twice in a row with no leftover directory.
- [ ] 3.3 `tests/work_dirs_tests.cpp:24-29`: pin TMP/TEMP to a private directory with `ScopedEnvVar` instead of `remove_all` on the shared application scratch root, and assert the created directory lives under the private root. Verify: `xmake test-report --tag="[work-dirs]"` passes and the shared scratch root is untouched (`Test-Path $env:TEMP\encro\scratch` unchanged before/after).
- [ ] 3.4 `tests/tagger/real_model_tests.cpp:17`: read the model directory from a documented environment variable and `SKIP()` when it is unset, instead of hardcoding one machine's home directory. Verify: `xmake test-report --tag="[real-model]"` passes with the variable unset (reported as skipped, not passed-with-zero-assertions).

## 4. Gate-based proofs and deadline margins

- [ ] 4.1 `tests/e2e/encro_e2e_tests.cpp:886`: prove the two encodes overlapped with a gate or a positive overlap assertion without an upper bound on the spawn delay, instead of `maxOverlapMs >= 2500` within a 500 ms window. Verify: `xmake build e2e_tests && xmake run e2e_tests` passes and the case still fails when the second invocation is serialized (temporarily assert overlap `> 0` on a single-threaded plan to see it go red).
- [ ] 4.2 Widen the poll deadlines to at least three producer periods: `tests/logging_file_mgmt_tests.cpp:394` 3 s -> 10 s, `FinalizingWindow::hold()` in `tests/pack_service_tests.cpp` 5 s -> 30 s, and the real-ffmpeg segment wait in `tests/e2e/encro_e2e_tests.cpp:2060-2085` 60 s -> 180 s. Verify: `xmake test-report --tag="[file_mgmt]"` and `--tag="[pack-service]"` pass; e2e passes.
- [ ] 4.3 `tests/video/video_process_orchestration_tests.cpp:44-49`: assert the stray `-progress` file explicitly at the end of the case instead of in the guard's destructor, and keep the watched path inside the case's own working directory (the private-path requirement in `test-isolation`). Verify: `xmake test-report --tag="[video-process]"` passes.

## 5. Tooling: shard verdicts and gate observability

- [ ] 5.1 `tests/e2e/fake_media_tool.cpp:531-536`: report the unreleased gate file on stderr before proceeding at the deadline; cover it with a fake-tool case that configures a gate, never releases it, and asserts the warning (the existing fail-safe keeps the 30 s bound). Verify: `xmake test-report --tag="[fake-tool]"` passes.
- [ ] 5.2 `plugins/test_parallel/xmake.lua`: launch each shard with a per-shard JUnit report and decide the verdict from the shard's exit status plus that report (missing or unparsable report = failed; `<failure>`/`<error>` entries = failed), keeping the log for the printed summary. Verify: a deliberately broken test in one shard marks that shard failed (flip one assertion to see it red), a clean run reports pass, and the build-status guard added earlier still refuses to run a stale binary.
- [ ] 5.3 Confirm the shard isolation rule with an observable check: run `xmake test-parallel` and assert no shard observes another shard's temporary paths or the shared log directory (the per-shard TMP/TEMP override plus 3.1 cover the paths). Verify: `xmake test-parallel` passes and its logs show per-shard TMP roots.

## 6. CI, guardrails and documentation

- [ ] 6.1 `.github/workflows/ci.yml:102`: keep the unit suite's shuffled order (Catch2 v3.15 already randomises per run, `docs/backlog.md:83`) but make its seed observable — run the console reporter alongside the JUnit report (`-r console -r junit::out=/tmp/ut.xml > /tmp/ut.log 2>&1`) so `ut.log` carries the seed line, the console summary and per-test output. Verify: the next CI run is green and its uploaded `ut.log` contains `Randomness seeded to:`; local reproduction with that seed works as documented in 6.3.
- [ ] 6.2 Extend the meta-check family in `tests/test_utils_tests.cpp` with a source scan over `ENCRO_TEST_SOURCE_DIR` rejecting raw `dup2`/`_dup2`, `freopen`, `std::cin.rdbuf(` and `std::cout.rdbuf(` outside `tests/test_utils.h` unless the line carries `// isolation-ok: <reason>`; include a case proving the check rejects an unmarked probe and accepts a marked one. Verify: `xmake test-report --tag="[test-utils][meta]"` passes.
- [ ] 6.3 Document in `AGENTS.md` (Testing section) how to reproduce an order-dependent failure from the seed in CI's `ut.log` (`tests.exe --rng-seed <N>`) and how to run the `-s` reporter probe from 2.1-2.4. Verify: `xmake fmt -k` passes and the documented commands work as written.
- [ ] 6.4 Record in `docs/backlog.md` the JUnit-`tests=`-vs-console assertion-count difference seen with `test-report`, and the deferred option of a second fixed-seed shuffled CI pass. Verify: `xmake fmt -k` passes.

## 7. Verification sweep

- [ ] 7.1 Run the full unit suite four times, recording the seed each run prints, and compare the executed assertion locations and counts between runs; confirm zero differences apart from order. Verify: all four runs exit 0 with identical assertion counts and identical assertion-location multisets.
- [ ] 7.2 Run `xmake test-parallel` (real-ffmpeg tests included) twice and confirm both runs pass with the new shard verdicts. Verify: both runs report pass, with no shard reported failed and no shard touching another shard's paths in the logs.
- [ ] 7.3 Run the full CI matrix once on the change's commits (debug/release/coverage) and confirm all jobs pass with the seed visible in the uploaded logs. Verify: all four jobs succeed and each uploaded `ut.log` shows the shuffled seed line.
