# Tasks: harden-test-isolation

## 1. Shared guards and capture helper

- [x] 1.1 Add `testutils::captureStdout(callable)` to `tests/test_utils.h` on top of the RAII `FileCapture` and delete the raw `_dup2` copy in `tests/utils_tests.cpp:24-63`; cover it in `tests/test_utils_tests.cpp` with (a) a normal capture, (b) a callable that throws, asserting the stream works again afterwards. Verify: `xmake test-report --tag="[test-utils]"` passes.
- [x] 1.2 Add `testutils::shutdownLogging()`, which calls `logging::shutdown()` and re-installs the run's sink-less default logger (see `tests/test_main.cpp:127`, and `docs/backlog.md:75-92` for the flake this prevents), with a case in `tests/test_utils_tests.cpp` asserting that a `LOG_*` fallback after the call does not write to captured stderr; apply it to every test call site of `logging::shutdown()` (24 sites, including `tests/logging_*_tests.cpp`, `tests/infra/stop_signal_tests.cpp:115`, `tests/infra/crash_runtime_tests.cpp:342`). Verify: `xmake test-report --tag="[logging]"` and `--tag="[file_mgmt]"` pass, and `xmake test-report --tag="[crash]"` plus `--tag="[stop-signal]"` pass for the two non-logging sites.
- [x] 1.3 Add a scoped crash-context-provider guard and use it in `tests/infra/crash_runtime_tests.cpp:175` so the provider installed in `tests/test_main.cpp:119` is back when the case ends; cover the restore in a `[crash]` case. Verify: `xmake test-report --tag="[crash]"` passes.
- [x] 1.4 Add a scoped `std::cin` buffer guard and use it at all seven statement-based `std::cin.rdbuf` sites: `tests/utils_tests.cpp:107,124,134,146,159`, `tests/picture/picture_process_tests.cpp:377` and `tests/video/video_batch_execution_tests.cpp:373`. Verify: `xmake test-report --tag="[utils]"`, `--tag="[picture-process]"` and `--tag="[video-batch-execution]"` pass.
- [x] 1.5 Make the terminal guard shared and use it: move the colour-mode guard that today lives inside `tests/infra/terminal_tests.cpp` into `tests/test_utils.h`, then replace every statement-based colour-mode reset in `tests/cmd_cmd_tests.cpp` with it — the 7 `terminal::configure(...)` sets (lines 354, 367, 387, 390, 401, 637, 641) and the 9 `terminal::reset()` calls (lines 362, 372, 393, 404, 417, 428, 442, 639, 643), including the three that clean up after the `configureFromColorString` cases. The force-exit watchdog case in `tests/infra/stop_signal_tests.cpp:103-124` disarms the armed deadline and grace period through its guard, so a mid-case assertion failure cannot leave the watchdog armed. Verify: `xmake test-report --tag="[cmd]"`, `--tag="[terminal]"` and `--tag="[stop-signal]"` pass, and breaking one of the edited cases mid-way (temporarily `REQUIRE(false)`) still leaves the colour mode and watchdog clean for the rest of the run.

## 2. Assertions outside redirect windows

- [x] 2.1 `tests/utils_tests.cpp`: keep the captured result in a variable and assert after the capture scope (the prompt cases at `:113` and the interrupted-prompt case at `:169`), so a reporter that echoes successful assertions cannot land in the captured file. Verify: `build\windows\x64\release\tests.exe -r console -s "readUserIpt*"` passes and reports the same assertion count as without `-s`.
- [x] 2.2 `tests/infra/terminal_tests.cpp` (`messageln routes each kind to its own stream`): no edit of its own was needed — it already asserted on the captured text only after both redirects ended, and the helper assertion that polluted it went away with 2.3. Verify: `build\windows\x64\release\tests.exe -r console -s "[terminal]"` and `build\windows\x64\release\tests.exe -r console "[terminal]"` both pass with identical counts (276 assertions in 10 test cases).
- [x] 2.3 Make `testutils::FileCapture` assert-free: the constructor records whether it redirected the stream and reports a failed redirect on stderr (same idiom as `TempDir`'s kept-directory hint) instead of asserting, and exposes it through `redirected()`; `testutils::captureStdout` checks it after its own capture scope has ended. The 42 direct capture sites keep their assertions on the captured text — which is empty when a redirect could not be established — so a failure stays loud without 42 duplicated post-scope checks. Verify: `build\windows\x64\release\tests.exe -r console -s` reports 0 failures (before this task it reports 5) and `xmake test-report` still reports 0 failures.
- [x] 2.4 Make the reporter-mode requirement checkable without reading source: add the `-s` probe from 2.1-2.3 to the verification notes in `AGENTS.md` (Testing section) so the next change that touches capture code re-runs it. Verify: `xmake fmt -k` passes and the note names the exact command.

## 3. Per-run private fixture paths

- [x] 3.1 `tests/test_utils.h:44-45`: `TempDir` derives its name from process id, a per-process counter and the clock; cover that two fixtures created in the same tick differ and that the name contains the process id. Verify: `xmake test-report --tag="[test-utils]"` passes.
- [x] 3.2 `tests/pack_execute_tests.cpp:54`: use a `TempDir` instead of the shared `%TEMP%/encro_test_empty`. Verify: `xmake test-report --tag="[pack][execute]"` passes twice in a row with no leftover directory.
- [x] 3.3 `tests/work_dirs_tests.cpp:24-29`: pin TMP/TEMP to a private directory with `ScopedEnvVar` instead of `remove_all` on the shared application scratch root, and assert the created directory lives under the private root. Verify: `xmake test-report --tag="[work-dirs]"` passes and the shared scratch root is untouched (`Test-Path $env:TEMP\encro\scratch` unchanged before/after).
- [x] 3.4 `tests/tagger/real_model_tests.cpp:17`: read the model directory from a documented environment variable and `SKIP()` when it is unset, instead of hardcoding one machine's home directory. Verify: `ENCRO_TEST_MODEL_DIR=<dir> xmake test-report --tag="[real-model]"` passes (the case then runs real inference); with the variable unset the case reports SKIPPED and Catch2 exits 4 (`no tests ran`) because it is the only matched case. `AGENTS.md` documents the variable.
- [x] 3.5 `tests/e2e/e2e_test_utils.cpp`: pin the app log root the e2e suites use to a per-process private directory (`LOCALAPPDATA` on Windows, `XDG_STATE_HOME` on POSIX, named by `uniqueTempName`), because the 12 parallel shards otherwise share one log directory, prune each other's `encro_*.log` files and make a case read back the wrong run's log (found by the `--quiet` narration case failing under `xmake test-parallel`). Cases that read their own logs keep overriding the root per run with `logRootEnv`. Verify: `xmake test-parallel` passes twice in a row and the app writes no new file under the real user log directory.

## 4. Gate-based proofs and deadline margins

- [x] 4.1 `tests/e2e/encro_e2e_tests.cpp`: the overlap proof is gate-based now — every ffmpeg invocation blocks on `ENCRO_FAKE_FFMPEG_GATE_FILE`, a watcher thread polls the fake tool invocation log until two invocations are held at once, releases the gate either way, and the case asserts that observation instead of `maxOverlapMs >= 2500`. Verify: `xmake build e2e_tests && xmake run e2e_tests "[parallel]"` passes, and flipping the assertion to `CHECK_FALSE(bothInFlight)` goes red (verified). Note: `-j 1` still overlaps ffmpeg stages, so the proof does not hinge on the job count.
- [x] 4.2 Widen the poll deadlines to at least three producer periods: `tests/logging_file_mgmt_tests.cpp` 3 s -> 10 s, `FinalizingWindow::hold()` in `tests/pack_service_tests.cpp` 5 s -> 30 s, and the real-ffmpeg segment wait in `tests/e2e/encro_e2e_tests.cpp` 60 s -> 180 s. Verify: `xmake test-report --tag="[file_mgmt]"` and `--tag="[pack-service]"` pass; `xmake run e2e_tests "[interrupt]"` and `"[segment]"` pass.
- [x] 4.3 `tests/video/video_process_orchestration_tests.cpp`: `StrayProgressGuard` now runs its case inside that case's own directory (`ScopedCurrentPath`) and checks on scope exit only when the case is not already unwinding, so the stray `-progress` file is watched in a private directory and no assertion runs from a destructor during unwinding; the 9 call sites pass `temp.path`. Verify: `xmake test-report --tag="[orchestration]"` and `--tag="[video-process]"` pass.

## 5. Tooling: shard verdicts and gate observability

- [x] 5.1 `tests/e2e/fake_media_tool.cpp:531-536`: report the unreleased gate file on stderr before proceeding at the deadline; cover it with a fake-tool case that configures a gate, never releases it, and asserts the warning (the existing fail-safe keeps the 30 s bound). Verify: `xmake test-report --tag="[fake-tool]"` passes.
- [x] 5.2 `plugins/test_parallel/xmake.lua`: launch each shard with a per-shard JUnit report and decide the verdict from that report (missing or unparsable report = failed; `<failure>`/`<error>` entries = failed), keeping the log for the printed summary. Verify: a deliberately broken test in one shard marks that shard failed (flip one assertion to see it red), a clean run reports pass, and the build-status guard added earlier still refuses to run a stale binary.
- [x] 5.3 Confirm the shard isolation rule with an observable check: run `xmake test-parallel` and assert no shard observes another shard's temporary paths or the shared log directory (the per-shard TMP/TEMP/TMPDIR override plus 3.1 cover the paths). Verify: `xmake test-parallel` passes and its logs show per-shard TMP roots.
- [x] 5.4 Residual load flake found while verifying 5.2/5.3: under `xmake test-parallel` (12 shards) the real-ffmpeg case `encro real ffmpeg resumes a killed segmented encode frame-exactly` failed in one of three runs at `tests/test_utils.h:477` (`REQUIRE(ifs.is_open())` in `readTextFile`) — a helper read a file a child process had not produced yet — while passing 3/3 in isolation (87-89 assertions). Fixed by two window/visibility defects the load exposed: `readTextFile` now waits briefly for the producer and names the file on failure, and the three `[debug]`-in-log assertions read the whole log through the new `encroLogText` instead of `encroLogTail`'s diagnostic 8 KiB tail (a loaded run can end with more non-debug lines, and the `--quiet` case failed exactly that way twice). Verify: 8 consecutive `xmake test-parallel` runs pass (one failure observed before the fix), plus `[quiet]`, `[verbose]` and `[parallel]` e2e tags green.

## 6. CI, guardrails and documentation

- [x] 6.1 `.github/workflows/ci.yml:102`: keep the unit suite's shuffled order (Catch2 v3.15 already randomises per run, `docs/backlog.md:83`) but make its seed observable — run the console reporter alongside the JUnit report (`-r console -r junit::out=/tmp/ut.xml > /tmp/ut.log 2>&1`) so `ut.log` carries the seed line, the console summary and per-test output. Verify: the next CI run is green and its uploaded `ut.log` contains `Randomness seeded to:`; local reproduction with that seed works as documented in 6.3.
- [x] 6.2 Extend the meta-check family in `tests/test_utils_tests.cpp` with a source scan over `ENCRO_TEST_SOURCE_DIR` rejecting raw `dup2`/`_dup2`, `freopen`, `std::cin.rdbuf(` and `std::cout.rdbuf(` outside `tests/test_utils.h` unless the line carries `// isolation-ok: <reason>`; include a case proving the check rejects an unmarked probe and accepts a marked one. Verify: `xmake test-report --tag="[test-utils][meta]"` passes.
- [x] 6.3 Document in `AGENTS.md` (Testing section) how to reproduce an order-dependent failure from the seed in CI's `ut.log` (`tests.exe --rng-seed <N>`) and how to run the `-s` reporter probe from 2.1-2.4. Verify: `xmake fmt -k` passes and the documented commands work as written.
- [x] 6.4 Record in `docs/backlog.md` the JUnit-`tests=`-vs-console assertion-count difference seen with `test-report`, and the deferred option of a second fixed-seed shuffled CI pass. Verify: `xmake fmt -k` passes.

## 7. Verification sweep

- [x] 7.1 Run the full unit suite four times, recording the seed each run prints, and compare the executed assertion locations and counts between runs; confirm zero differences apart from order. Verify: all four runs exit 0 with identical assertion counts and identical assertion-location multisets.
- [x] 7.2 Run `xmake test-parallel` (real-ffmpeg tests included) twice and confirm both runs pass with the new shard verdicts. Verify: both runs report pass, with no shard reported failed and no shard touching another shard's paths in the logs.
- [ ] 7.3 Run the full CI matrix once on the change's commits (debug/release/coverage) and confirm all jobs pass with the seed visible in the uploaded logs. Verify: all four jobs succeed and each uploaded `ut.log` shows the shuffled seed line.
