## 1. Packing reports its cancellation

- [ ] 1.1 Add the failing cases in `tests/pack_execute_tests.cpp` (`[pack][execute]`): a `pack::execute(PackRequest)` with a stop requested before any group runs prints `warning: Packing canceled by user.` once on stderr, returns exit code 130 in both the resumable and the non-resumable configuration, and prints no `Failed to pack ...` or other error line; verify with `xmake test-report --tag="[pack]"` that they fail for the current behavior.
- [ ] 1.2 Make the non-resumable path translate an abort instead of an error: in `pack::runNonResumable` (`src/pack/pack.cpp`), a failed `packGroups` with a stop pending returns `PackRunResult{.exitCode = stopsignal::kCanceledExitCode}` the way `runResumable` already does, and the resumable path's abort keeps its existing result; verify case 1.1's non-resumable assertion fails before and passes after.
- [ ] 1.3 Print the single notice at the `pack::execute(PackRequest const&)` funnel in `src/pack/pack.cpp`: when the inner result carries the cancellation exit code, print `Packing canceled by user.` through `terminal::messageln(Warning, ...)` (stderr) and return that result unchanged; a result with exit code 0 prints nothing even when a stop is pending; verify cases 1.1 pass and the existing `[pack]` suite stays green.
- [ ] 1.4 Verify all four pack callers keep their behavior: picture direct pack, picture compress+pack, video media pack and `--pack-only` each exit 130 after the new notice — check with `xmake test-report --tag="[pipeline]"` (the pack-only cancel case asserts 130 today) plus a manual run of 1.1's non-resumable case.

## 2. Probing and encoding report their cancellation

- [ ] 2.1 Add the failing cases in `tests/video/video_batch_execution_tests.cpp` (`[video-batch-execution]`): a `videobatch::runEncodingTasks` whose probe stage is aborted by a stop request prints `warning: Probing canceled by user.` on stderr, returns no results, and prints no plan block; add the run-level half in `tests/video/video_process_orchestration_tests.cpp` (`[video-process]`) pinning that the same abort ends the run with the cancellation exit code 130 (already true today, so only the notice half is red) and that a stop arriving with no video left to encode prints no encode notice; verify the notice cases fail today with `xmake test-report --tag="[video-batch-execution]"` while the exit-code case passes.
- [ ] 2.2 Print the notice where the stop is detected: `runProbeStage`'s `Aborted` branch (`src/video/video_batch_execution.cpp`) prints `Probing canceled by user.` next to its `noteStopRequest(ctx)` call, and the stage still returns `ProbeStageStatus::Aborted`; verify case 2.1 passes and no plan block reaches stdout.
- [ ] 2.3 Add the failing case for the encode batch in the same file: a stop that cuts the batch short prints `warning: Encoding tasks canceled by user.` exactly once on stderr, returns the cancellation exit code, and prints neither an encode summary line nor the failed-file list; verify it fails today (the stopped encode is silent) while the declined-prompt case is unaffected.
- [ ] 2.4 Print the notice at the encode batch's single stop funnel: `maybeHandleInterruptedEncoding` (`src/video/video_process.cpp`) prints `Encoding tasks canceled by user.` before it returns the cancellation code, and only when the stop actually cut the batch short — the workflow can tell by comparing the results it collected against the total action count, not merely by `pendingVids` being non-empty, so a stop that lands after the last encode finished leaves the encode stage finished and lets the next stage report the cancellation; the prompt-decline path (which returns earlier, from `confirmEncodingStart`) keeps its own single line; verify case 2.3 passes, that a probe abort still prints only the probe notice, that a resume run with nothing left to encode prints none, and that `xmake test-report --tag="[video-batch-execution]"` plus `--tag="[video-process]"` are green.

## 3. Preview reports its cancellation and exits 130

- [ ] 3.1 Add the failing cases in `tests/preview/preview_process_tests.cpp` (`[preview]`): with a stop requested, (a) at the window-encode checkpoint, (b) after the render child was terminated by the stop, and (c) after the probe step was terminated by the stop, the run prints `warning: Preview canceled by user.` on stderr, returns 130, and prints neither the window list nor a written-to line, no `Preview generation failed (exit code ...)` and no `Probing skipped` warning; verify with `xmake test-report --tag="[preview]"` that they fail for the current behavior.
- [ ] 3.2 Report the abort instead of the child's failure: in `src/preview/preview_process.cpp`, the window-encode stop check, the render-failure handling in `renderAndReportSingleInput` and the probe step in `probeSingleInputPlan` each check `stopsignal::isStopRequested()` and return the cancellation (notice plus `kCanceledExitCode`) before reporting a child failure or starting window encodes; verify cases 3.1 pass and the existing `[preview]` suite stays green.

## 4. Organize reports its cancellation and honors it while copying

- [ ] 4.1 Add the failing cases in `tests/organize/` (`[organize]`): a stop requested during analysis and a stop requested during the copy phase each end the run with `warning: Organize canceled by user.` on stderr, exit code 130, no report on stdout, and — for the copy case — the images not yet started are not copied; verify with `xmake test-report --tag="[organize]"` that they fail for the current behavior (analysis aborts with `interrupted: completed analysis is cached` and exit 1; copying ignores the stop).
- [ ] 4.2 Report the abort explicitly: `ExecuteStats` (`src/organize/execute.h`) gains a canceled flag set when the copy loop's new per-image `stopsignal::isStopRequested()` checkpoint breaks, and `runOrganize` (`src/organize/pipeline.cpp`) surfaces it on its result instead of the analyze-abort error string; a genuine analysis or copy failure still reports as an error; verify the copy case of 4.1 fails before and passes after, with the analysis-abort message no longer reaching the console.
- [ ] 4.3 Map the cancellation at the command boundary: `organize::runOrganizeCommand` (`src/organize/organize_command.cpp`) prints `Organize canceled by user.` through `terminal::messageln(Warning, ...)` and returns `stopsignal::kCanceledExitCode` when the pipeline reports a cancellation, prints the report otherwise; verify both cases of 4.1 pass and `xmake test-report --tag="[organize]"` is green.

## 5. Pin the picture-compression cancellation at the process level

- [ ] 5.1 Add an e2e case (`tests/e2e/encro_e2e_tests.cpp`, `[e2e][picture][interrupt][fake-toolchain]`): a picture compression held open by the fake toolchain's gate receives a real console Ctrl+C through `runEncroAsync` + `sendCtrlC` (`SKIP()` when `consoleCtrlEventsAvailable()` is false) and then reports exit 130, `warning: Compression task canceled by user.` on stderr, and no `<path>: exit code <n>` line on stdout; verify with `xmake build e2e_tests && xmake run e2e_tests` — the unit guard `picture pipeline compress cancel prints no per-file failure lines` (added in commit 79046a5) already covers the contract, and this task proves it over a real console event.

## 6. Integration verification

- [ ] 6.1 Verify the notice rule end to end for the paths without a process-level case: no stopped stage prints a summary line, plan block, report or success line, and no stage reports a killed child as a failure — run `xmake test-report` (full unit suite), `xmake test-parallel`, and `xmake build e2e_tests && xmake run e2e_tests`, all green.
- [ ] 6.2 Verify the exit-code contract against the run summary and update the comment that documents the old one: a canceled run's log summary reports `interrupted`, a stop arriving after the last stage still exits 0, and the `runStatus` comment in `tests/app/app_entry_tests.cpp` no longer justifies itself with "preview reports its cancel as an error (exit 1)" — the `runStatus(1, true) == "interrupted"` case stays as "a genuine failure while a stop is pending reads interrupted" — run `xmake test-report --tag="[logging]"` and `--tag="[appentry]"`.
- [ ] 6.3 Manual check with a real terminal: `encro -t picture -r --compress -i <dir>` interrupted with Ctrl+C during compression, and a Ctrl+C during `encro --pack-only -i <dir>` — each shows cleared bars, one `warning: <Stage> canceled by user.` line, no failure list, and `echo $LASTEXITCODE`/`%ERRORLEVEL%` reads 130.
