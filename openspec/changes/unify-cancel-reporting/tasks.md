## 1. Packing reports its cancellation

- [x] 1.1 Add the failing cases in `tests/pack_execute_tests.cpp` (`[pack][execute]`): a `pack::execute(PackRequest)` with a stop requested before any group runs prints `warning: Packing canceled by user.` once on stderr, returns exit code 130 in both the resumable and the non-resumable configuration, and prints no `Failed to pack ...` or other error line; verify with `xmake test-report --tag="[pack]"` that they fail for the current behavior.
- [x] 1.2 Make the non-resumable path translate an abort instead of an error: in `pack::runNonResumable` (`src/pack/pack.cpp`), a failed `packGroups` with a stop pending returns `PackRunResult{.exitCode = stopsignal::kCanceledExitCode}` the way `runResumable` already does, and the resumable path's abort keeps its existing result; verify case 1.1's non-resumable assertion fails before and passes after.
- [x] 1.3 Print the single notice at the `pack::execute(PackRequest const&)` funnel in `src/pack/pack.cpp`: when the inner result carries the cancellation exit code, print `Packing canceled by user.` through `terminal::messageln(Warning, ...)` (stderr) and return that result unchanged; a result with exit code 0 prints nothing even when a stop is pending; verify cases 1.1 pass and the existing `[pack]` suite stays green.
- [x] 1.4 Verify all four pack callers keep their behavior: picture direct pack, picture compress+pack, video media pack and `--pack-only` each exit 130 after the new notice — check with `xmake test-report --tag="[pipeline]"` (the pack-only cancel case asserts 130 today) plus a manual run of 1.1's non-resumable case.

## 2. Probing and encoding report their cancellation

- [x] 2.1 Add the failing cases in `tests/video/video_batch_execution_tests.cpp` (`[video-batch-execution]`): a `videobatch::runEncodingTasks` whose probe stage is aborted by a stop request prints `warning: Probing canceled by user.` on stderr, returns no results, and prints no plan block; add the run-level half in `tests/video/video_process_orchestration_tests.cpp` (`[video-process]`) pinning that the same abort ends the run with the cancellation exit code 130 (already true today, so only the notice half is red) and that a stop arriving with no video left to encode prints no encode notice; verify the notice cases fail today with `xmake test-report --tag="[video-batch-execution]"` while the exit-code case passes.
- [x] 2.2 Print the notice where the stop is detected: `runProbeStage`'s `Aborted` branch (`src/video/video_batch_execution.cpp`) prints `Probing canceled by user.` next to its `noteStopRequest(ctx)` call, and the stage still returns `ProbeStageStatus::Aborted`; verify case 2.1 passes and no plan block reaches stdout.
- [x] 2.3 Add the failing case for the encode batch in `tests/video/video_process_orchestration_tests.cpp` (`[video-process][stop-signal]`, where the notice is emitted — the batch itself cannot observe it): a stop that cuts the batch short prints `warning: Encoding tasks canceled by user.` exactly once on stderr, ends the workflow with the cancellation exit code, and prints neither an encode summary line nor the failed-file list; verify it fails today (the stopped encode is silent) while the declined-prompt case is unaffected.
- [x] 2.4 Print the notice at the encode batch's single stop funnel: `maybeHandleInterruptedEncoding` (`src/video/video_process.cpp`) prints `Encoding tasks canceled by user.` before it returns the cancellation code, and only when the stop actually cut the batch short — the workflow can tell by comparing the results it collected against the total action count, not merely by `pendingVids` being non-empty, so a stop that lands after the last encode finished leaves the encode stage finished and lets the next stage report the cancellation; the prompt-decline path (which returns earlier, from `confirmEncodingStart`) keeps its own single line; verify case 2.3 passes, that a probe abort still prints only the probe notice, that a resume run with nothing left to encode prints none, and that `xmake test-report --tag="[video-batch-execution]"` plus `--tag="[video-process]"` are green.

## 3. Preview reports its cancellation and exits 130

- [x] 3.1 Add the failing cases in `tests/preview/preview_process_tests.cpp` (`[preview]`): with a stop requested, (a) at the window-encode checkpoint, (b) after the render child was terminated by the stop, and (c) after the probe step was terminated by the stop, the run prints `warning: Preview canceled by user.` on stderr, returns 130, and prints neither the window list nor a written-to line, no `Preview generation failed (exit code ...)` and no `Probing skipped` warning; verify with `xmake test-report --tag="[preview]"` that they fail for the current behavior.
- [x] 3.2 Report the abort instead of the child's failure: in `src/preview/preview_process.cpp`, the window-encode stop check, the render-failure handling in `renderAndReportSingleInput` and the probe step in `probeSingleInputPlan` each check `stopsignal::isStopRequested()` and return the cancellation (notice plus `kCanceledExitCode`) before reporting a child failure or starting window encodes; verify cases 3.1 pass and the existing `[preview]` suite stays green.

## 4. Organize reports its cancellation and honors it while copying

- [x] 4.1 Add the failing cases in `tests/organize/` (`[organize]`): a stop requested during analysis and a stop requested during the copy phase each end the run with `warning: Organize canceled by user.` on stderr, exit code 130, no report on stdout, and — for the copy case — the images not yet started are not copied; verify with `xmake test-report --tag="[organize]"` that they fail for the current behavior (analysis aborts with `interrupted: completed analysis is cached` and exit 1; copying ignores the stop).
- [x] 4.2 Report the abort explicitly: `ExecuteStats` (`src/organize/execute.h`) gains a canceled flag set when the copy loop's new per-image `stopsignal::isStopRequested()` checkpoint breaks, and `runOrganize` (`src/organize/pipeline.cpp`) surfaces it on its result instead of the analyze-abort error string; a genuine analysis or copy failure still reports as an error; verify the copy case of 4.1 fails before and passes after, with the analysis-abort message no longer reaching the console.
- [x] 4.3 Map the cancellation at the command boundary: `organize::runOrganizeCommand` (`src/organize/organize_command.cpp`) prints `Organize canceled by user.` through `terminal::messageln(Warning, ...)` and returns `stopsignal::kCanceledExitCode` when the pipeline reports a cancellation, prints the report otherwise; verify both cases of 4.1 pass and `xmake test-report --tag="[organize]"` is green.

## 5. Pin the picture-compression cancellation at the process level

- [x] 5.1 Add an e2e case (`tests/e2e/encro_e2e_tests.cpp`, `[e2e][picture][interrupt][fake-toolchain]`): a picture compression held open by the fake toolchain's gate receives a real console Ctrl+C through `runEncroAsync` + `sendCtrlC` (`SKIP()` when `consoleCtrlEventsAvailable()` is false) and then reports exit 130, `warning: Compression task canceled by user.` on stderr, and no `<path>: exit code <n>` line on stdout; verify with `xmake build e2e_tests && xmake run e2e_tests` — the unit guard `picture pipeline compress cancel prints no per-file failure lines` (added in commit 79046a5) already covers the contract, and this task proves it over a real console event.

## 6. Integration verification

- [x] 6.1 Verify the notice rule end to end for the paths without a process-level case: no stopped stage prints a summary line, plan block, report or success line, and no stage reports a killed child as a failure — run `xmake test-report` (full unit suite), `xmake test-parallel`, and `xmake build e2e_tests && xmake run e2e_tests`, all green. Verified: full unit suite 16177 assertions in 797 cases (11 opt-in skips) with 0 failures, `xmake test-parallel` 797/797 unit cases and 54/54 e2e cases, and the e2e suite 54/54 cases (its assertion count moves with which opt-in console cases skip).
- [x] 6.2 Verify the exit-code contract against the run summary and update the comment that documents the old one: a canceled run's log summary reports `interrupted`, a stop arriving after the last stage still exits 0, and the `runStatus` comment in `tests/app/app_entry_tests.cpp` no longer justifies itself with "preview reports its cancel as an error (exit 1)" — the `runStatus(1, true) == "interrupted"` case stays as "a genuine failure while a stop is pending reads interrupted" — run `xmake test-report --tag="[logging]"` and `--tag="[appentry]"`. Done for both comments (`tests/app/app_entry_tests.cpp` and the same claim in `src/app/app_entry.cpp`); `[logging]` 320 assertions and `[appentry]` 12 assertions pass.
- [x] 6.3 Manual check with a real terminal: `encro -t picture -r --compress -i <dir>` interrupted with Ctrl+C during compression, and a Ctrl+C during `encro --pack-only -i <dir>` — each shows cleared bars, one `warning: <Stage> canceled by user.` line, no failure list, and `echo $LASTEXITCODE`/`%ERRORLEVEL%` reads 130. Compression verified with a real console event (CTRL_BREAK to the child's group) against the fake toolchain: exit 130, stderr `warning: Compression task canceled by user.`, stdout only `Found 2 picture(s)` + `Compressing 2 picture(s) to JPEG (quality=2)...`, no `exit code` line, prompt exit. Pack-only: the input tree packs as ONE archive, so its only checkpoint is the archive boundary — a stop inside the write either lets the pack finish (success, exit 0, per the exit-code requirement) or outlasts the watchdog's grace period (130 without a notice, a change non-goal); the cancel-attributed pack path (stop seen at a checkpoint: 130 + one notice, no failure lines, both modes) is pinned by `tests/pack_execute_tests.cpp` and the `[pipeline]`/`[video-process]` pack callers. Recorded as a deferred issue with its diagnosis in `docs/backlog.md` (coarse pack-only checkpoint).

## Planning-artifact review (before implementation)

Two fresh-reviewer rounds on the artifacts, per the `code-review` skill; every finding was fixed in the planning commit `b989438` and re-verified by a second fresh reviewer.

- `[blocker]` the specs claimed the encode stage already announced a stop, but `Encoding tasks canceled by user.` came only from `confirmEncodingStart`'s declined prompt while `maybeHandleInterruptedEncoding` returned 130 silently — resolved: the requirement, proposal, design and tasks now own the encode stop notice (tasks 2.3/2.4).
- `[major]` "the stop's victims are never reported as failures" over-reached into durable state, which records a stop-killed encode task as `failed` because resume needs it — resolved: the requirement is scoped to what the user is shown, with a resume scenario.
- `[major]` design D2 (a stage's own failure must not be relabelled) contradicted D3 (a killed child is judged by the stop flag) — resolved: D2/D3 rewritten around where the decision is made and why the flag is the only usable predicate for a console-event kill.
- `[minor]` the probing task asserted no exit code although the exit-code requirement lists probing — resolved: task 2.1 gained the run-level 130 assertion.
- `[minor]` `tests/pack/` in the proposal does not exist — resolved: `tests/pack_execute_tests.cpp`.
- `[minor]` the WebP 130 mapping was attributed to the wrong file — resolved in design.md's Context.
- `[minor]` the `runStatus` comment in `tests/app/app_entry_tests.cpp` was left stale — resolved: task 6.2 rewrites it (and the same claim in `src/app/app_entry.cpp`).
- Second round found four precision problems in the fixes (the encode funnel firing when nothing was left to encode; a D2 claim pack does not satisfy; a task claiming a test would be red when it already passed; a comment task that read as deleting a live case) — all four resolved and re-verified.

## Code-diff review (three axes, before the implementation commit)

Three fresh reviewers on `git diff HEAD -- src tests` (Standards, Spec, Leanness), then the fix loop.

**Standards (8 findings).** Promoted `countFfmpegInvocations`, `openGate` and `spawnGatedStop` into `tests/test_utils.h` (four local copies removed, the picture suite's seven call sites switched) — resolved. Renamed the organize stage case, which claimed a loop position the seam cannot observe — resolved (comment documents the limit). Collapsed the pack case's two identical sections into one loop and dropped the resumable half's re-assertion of the existing cancel case — resolved. Gave `reportPreviewCanceled` the progress context so it owns `eraseBars` (eight duplicated call sites became one) — resolved. Dropped the `batchFinished` parameter; the caller gates the call and the function keeps its flag-only contract — resolved. Renamed the gated-stop out-param to `started` — resolved. Factored the per-case capture scaffolds (`runStoppedPreview`, `capturedRun`) — resolved. Rejected one: the same notice wording appearing as a stage notice and as a pack-executor error string is deliberate — they are different roles in different modules, each spec-pinned, and a shared constant would couple them.

**Spec (4 findings).** The requirement demanded a notice for every aborted stage while the watchdog path cannot print one — resolved: the requirement now says that path is best-effort (design non-goal + `docs/backlog.md`). Extra preview stop checkpoints (first probe of both input shapes, the two-input scoring pass) had no test — resolved: two new sections pin them. `!hasEncodingFailures` in the workflow gate made a *completed* batch with a genuine failure report as a cancellation, contradicting the exit-code requirement and the design goal that failure detail is never replaced — resolved: the batch now records a per-task stop victim while the child's result is handled (no result entry, no failure reason, durable status unchanged), so the gate is the action count alone. A stop that forced a packing-prompt decline printed the prompt's wording instead of the stage notice — resolved: `pack::promptDeclineNotice()` picks by the stop flag, used by all three packing-prompt sites.

**Leanness (6 findings, `net: ~-80 lines`).** All resolved by the same promotions and factorings as above; nothing left to cut in the organize, pack and video sources.
