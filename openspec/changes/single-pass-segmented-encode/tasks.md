## 1. Prerequisite

- [ ] 1.1 Restore the build toolchain: the cached xmake config points at `D:\scoop\apps\llvm\23.1.0\bin\clang-cl`, which no longer exists, so `xmake build` fails on reconfigure. Reconfigure (`xmake f -m release -c`) or clear the build cache, then verify `xmake build encro` succeeds and a baseline `xmake test-report` is green before touching code. (Environment fix only, not part of the feature.)

## 2. Command builders and parsers (no ffmpeg required)

- [ ] 2.1 Add the single-pass segment encode config to `src/video/encode_config.h`: one invocation emitting the existing codec/quality/maxrate flags plus `-an`, `-forced-idr 1`, `-force_key_frames "expr:gte(t,n_forced*10)"`, `-f segment -segment_time 10 -segment_format mpegts -reset_timestamps 1`, `-segment_list <segdir>/segments.csv -segment_list_type csv -segment_list_flags +live`, and today's unpadded `seg_%d.ts` output naming, with the progress file staying at the job's existing work-directory path. Verify with a unit test asserting the built command contains the segment-muxer flags and the forced-IDR cadence, contains `-an`, and contains no `-ss`/`-t` for a fresh run while still carrying the progress-file flag.
- [ ] 2.2 Add the resume variant of that config: `-ss <completed * 10s>` and `-segment_start_number <completed>`, with the candidate output path still `seg_%d.ts`. Verify with a unit test asserting both flags and the exact seek value for a completed count of 0, 1 and 12.
- [ ] 2.3 Add segment-list parsing (`segments.csv` -> ordered segment file names with the recorded start/end times), tolerating a truncated final line since the list is written live. Verify with unit tests for a complete list, an absent file, an empty file, a truncated line, and segment numbers above 999.

## 3. Encode runner

- [ ] 3.1 Replace the per-segment loop in `runSegmentedEncoding` with the single invocation: watch the live segment list for new rows, mark each completed segment in job state through the existing record shape (`segmentIndex` + cumulative `resumeTimeUs`), and force-flush per row. Verify with the fake-tool e2e that exactly one encoder invocation is started for the whole file and that the state file shows increasing segment counts as the list grows.
- [ ] 3.2 Implement the resume path: start from the recorded completed count, delete the partially written in-flight segment file, run the resume config, and merge the new list rows onto the recorded boundaries. Resume must also validate the segments the list claims: if a listed segment file no longer exists, rewind to the first missing index; if no uncompleted segment remains but the final output is missing, skip the encoder and go straight to assembly. Verify with fake-tool e2e tests that (a) a resumed run leaves the first N segment files untouched and continues numbering at N, and (b) deleting a listed segment file makes the run restart from that index.
- [ ] 3.3 Build the concat manifest from the segment list (list order, manifest-relative names as today) and keep the existing audio extraction and lossless assembly, including the concat-only path where every segment already exists. Verify with an e2e test that the assembled output file exists after a multi-segment run and that it contains every frame the list names: dropping a listed segment file must make the run re-encode that segment and the assembled frame count must equal an uninterrupted run of the same input, never a silently shortened file.
- [ ] 3.4 Keep the pre-change compatibility fallback: when the segment list is absent, derive completed segments from segment-file existence so a run interrupted before this change (or a deleted list) still resumes. Verify with an e2e case that removes the list but keeps the segment files.
- [ ] 3.5 Delete the now-dead per-segment plumbing: `segmentProgressFilePath`, the runner's use of `parseSegmentEndUs`, the parser itself, its unit tests (`tests/video/video_progress_parser_tests.cpp`), the runner fallback-warning test plus the fake tool's `ENCRO_FAKE_FFMPEG_PROGRESS_NO_END_TIME` emulation that only exists to trigger it, and the single `[fake-tool]` case asserting that emulation (the progress end-time suppression case in `tests/fake_tool_tests.cpp`), leaving the rest of that file's contract cases intact. Move progress reporting onto the single continuous `-progress` counter plus the recorded resume offset. Verify `rg "seg_.*\.progress|parseSegmentEndUs|PROGRESS_NO_END_TIME" src/ tests/` returns no matches at all, and that the remaining progress tests pass.
- [ ] 3.6 Keep the remaining lifecycle behavior: a `--restart` run discards stale segment records and segment files before encoding, and non-video paths (WebP/picture/pack) keep their single-pass behavior with no segmentation. Verify with the existing restart e2e test and the WebP/picture suites passing unchanged.

## 4. Tests and fake tool

- [ ] 4.1 Teach `tests/e2e/fake_media_tool.cpp` to emulate the segment muxer: write numbered `seg_%d.ts` files, append CSV rows for completed segments while it "runs", honour `-segment_start_number`, and support failing after a configurable number of completed segments (the existing `ENCRO_FAKE_FFMPEG_FAIL_MATCH` on a segment file name no longer matches, since the command carries the `seg_%d.ts` pattern). Verify the fake-tool e2e tests pass.
- [ ] 4.2 Update the assertions that assumed one encoder invocation per segment: invocation-log counts, per-segment progress files, failure injection mid-run, and the `writeConcatManifest` unit test in `tests/video/video_process_orchestration_tests.cpp` whose contract becomes list-driven. Verify the e2e and unit suites pass.
- [ ] 4.3 Add a real-ffmpeg smoke test on a long-GOP fixture: encoded segments start with a keyframe, every cut lands within one frame interval after its 10 s mark, and the assembled output's frame count and duration match the source. Verify the test passes locally with ffmpeg on PATH.
- [ ] 4.4 Add a real-ffmpeg interrupted-and-resumed test: encode a prefix, resume, assemble, and assert the frame count equals an uninterrupted run of the same input (the design's frame-exactness evidence). Verify the test passes locally.

## 5. Verification

- [ ] 5.1 Run `xmake test-report`, then `xmake build e2e_tests && xmake run e2e_tests`, then `xmake test-parallel`; all green (judge from the Catch2 logs, not the parallel job statuses), with the WebP/picture/pack suites confirming non-video paths are untouched.
- [ ] 5.2 Re-measure on the same 1080p24 long-GOP fixture used for the proposal numbers: sample `nvidia-smi --query-gpu=utilization.encoder` during a `--crf` run and record wall clock and mean engine-busy percentage. Expect mean engine-busy above 90% and wall clock close to the uninterrupted single-pass time (baseline measured: 24.7 s / 74%; single-pass floor 18.3 s / ~100%).
- [ ] 5.3 Run `xmake fmt -k` and `xmake tidy` and resolve findings in touched files.

## 6. Delivery

- [ ] 6.1 Commit implementation, tests and the ticked `tasks.md` in one atomic commit with a conventional subject (e.g. `perf: encode a video task in one ffmpeg pass`) and a body carrying the measured before/after numbers; verify `git status` is clean afterwards.
- [ ] 6.2 Run the post-change code review (Standards, Spec, leanness) per the repo's `code-review` workflow and resolve accepted findings with a fresh verification pass; verify the review findings are each marked resolved, rejected with justification, or handed to the user.
