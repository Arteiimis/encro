## 1. Tests

- [x] 1.1 Add `[progress]` cases for the two shared predicates in `tests/infra/progress_tests.cpp`: `showsSlotBars` over `(totalTasks ∈ {1, 2, 5}) × (compact ∈ {true, false})`, and `showsOverallBar` over `(totalTasks, workerCount) ∈ {(1,1), (1,4), (4,4), (8,4)} × (compact ∈ {true, false})`; the one-file cells pin the carve-out from design D5 (one slot bar, no Overall bar, in both modes); verify each case fails if a predicate's compact branch is dropped
- [x] 1.2 Add a `[progress]` case for `ProgressContext::barCount()` asserting it counts the bars added (0 before any `addBar`, then 1, then 2 after a second); verify it fails before the accessor exists. Do NOT assert it drops after `eraseBars()` — that method only clears the rendered lines and leaves `bars_` populated (`src/core/progress.cpp:455-465`); `barCount()` reports created bars, not rendered ones
- [x] 1.3 Add a `[encode-probe]` case for `createProbeBars` calling it with `compact = false` and `compact = true` for a batch of 8 files with 4 workers, asserting through `barCount()` that compact creates exactly the Overall bar and full creates the Overall bar plus one per slot; also cover the one-file batch in both modes (one slot bar, no Overall bar) and a full-mode batch of 4 files with 4 workers (slot bars, no Overall bar — the full threshold is `fileCount > workerCount`); verify the compact expectation fails if the function ignores its `compact` argument (this is the regression the change exists to prevent, per design D3)
- [x] 1.4 Add a `[encode-probe]` case that runs `runProbePhase` with an injected `ProgressContext` (design D3) once with `config.fullProgress = false` and once with `true` over the same multi-file batch, asserting the created bar count differs as the rule requires; verify the case fails if `runProbePhase` ignores `config.fullProgress`
- [x] 1.5 Add a `[video-batch-execution]` case asserting `EncodingProgressState` with `compact = true` and more than one task creates no slot bars and does create the Overall bar, read through the existing `slots.barIndexes` / `counters.overallBarIndex` members (`tests/video/video_batch_execution_tests.cpp:72` is the pattern); verify the case passes before the refactor (it pins the behaviour being moved) and still passes after

## 2. Implementation

- [x] 2.1 Add `showsOverallBar(totalTasks, workerCount, compact)` and `showsSlotBars(totalTasks, compact)` to `src/core/progress.h` and rewrite `EncodingProgressState::createOverallBar` / `makeSlotBars` (`src/video/video_batch_execution.h:103-131`) to call them; verify tasks 1.1 and 1.5 pass with `xmake test-report --tag="[progress]"` and `--tag="[video-batch-execution]"`
- [x] 2.2 Add `ProgressContext::barCount()` (`src/core/progress.h` / `progress.cpp`, beside `tickCount()`); verify task 1.2 passes
- [x] 2.3 Extract the probe's bar creation into `createProbeBars`, declared in `src/video/encode_probe.h` so the `[encode-probe]` tests can call it (design D3; that header needs a `core/progress.h` include for the parameter type), and apply the rule: gate the Overall bar on `showsOverallBar(vids.size(), workerCount, compact)` and the slot bars on `showsSlotBars(vids.size(), compact)` (design D2 — the batch size, not the post-cache task count), keeping the post-cache `slotCount` as the size of the bars that are created; verify task 1.3 passes and `xmake test-report --tag="[encode-probe]"` stays green
- [x] 2.4 Give `runProbePhase` the optional injected `ProgressContext` parameter (design D3) and pass `!ctx.config.fullProgress` as `compact`; verify task 1.4 passes and `xmake test-report --tag="[encode-probe]"` stays green
- [x] 2.5 Guard the per-task bar updates in `buildProbeTaskSpec`'s lambda on a non-empty `slotBars` — the `slotBars[slot]` read at `src/video/encode_probe.cpp:567` and both local lambdas — while leaving `slotProgress`/`completed` updating unconditionally (design D4); verify the `[encode-probe]` cases pass
- [x] 2.6 Correct the `runProbePhase` doc comment in `src/video/encode_probe.h` to describe the mode-dependent layout instead of "one progress bar per file"

## 3. Verification & commits

- [x] 3.1 Run `xmake test-report` (full unit suite) and confirm zero failures
- [x] 3.2 Run `build/windows/x64/release/tests.exe -r console -s` and confirm 0 failures (the reporter-mode probe in AGENTS.md)
- [x] 3.3 Run `xmake fmt` before committing; verify a second run produces no further changes
- [x] 3.4 Commit the planning artifacts first as their own `docs:` commit, then implementation + tests + ticked `tasks.md` in one `fix:` commit (English, conventional, subject < 72 chars, body wrapped at 80); verify with `git log --oneline -2` that the split is docs-then-fix
