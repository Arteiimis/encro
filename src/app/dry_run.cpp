#include "app/pipeline.h"
#include "core/error_handle.h"

namespace pipeline {

auto runDryRun(appctx::AppContext const& ctx) -> eh::Result<int> {
  // Stub: Task 2 will replace this with the full three-layer implementation
  (void)ctx;
  return 0;
}

}  // namespace pipeline
